# 行止开发接力指南

> 本文件位于仓库根目录，供团队每轮开发完成后互相交接并更新。它记录接手方法和最近一次交接摘要；详细产品规则以 `xingzhi/doc/` 为准，唯一阶段状态见 `xingzhi/doc/04-development.md`，避免在多处维护不同版本的开发进度。

## 1. 先了解项目

行止依托银行场景，帮助青年用户理解已发生的收支、整理账目、安排近期资金，并在收入、支出或计划变化后调整安排。核心体验是“看懂收支—安排资金—应对变化”；AI 用于理解需求、解释事实和辅助规划。商品、购买、支付与退款是已有消费场景能力，不应主导产品定位。

当前仓库服务于本地联调和竞赛演示，不代表已接入真实银行或正式上线。Demo 数据、Simulation 支付、支付宝 Sandbox、模型辅助和用户确认各有边界。项目不允许把预计收入或退款当成已到账现金，也不允许 AI 或商户代替用户确认资金操作。

## 2. 推荐阅读顺序

| 顺序 | 文档 | 接手时要解决的问题 |
| --- | --- | --- |
| 1 | [仓库 README](README.md) | 环境要求、配置、启动方式、服务地址、小程序导入路径和常用命令 |
| 2 | [产品目标与功能范围](xingzhi/doc/01-product.md) | 当前服务谁、主体验是什么、哪些功能已经实现或明确不在当前范围 |
| 3 | [业务口径与关键规则](xingzhi/doc/02-business-rules.md) | 资金、预算、流水、退款、用户确认和权限如何解释 |
| 4 | [架构与代码导航](xingzhi/doc/03-architecture.md) | 前端、服务端、Worker、共享契约、主要数据与路由入口在哪里 |
| 5 | [当前进度与后续工作](xingzhi/doc/04-development.md) | 哪些只有代码、哪些有执行记录、下一步顺序和待核实项 |
| 6 | [验收与演示](xingzhi/doc/05-acceptance-and-demo.md) | Simulation、微信工具、真机、Sandbox、跨角色证据和录制如何验收 |

根目录 README 是启动入口；阶段状态只在 `xingzhi/doc/04-development.md` 维护，验收操作与结果模板只在 `xingzhi/doc/05-acceptance-and-demo.md` 维护。根目录和 `xingzhi/doc/` 中的前端设计参考稿不是已批准的交付契约，不可据此改变业务规则或验收门槛。

## 3. 开始接手前

1. 与当前负责人确认目标分支和正在处理的模块，然后检查 Git 分支及工作区状态。不要覆盖或清理他人未提交改动；如果代码由另一位开发者正在修改，先约定文件范围和合并顺序。
2. 从仓库根目录按 README 准备 Node.js、pnpm、PostgreSQL 和本机配置。将 `xingzhi/.env.example` 复制为本机 `xingzhi/.env` 后填写必要值；模板只说明配置项，不应把任何真实密码、密钥或支付凭据提交回仓库。
3. 初次联调只使用隔离的本地开发数据库。运行迁移前查询该目标库的 `schema_migrations`；仓库中存在迁移文件不代表该数据库已经执行。不要在未知或共用数据库上重复 Seed、删除数据或手工改状态。
4. 按 README 启动服务。开发者工具导入 `xingzhi/apps/miniapp` 项目目录，而不是 `dist/weapp`；热点下的真机调试使用 `-Mobile` 编入电脑局域网地址，不能使用电脑的 `localhost` 或 `127.0.0.1`。正式预览和发布仍需微信合法域名的 HTTPS API。

每位开发者完成一轮工作后，都应更新本文件末尾的“最近一次交接摘要”，让下一位知道改动范围、真实执行过的检查、尚未完成的事项和建议接手点。接力摘要只保留最近一轮；若涉及阶段状态变化，同时更新 `xingzhi/doc/04-development.md`，不能只改本文件。

本机 `.env`、微信 `project.config.json`、`project.private.config.json`、证书/私钥、开发者工具缓存及个人导出数据均不属于 Git 共享交付。`project.config.json` 含本机 AppID 和项目设置，需由负责人通过私下渠道提供；`.gitignore` 已覆盖这些本机配置和密钥文件。不要用 `git add -f` 绕过忽略规则。如果不确定某文件是否可提交，先只核对文件名和用途，不要把凭据内容贴进聊天或文档。

## 4. 代码从哪里看

| 工作内容 | 主要入口 |
| --- | --- |
| 消费者小程序与 H5 | `xingzhi/apps/miniapp/src/pages/`、`composables/`、`lib/` |
| 后端路由 | `xingzhi/apps/server/src/routes/` |
| 资金与预算规则 | `xingzhi/apps/server/src/domain/`，重点查看 `budget-periods`、`budget-cashflow`、`cashflow-engine`、`finance-facts` |
| AI 运行与规划草稿 | `xingzhi/apps/server/src/domain/consumer-agent-runtime.ts`、`consumer-planning-tools.ts` 及相关 routes |
| 数据库与异步工作 | `xingzhi/apps/server/src/db/`、`apps/server/src/worker.ts` 与 Worker runtime |
| 前后端共享契约 | `xingzhi/packages/contracts/src/` |

新消费者流程以自然月 `budget_periods` / `budget_items` 为主；旧 `plans` / `plan_items` 主要用于历史兼容，新增消费者功能前先读架构和业务规则，避免接入错误模型。资金结论、确认、订单和退款边界以服务端及共享契约为准，页面不能自行重算或跳过用户确认。

## 5. 当前接力重点

当前唯一阶段状态和优先顺序见 [04-development.md](xingzhi/doc/04-development.md)。该文档记录：D1/D2 已交付；F1/F2 有实现及本地 H5 场景记录；F3.1—F3.3 有代码及定向服务端回归记录。F3 服务端、真实模型与指定 H5 草稿操作已完成定向复验；微信/真机完整财务主线仍待执行，不能把代码存在写成完整验收通过。开始设备验收前，先完成 Windows 启动脚本可靠性收尾，并在微信开发者工具重新核对当前包体。

后续工作按“启动入口收尾 → 微信包体/启动检查 → 微信完整财务主线 → 真机最小主线 → 财务 Demo 录制”推进。之后再按交付范围决定是否进入交易专项和补最小前端自动化冒烟。两条验收线不能互相替代：

- 财务主线：按开发状态中的顺序完成微信开发者工具主线、至少一台真机最小主线，再按脚本录制财务 Demo。
- 交易专项：若交付范围包含渠道交易，仍需全新支付宝 Sandbox 订单连续完成付款、主动查单、取消退款和退款查单，并核对商户处理及审核证据。纯财务主线通过不代表这条线通过。

不要另起一份阶段计划，也不要仅凭本指南推断本机数据库、真实模型、微信设备或支付渠道已经验收。

## 6. 微信开发者工具：本地财务主线测试

本节是接手者执行下一项验收的本地操作指引；完整通过条件与结果模板见[验收与演示文档](xingzhi/doc/05-acceptance-and-demo.md)。本次尚未执行开发者工具或真机验收，以下是待执行步骤，不是通过记录。

### 6.1 准备本地环境

1. 从仓库根目录阅读 [README 的环境和启动说明](README.md#2-环境与本地配置)。确认 Node.js 22、pnpm 11、PostgreSQL 已就绪；按需从 `xingzhi/.env.example` 准备本机 `xingzhi/.env`，只在本机填写数据库、演示账户及服务配置，不把文件或配置值发到仓库、聊天或截图。
2. 确认 `.env` 指向专用本地验收数据库，而不是团队共用库或生产库；支付模式使用 `PAYMENT_MODE=simulation`。仓库有迁移不等于目标库已执行，按[架构与代码导航](xingzhi/doc/03-architecture.md)核对目标库 `schema_migrations`。需要全新场景时使用独立数据库和唯一场景键，并遵循验收文档；不要清空当前 Demo 库或对已有场景重复 Seed。
3. 当前本地 Demo 库已有验收草稿、运行记录和两个正式计划测试项目。若正在使用这份数据，保留原状，不重新 Seed，也不要为凑出预期页面状态而直接改数据库。没有可用演示账号或合适的流水场景时，先向负责人确认隔离场景与账号，不要把凭据写进本指南。

### 6.2 启动 API 和小程序监听编译

在仓库根目录运行：

```zsh
./start-xingzhi.zsh --restart --no-browser
```

脚本会启动本地 API、Worker、H5/管理端及微信小程序监听编译；`--no-browser` 只是不自动打开 H5 浏览器，不会跳过小程序编译。等待终端显示微信监听构建成功，并确认 `xingzhi/apps/miniapp/dist/weapp/app.json` 已生成或更新。然后在浏览器打开 `http://127.0.0.1:8877/api/health`，确认健康检查成功。若 API 未就绪或监听构建失败，先按验收文档记录第一条错误并处理，不要继续把旧产物当成本轮构建结果。

### 6.3 导入并编译

1. 确保已从负责人处私下取得 `xingzhi/apps/miniapp/project.config.json`，再打开微信开发者工具选择“导入项目”，项目目录选择仓库中的 `xingzhi/apps/miniapp`。不要导入 `dist/weapp`；本机项目配置的 `miniprogramRoot` 应设为 `dist/weapp/`。
2. 确认开发者账号对项目使用的 AppID 有权限。若提示无权限，联系项目负责人处理，不要为绕过权限而改写或提交项目 AppID/本机私有配置。
3. 导入后等待监听编译完成，在开发者工具中点击“编译”。检查模拟器能进入登录页，控制台没有阻断启动的构建或运行错误。小程序本机 API 默认是 `http://127.0.0.1:8877`；`project.config.json` 中的本地 URL 校验设置只服务于本机开发，不代表可用于真机或生产。登录后可在“我的 → 应用设置 → 演示环境”核对编入产物的 API 地址。

### 6.4 按财务主线逐页核对

使用已有隔离 Demo 账号，不进入购买、付款、取消退款、Sandbox 或商户审核流程；这些是独立验收线，不是本次财务主线的前置步骤。

1. **首页、账户与流水：** 登录后核对账户归属、当前余额与资金事实；进入账目查看已发生流水、退款和未来义务的区分，不把预计收入或待到账退款算成当前现金。
2. **月度统计：** 查看当前月统计，切换到已有历史月；核对摘要与流水列表同属所选账户和月份。若场景没有对应历史数据，记录实际空态，不补造数据。
3. **近期规划与变化影响：** 查看本月安排及未来 30 日派生视图。选择可调整收入日期或新增必要支出，先查看变更前后影响，再由执行者明确确认保存；未确认前不得把建议当成正式计划。
4. **AI 解释与草稿：** 从当前资金/月份上下文发起只读解释，核对答复中的金额、未知原因与页面事实一致。再提交一个规划需求，检查用户提供、建议及待补充字段的区分；完整草稿只读预览影响，字段不足时补充信息。AI 不代替用户确认，也不应在这条财务主线中创建订单。
5. **计划与实际复盘：** 若隔离场景有同账户、同月的已入账普通流水和未承诺计划，按页面流程确认关联，核对实际金额只计一次、剩余计划正确；检查刷新/重新进入后的结果。可在场景允许时再核对解除后流水仍保留。没有适用流水时记录未覆盖，不要把订单流水、退款或未来义务拿来凑数。
6. **恢复与页面检查：** 刷新或重新进入相关页面，确认计划、草稿、流水和关联结果恢复一致；检查首页、计划、账目、AI/草稿、我的五个标签页，以及短屏、大字号、底部面板和一次断网恢复。具体通过条件按[验收清单与结果模板](xingzhi/doc/05-acceptance-and-demo.md)记录。

记录开发者工具版本、基础库版本、分支与提交、使用的隔离场景、API 地址、每一步结果及第一条错误。截图和日志不得包含密码、密钥、完整账户号或个人资料。微信开发者工具通过后，真机仍需单独执行；手机不能访问开发机的 `127.0.0.1`。本地热点真机调试使用 `-Mobile` 打印的局域网地址，正式预览和发布使用手机可访问的 HTTPS API 与合法域名。

## 7. 如何记录交付

- 开始前记录当前分支、提交和工作区状态；结束时说明实际修改范围及需要他人接手的事项。
- 只有确实执行过的检查才写入结果，记录日期、环境、命令或操作、覆盖范围和通过/失败/未执行状态。代码检查、浏览器联调、微信工具、真机和外部支付分别记录，不合并成笼统的“全部通过”。
- 新的阶段进度更新到 `xingzhi/doc/04-development.md`；手工验收步骤或每次验收结果更新到 `xingzhi/doc/05-acceptance-and-demo.md`。不要在 README 复制一套进度或验收数字。
- 记录中不放密码、密钥、完整账户号、个人资料、付款凭据或模型私有推理。必要的场景与业务编号按项目约定脱敏，并把截图和录屏存放在已忽略的本地输出目录。
- 本指南不固定提交号。交接时以实际 Git 历史和远程分支为准；提交或推送状态必须经 Git 核实后再告知团队。

## 8. 最近一次交接摘要

> 每轮完成开发后，用最新事实替换本节；不要把尚未执行的检查写成通过，也不要复制一份完整开发计划。

- 日期：2026-09-25。
- Git 基线：审查时本地分支为 `huixiang`，HEAD 为 `d277d4d`；本轮文档更新前与 `origin/huixiang` 同步且工作区干净。本段是审查快照，后续仍以 Git 实际状态为准。
- 本轮范围：合并后审核现有代码、页面、接口、文档、迁移、测试和启动入口；没有修改业务代码。源码未发现 TODO/FIXME、敬请期待、空按钮或纯占位页面，小程序登记 34 个页面和 10 个公共组件。
- 验证边界：本机目标库应用到迁移 `035` 后，`pnpm typecheck`、`pnpm test:targeted` 86/86、消费者 H5 构建、微信小程序构建和商户/审核网页构建通过。2026-09-25 真机调试以 80051 确认旧主包约 3,140 KiB 超限；分包后本地测得主包约 605.8 KiB，6 个主包页面和 28 个分包页面产物齐全。该测量仍不替代开发者工具重新上传和真机主线验收。
- 工程状态：`start-xingzhi.ps1` 已补入 IPv4 `localhost` 探测、残留 Vite 清理和 `-Mobile` 热点真机联调模式；Windows/macOS 启动入口的小程序监听构建使用压缩模式，非核心页面已分包。旧 `stash@{0}` 不再是待合入来源，不要整包恢复。根目录 `pnpm build` 不包含小程序构建，完整构建需另跑两个 miniapp 命令。前端暂无自动化 UI 测试。
- 未完成事项：按开发状态文档第 4 节依次完成启动入口收尾、微信包体/启动检查、微信完整财务主线、真机最小主线和财务 Demo。交易专项按 Demo 范围另行决定，不能由财务主线代替。
- 接手提示：保留本地 Demo 中的验收草稿、运行及两个新增计划项目；不要重复 Seed 当前 Demo 库，也不要提交本机环境配置、会话、密钥、微信私有配置或浏览器输出。
