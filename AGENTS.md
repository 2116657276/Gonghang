# 项目开发约定

## 当前项目边界

- 本仓库当前以工行杯项目的研究、方案评审和竞赛文档为主。除非用户明确授权，不创建应用代码、测试脚本、部署配置、CI 工作流或外部服务集成。
- 行止是当前第一优先项目；潮汐与绿链智算仅按已有资料维护。不得把方案、设想或待验证能力表述为已经实现、接入、上线或验证完成。
- 先阅读与任务直接相关的现有文档，沿用已确定的术语、结论和文件结构；不顺带重写、清理或重构无关内容。

## 开发与设计原则

- 以完成用户当前目标为准，优先采用最小、直接、易于理解的改动；复用仓库已有做法，不为假设中的未来需求预建抽象层、通用框架、配置项或扩展点。
- 不因“可能有用”而新增依赖、脚本、目录、文档体系、工程化流程或复杂架构。确有必要时，说明其与当前目标的直接关系。
- 只处理任务中明确要求、代码或文档中已观察到、或会直接影响本次结果的边界情况。无需为纯假设的极端输入、跨平台兼容、超大规模、并发、部署环境或遥远未来的迁移预先设计。
- 以上简化原则不适用于已经明确的安全、隐私、数据损坏、资金操作、权限控制或合规风险；遇到这些风险时，应如实指出并采用与风险相称的处理方式。

## 验证与测试

- 验证应与改动类型和风险相称，先选择最窄、最快且足以证明本次改动的检查。文档改动通常只需复读受影响内容、检查链接或格式，并查看差异。
- 不默认计算哈希值、校验和或文件指纹；仅在任务明确要求完整性校验、需要核验大文件/交付物一致性，或现有流程已将其作为必要步骤时执行。
- 不默认执行全量编译、全量测试、完整打包、跨平台验证或耗时的集成检查。仅在用户明确要求、改动直接影响构建/发布路径，或项目既有的必要门禁要求时执行。
- 不为每次改动新建测试，也不批量补测、追求覆盖率或引入测试框架。仅当用户要求、现有规则要求，或本次实现了可执行且有明确回归风险的行为变更时，补充最少的定向测试。
- 如果未运行某项较重的检查，应简洁说明原因和已完成的替代验证；不得把未执行的检查表述为已通过。

## 工作节奏

- 先检查相关文件与现有约束，再动手修改；信息足够时直接推进，不为低影响细节反复索取确认。
- 将发现但与当前任务无关的问题单独说明，不擅自扩大范围处理。
- 完成后简要报告：修改了什么、做了哪些必要验证，以及仍未验证的事项（如有）。
