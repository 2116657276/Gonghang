# 决策台账

| 字段 | 内容 |
| --- | --- |
| 文档编号 | XZ-DEC |
| 更新日期 | 2026-09-12 |
| 状态 | P0 开发基线及 S4 选型已确认；交易执行工具、事件恢复、T10 复核和变更善后连续链已作本地受控核对，真实模型仅有局部评测，本轮实现与检查结果以开发计划为准；429 退避切片的 36 项自动检查仍属历史证据，S4／S5 未放行 |

## 一、已确认决定

| 编号 | 决定 | 依据与影响 |
| --- | --- | --- |
| DEC-01 | 行止第一优先，潮汐与绿链备选 | 用户明确决定；不并行建设三个项目 |
| DEC-02 | 先统一 P0 开发基线，再启动本地实现 | 建立基线时已安装全局 TypeScript CLI；项目依赖、支付联调和真实资金按阶段推进，支付联调和真实资金须另行授权 |
| DEC-03 | 可以复用成熟技术；同类获奖不自动淘汰 | 来源、许可、自主贡献与效果分别说明 |
| DEC-04 | 必须完整购买—付款—变更—取消／退款—结果核验 | 用户明确“必须做到完整闭环”；替代原仅演示付款退款的简述 |
| DEC-05 | P0 首发为响应式网页，小程序后续 | 网页覆盖电脑与手机展示；小程序不阻断基础闭环 |
| DEC-06 | 官方沙箱为 P0 交易验证基线，普通付款入口先行 | 纯模拟不计交易通过；官方 Agent 支付是独立增强；账号不适用则报告阻塞重评 |
| DEC-07 | P0 为模块化单体、持久数据库和后台执行者 | 一个业务后端与后台执行者共享数据库；不拆微服务 |
| DEC-08 | 首版使用自己的测试商户承接购买与善后 | P0 固定 A 去程交通 420 元、B 住宿 880 元、C 景点 180 元、D 接驳 120 元；计划总购买上限 3000 元、购买授权有效期 24 小时，仅作为测试参数 |
| DEC-09 | 使用累计购买上限，退款不自动恢复购买空间 | 需要再次购买时必须取得新的购买确认；不以退款重置旧授权账本 |
| DEC-10 | 测试商户按已配置规则自动批准或拒绝取消，并由商户执行原单退款 | 用户确认不等于商户退款权；费用、拒绝和延迟均保留明确状态与人工路径 |
| DEC-12 | 自定义 Pi agent-core 单 Agent、多工具 | 用户明确选择；不叠加 LangGraph、不深度派生完整编程助手；见 ADR |
| DEC-13 | 先整合现有仓库文档，再建行止体系 | 已完成顺序调整；保留必要历史研究，清理重复和无关导航 |
| DEC-15 | P0 首发为响应式网页、TypeScript 模块化单体、Vue 3/Vite 前端、Fastify 业务 API、PostgreSQL 和同库独立后台执行者 | PostgreSQL 是授权、订单、支付、退款和任务的唯一业务事实库；本机 Redis 仅可按需用于非关键缓存或限流，不作为 P0 依赖、队列或交易状态来源；不引入微服务、消息队列、多 Agent 或跨端框架作为首发前提 |
| DEC-16 | 首次 P0 包含费用预览、用户接受后的部分退款、多次部分退款、商户拒绝／延迟和明确失败处理 | P0 目标是测试商户内完整、可解释的购买与善后闭环；不扩展到真实商户、钱包、分账或自动垫付 |
| DEC-17 | P0 使用服务端会话识别消费者、测试商户管理员和只读审核者 | 身份与角色不能由前端切换或模型参数决定；具体身份提供者可在保持该契约前提下实现 |
| DEC-18 | 购买授权、善后授权和查询授权分开保存 | 购买确认创建购买授权；变更确认创建订单范围内的善后授权；暂停只影响购买授权，撤回查询权限停止新增受托查询 |
| DEC-19 | 第一开发阶段先完成本地 P0 业务基础纵切 | 建立 Vue 3/Fastify/PostgreSQL 本地应用、服务端角色、A/B/C/D 测试目录、授权／暂停／方案／订单／任务持久化与本地恢复；不把本地模拟写成支付沙箱通过 |
| DEC-20 | 创建订单即占用计划预算，付款交接不重复占用 | 用户确认推荐方案；已付款转入累计支出，明确未付关闭／未受理失败后释放，未知继续占用，纯计划项不占用 |
| DEC-21 | 同计划跨购买授权继承账本 | 用户确认推荐方案；恢复默认沿用原计划总上限，提升上限须明确确认，不额外发放 3000 元；旧授权不能因新确认重新生效 |
| DEC-22 | 变更确认分别建立善后和查询授权，P0 默认各 7 天 | 用户确认推荐方案；到期／撤回停止新增对应受托动作；已受理交易与商户自身核对职责保留，续期不重复取消退款 |
| DEC-23 | P0 分次退款采用一次取消确认总额内分批执行 | 用户确认推荐方案；800 元退款可分两笔各 400 元，独立固定退款号；总额或费用变化重新确认 |
| DEC-24 | 首个完成的 MVP／Demo 覆盖全部 P0 基础能力 | 用户明确要求基础功能尽量齐全、后续逐步深化；S1—S4 为中间里程碑，S5 放行才是首次完整交付；不能后移 Pi、付款退款、权限和必要恢复 |
| DEC-25 | 分阶段开发计划统一实施顺序，领域／接口／验收各保留唯一职责 | 用户允许删减合并冲突和重复文档；实施顺序只在 XZ-DEV 维护，历史材料不参与现行规则解释 |
| DEC-26 | 采用适合本地 Demo 的直接实现与运行默认值 | 依据用户授权按推荐方案完善：pnpm 工作区、参数化 SQL／迁移、测试账号与 PostgreSQL 会话、本地参数和数据政策；具体见架构与运行文档。这些是开发默认值，不冒充依赖兼容实测、生产政策或用户模型费用授权 |
| DEC-27 | 当前先完成本机开发，云服务器与固定域名后置评估 | 本机可实现并验证内部业务、支付适配器、配置校验、受控付款交接和主动查单；官方付款、退款、关单及通知联调仍按沙箱条件和单独授权推进。异步通知验收须渠道可访问的 HTTPS 地址，可经用户授权采用临时隧道，或在后续云服务器部署后使用固定地址 |
| DEC-28 | S2 外部条件未关闭时，先推进 S3 服务端本机准备 | 用户明确指定；只实现商户协同、退款批次、人工任务与恢复的内部约束，不发起外部资金操作，不改变 S2 官方交易证据仍为 S3 正式出口前置条件的规则 |
| DEC-29 | 本机官方沙箱演示不把异步通知作为前置条件 | `notify_url` 可选；无 HTTPS 时，付款返回后由 API-06R 主动查单并核对订单号、金额、卖家和交易状态。异步通知仍可作为后续增强路径，启用时才要求渠道可访问的 HTTPS 地址 |
| DEC-30 | S4 固定使用 Pi `@earendil-works/pi-agent-core@0.85.1` 与 `@earendil-works/pi-ai@0.85.1` | 2026-09-07 依据 Pi 官方 releases、package.json 和 README 核对；Node 要求 `>=22.19.0`、许可证 MIT。精确版本已写入项目依赖和锁文件；两个包已安装，类型检查、本地事件／工具循环及真实只读／方案联调通过，交易执行和取消／恢复已有本地受控行为证据，完整真实模型／官方渠道链仍待验收 |
| DEC-31 | 原模型服务选型（历史基线）：DeepSeek OpenAI 兼容接口的 `deepseek-v4-flash` | 基地址 `https://api.deepseek.com`；官方模型与价格资料于 2026-09-08 核对。凭据只由服务端读取；真实只读／方案调用和人民币 usage 已实测，完整交易调用仍待验收 |
| DEC-32 | Agent 模型调用累计费用上限为人民币 100 元，并采用常规服务端限流 | 费用由本地账本按实际 usage 和官方人民币费率计算；预算不足时拒绝新调用。默认每个已认证消费者每分钟 10 次 Agent 运行请求、突发 2，同一计划同时 1 个运行；上游 429 若带 `Retry-After` 则遵守，否则按本地退避，最多 3 次。该配置是本项目保护阈值，不是 DeepSeek 账号额度或价格承诺 |
| DEC-33 | 当前模型切换为 DeepSeek V4.1 Flash，官方 OpenAI 兼容 API 标识为 `deepseek-flash`（用户简称“ds4.1flash”） | 2026-09-12 依据用户要求同步 `.env`、`.env.example`、运行代码与文档；官方模型标识及费率已核对，本地请求体测试已覆盖新标识。既有 `deepseek-v4-flash` 运行记录保持历史属性，不迁移为新模型业务证据 |

已确认业务参数以本台账关联的领域规则为准；本地实现默认值以架构与运行文档为准；Pi／DeepSeek 历史公开选型、API key、本地循环、人民币费用账本、旧模型真实只读／方案联调和局部执行评测已核实，当前模型为 DeepSeek V4.1 Flash（API 标识 `deepseek-flash`，用户简称“ds4.1flash”），交易工具、T10 复核、变更善后和事件恢复已有本地受控证据，官方支付、新模型完整业务链和综合验收仍按开发计划推进。429 切片的自动检查是历史证据；恢复开发后的实现与检查结果以开发计划为准，不据此假定完整验收已经通过。日期均为本次整理记录日期；不编造独立签字、人员或联调结论。

## 二、已确认的定位与交互规则

| 编号 | 决定 | 影响与重评条件 |
| --- | --- | --- |
| DEC-11 | 以部分计划变更和交易善后为核心项目表述 | 已确认工作定位；用户价值仍需访谈和实测 |
| DEC-14 | 选购与变更用结构化卡片确认，模型无确认工具 | 确认可批量确认明确范围，付款仍按渠道要求 |

DEC-12、DEC-30—DEC-32 是正式 S4 选型与保护边界；Pi 安装、本地无资金循环、真实只读／方案调用和人民币 usage 已完成，交易执行、官方沙箱和完整恢复仍需按开发计划验收。官方沙箱仍是 P0 交易验证基线；沙箱不通时不得用模拟假装满足 DEC-04。

## 三、变更记录

以下记录保留决策依据；日期按各条记录的实际日期列出，不要求与文件追加顺序一致。较早条目是当时的状态快照，不能覆盖文档顶部及后述条目中的当前状态。

2026-09-06（本机优先开发）：用户确认当前不配置固定域名，先完成本机开发，云服务器部署后续评估。固定域名不阻断本机适配开发；异步通知验收仍需渠道可访问的 HTTPS 地址与单独授权，但本机官方沙箱演示改用主动查单，不以通知地址阻断。

2026-09-06（S3 服务端先行）：用户指定在 S2 外部条件未关闭时继续 S3 后端开发。允许实现本机数据、权限和恢复约束；官方调用、沙箱结论与 S3 出口验收继续受 S2 条件约束。

2026-09-06（阶段计划补全）：用户确认建单占用、计划级跨授权预算、善后／查询各 7 天和确认总额内分次退款，明确首个 MVP／Demo 基础功能齐全并允许后续深化；新增 XZ-DEV 作为唯一开发顺序，补齐身份、商户、查询续期、Agent 与人工处理契约，合并其他文档的重复阶段表。该记录形成时尚未开始编码；后续 S1 本地实现与验证结果以开发计划和本地运行说明为准。外部账号、渠道适用性、Pi 版本与模型资源仍未核实。

2026-09-06：统一 P0 开发口径：确定响应式网页、Vue 3/Vite、Fastify、PostgreSQL 与独立后台执行者；本机 Redis 只作为按需的非关键能力；将费用退款、多次部分退款、商户拒绝／延迟纳入 P0；固定 A/B/C/D 测试参数；补齐服务端角色、三类授权生命周期与 P0 接口语义；确定第一开发阶段为本地业务基础纵切。全局 TypeScript CLI 已安装。未把账号、沙箱、Pi 发行版、模型或联调结果写成已完成。

2026-09-07（Pi 与模型选型；历史状态）：用户确认选用最新 Pi agent、DeepSeek v4 flash，费用上限人民币 100 元并采用常规限流。公开资料核对后锁定 Pi v0.85.1、`@earendil-works/pi-agent-core`／`@earendil-works/pi-ai` v0.85.1、DeepSeek `deepseek-v4-flash` 和 `https://api.deepseek.com`；当时实际安装、凭据、网络调用、usage 和费用仍待 S4 烟测，不把选型写成接入完成。

2026-09-08（本轮实现与核对；历史状态）：两个 Pi 包已安装并锁定；本地确定性工具循环、未注册确认工具拒绝、共享费用预算及渠道／权限定向检查通过，共 22 项自动检查、全项目类型检查和服务端构建通过。当时模型配置尚未完成，真实 DeepSeek 烟测未发送；官方付款、退款、关单、通知及 36 项完整验收仍未完成，随后由后述记录更新。

2026-09-05：整理仓库总览和项目入口，标记统计工作簿与潮汐旧研究边界；将行止早期计划书归档、平台研究归入研究目录、Demo 归入产品目录，原路径保留导航；补齐产品、领域、Pi、接口、数据、验证、运行和项目叙事。

原台账曾在实际迁移未完成时写为“已完成”；本次整理已纠正，并以实际文件和链接检查为准。所有文档操作不记录为软件或交易完成。

后续重要决定记录理由、批准依据、影响文件与重评条件；技术细节未确认时不以默认建议代替批准。

[Pi ADR](../03-engineering/adr/0001-pi-single-agent.md) · [评审议题](review-agenda.md)

2026-09-08（人民币计费与只读 Agent 更新；阶段记录）：按用户纠正直接采用官方人民币费率，取消汇率配置。真实只读模型烟测通过，保守费用估算 0.002286 元。API-24 只读运行、四个只读工具、持久限流与运行取消已实现，该阶段有 28 项定向检查通过；随后 T04／T08 方案联调将当前总数更新为 31 项。写工具、事件恢复及官方交易完整验收尚未完成。

2026-09-08（方案工具与页面）：T04／T08 复用现有方案业务服务并与 WAITING_USER 原子提交；确认入口保持独立 UI。31 项定向测试通过，真实模型在虚构计划上生成购买／变更方案并恢复卡片；本轮没有执行确认或交易。累计模型预算估算 0.018827 元，官方交易与剩余执行工具仍未放行。

2026-09-10（429 切片与开发暂停；历史状态）：显式 HTTP 429 退避、逐次预算检查及结算等待完成了本地受控切片；该切片记录 36 项自动检查和服务端类型检查通过。随后工作区写入交易工具、事件恢复、009 迁移和页面衔接初稿，未完成最终类型与行为复核；当时用户要求暂停开发、仅整理设计，故不把 36 项历史结果扩展为当前工作区或 S4／S5 的通过结论。2026-09-11—12 恢复开发后的实现与检查结果以开发计划为准。
