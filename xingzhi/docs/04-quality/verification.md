# 新版验证策略与实际状态

更新日期：2026-09-16。状态：001—028、A00—A06 与 B00—B07 已进入当前本地代码基线；M1 受控 simulation 原范围目标套件已有 69/69 记录，模型费用账本隔离修复已应用但本工作区尚未重新执行完整套件确认。补充收口和消费者 HTTP 连续验收待执行（见第十四节），暂不放行 M2。所有前端/沙箱类 N 案例仍未执行；原范围检查通过不等于 M1 补充收口或新版产品全部 N 案例通过。

## 一、判据与证据

新版 P0 与 N 编号案例唯一见[Demo 与验收](../01-product/demo-and-acceptance.md)。原 36 项 P／A／R 和旧 FR 编号的实际结果保存在[历史验收](../archive/transaction-verification.md)，只作为对应交易行为的证据，不再构成新版全部放行门槛。历史测试总数不换算为新完成率。

| 能力 | 当前证据 | 新版结论 |
| --- | --- | --- |
| 原交易后端 | 历史 55 项检查、授权和金额、恢复样本 | 按未变路径复用；新跨模块逻辑未通过 |
| 支付宝沙箱 | 0.01 元付款及原单退款、另单待付关单核心链 | 渠道子链已有证据；新消费规划连续链未执行 |
| 账户、账目、还款、资金安排 | A00—A06 与 B03—B07 simulation 连续链 | 原领域主链通过；补充收口、HTTP 连续链、真实银行和新沙箱链未验 |
| 空状态 Agent、多候选、规划调整 | 新消费者 Pi 运行只注册三项规划工具；无账户草稿和 ¥400 调整选项已检查 | 工具接线局部通过；上下文和产物交接待收口，真实模型待代表性检查 |
| 统一前端与用户价值 | 无新场景证据 | 待 M2／M3 |
| 银行真实接入和宿主 | 无项目可用性证据 | 不在受控 Demo 已完成范围 |

## 二、按大阶段检查

M1 内按 B-Ⅰ／B-Ⅱ／B-Ⅲ 分批集中验证，最终作 A/B 联合验收；已通过且未变化的证据复用，不再全量重跑。核对迁移、种子、金额日期、流水关联去重、同账户准入和 Agent 权限；M2 集中做浏览器连续流程与必要角色检查；M3 完成新版 N 案例、沙箱连续演示、用户观察和交付构建。具体顺序见[开发计划](../03-engineering/development-plan.md)。

只对实际行为变更补最少回归检查，不建立覆盖率目标。已有代码、环境和输入条件未变的有效检查不重复。测试、类型检查、浏览器观察、模型调用和渠道结果分层记录；类型检查不能证明业务成功，模拟结果不能证明渠道或银行成功。

## 三、结果记录方式

每批记录日期、适用版本、案例、环境、结果和必要证据。状态使用未执行／通过／失败／外部条件阻塞；局部通过明确子场景。新方案所有 N 案例当前均为未执行，不因文档评审完成而更新为通过。

交易演示关联同一运行和原单号，标明账目来自测试数据，沙箱交易来自官方测试接口。只生成付款链接不算付款；退款等待不算到账；阶段性资金缺口不能被规划期末的预计收入掩盖。失败及复测只补实际发现问题的范围。

新种子加载和迁移验收不得覆盖原沙箱订单、业务退款号、未决任务、密钥或模型费用。演练用隔离环境，重置仅针对明确选择的模拟数据。

## 四、用户与交付

匿名访谈和情境任务记录用户是否理解资金范围、推荐理由、实际支出和退款等待。可记录步骤、耗时及错误，但不预填改善比例，不以少量样本推断整体青年群体。

新版必验范围通过后交付受控原型；金额、越权、误环境和假成功缺陷不得放行。未启用的复杂退款、通知和真实银行接入分别列出，不强行补齐无关专项，也不能声称这些已通过。

## 五、迁移方案的静态审核范围

本轮文档审核检查现有迁移执行器、种子覆盖逻辑以及新方案的模块衔接。已经核实迁移按文件独立事务执行、按文件名登记，旧文件改写不会重新应用；旧 seed 会按商品 code 覆盖并激活 A/B/C/D。计划因此采用增量迁移及独立新样本，历史已受理交易保留。

开工前的文档检查重点为：无账户目标草稿如何承接现有 Agent；用户原消息保存草稿、结构化商品选择与独立购买确认如何分层；余额基准及流水去重；各财务表允许的来源子集；账单分期覆盖；同账户多计划；账户撤回后的历史读取、新购买拦截和既有善后；旧入口不能绕过新准入；迁移失败如何恢复；沙箱结果如何与测试银行事实分开。

队员建议中的目标驱动、条件可行性、两套方案取舍、决策摘要和轻量复盘并入既有 N05／N06／N08／N09／N12，不另设一套验收流水线。变化触发规则重算，不自动执行交易；决策说明不保存模型私有推理。

文档审核通过只表示具备按计划开始实施的条件。SQL 的实际兼容性和 M1 新行为已经按本节当前记录取得受控 simulation 证据；新界面、沙箱连续链和用户可用性仍必须在后续 M2—M3 获得证据，未运行内容不得预填通过。最终交付判断统一在[开发计划](../03-engineering/development-plan.md)记录。

## 六、当前文档收口与证据状态

2026-09-16：统一 A/B 实施基线、自然月预算与 30 日派生视图、200／181／219 主样例、需求／预算草稿分支、项目写入事务与返回值、先评估再创建意图、撤回后的通用浏览与账户执行边界。`huixiang` 合并后的基线测试先行通过；随后 B 系列与 M1 后端继续开发，首次完整复跑发现模型费用账本测试之间的状态污染。隔离修复已应用，但本次文档审核前尚未重新执行完整套件，故 69/69 仅作为待复核记录。当前 `xingzhi_dev` 只读核对为 28 条迁移、24 条历史订单、2 条 Demo 报价，`finance_accounts`、`finance_account_snapshots`、`finance_ledger_entries`、`finance_obligations`、`budget_periods`、`budget_items` 和 `planning_drafts` 均为 0 行；A00 资金样例尚未加载。完整 N 案例仍需随 M2/M3 执行。

| 验证层次 | 可证明 | 不能证明 |
| --- | --- | --- |
| B-Ⅰ 固定 A 桩 | B 请求解析、调用参数、草稿结构、目录引用、工具与会话权限 | 真实预算持久化、资金算法、跨账户锁或最终准入 |
| 接入真实 A01—A03 | 项目新增／修改／取消落库、逐日依据、版本变化及草稿评估 | 购买已获本人授权或付款已成功 |
| B-Ⅱ 真实 A04—A05 | 同事务确认与建单、估价替换、撤回与并发拦截 | 渠道成功或账户已入账 |
| B-Ⅲ 与 M1 联合 | 资金事件、结果恢复、调整善后、API 连续链 | 前端 M2、沙箱 M3、真实银行接入 |

N03 以 200 元缓冲、99 元报价替换 80 元估价后的 181 元为准；N08 以新增 400 元后缺口 219 元为准，正式核算必须覆盖逐日不足与未知资料。N05 覆盖无账户持久草稿→本人选择周期→逐项确认；N10 覆盖新 Agent 无执行工具及撤回后仅保留浏览／历史善后。全部 N 案例仍未执行。

以下第七至第十二节是 2026-09-15 分批开发时形成的历史快照，保留用于追溯当时的迁移、交接和局部验证，不代表当前代码、数据库或 M1 总体状态。已有测试结果和数据库快照以本节及第十三节为准；整体放行条件以第十四节补充审核为准。

## 七、阶段 0（B 本地）与 B00 实施记录（历史快照）

2026-09-15：实际连接库初始仅登记 001—012，与旧交接记录不一致。先生成受 Git 忽略且权限为仅本人读写的本地备份，再通过现有迁移器成功应用 013—023，未改历史 SQL。当前 `xingzhi_dev` 登记 23 条迁移、24 条历史订单、2 条新 Demo 报价；`finance_accounts`、`budget_periods`、`budget_items` 和 `planning_drafts` 仍各为 0 行。与备份按原字段逐行比较，24 条订单完全一致（新增列不参与比较）；不沿用旧机的“2 条订单”假设。A 的独立数据库与分支尚待 A 自行确认，本记录不代表双方交接全部完成。

阶段 0 已补 BudgetItemCancelInput、FINANCE_SCOPE_REVOKED 和 BudgetItemMutationResult；服务端 budget-port.ts 固定同一 PoolClient 的 A/B 项目写入签名，没有安装模拟资金实现。B 本轮负责共享契约和 app.ts 的落笔，目录使用独立初始化入口，不改旧 seed；A 后续提供自己的资金样例和实现时对照这些签名合并。

B00 已实现 GET /api/offers?plannedOn=YYYY-MM-DD&categoryCode=food、GET /api/offers/:id/quote?plannedOn=YYYY-MM-DD。两者要求消费者会话；目录返回展示价与 purchaseMode，报价返回独立 quoteId、demo 来源、服务日期、规则和期限。不存在有效匹配报价统一 QUOTE_STALE；GET 不写报价。通用浏览不依赖账户授权，购买准入仍未实现。

新初始化以稳定 CONSUMER-* code 创建 99 元晚餐、69 元简餐和展示条目；服务日期显式传入，截止为该日上海时间 23:59:59。重复初始化不改现有商品、不延长报价、不恢复撤回或过期证据；换服务日期时另建报价。首批服务日期为 2026-09-20。当前不提供同日刷新入口，到期／撤回结果保留。

验证：服务端类型检查通过；单个数据库事务内的 B00 定向测试通过，覆盖重复初始化、旧 A/B/C/D 不变、消费者权限、非法日期、展示型不可报价、服务日期匹配、禁用／撤回／过期状态及 GET 不写入。测试修改全部回滚。未运行全量测试、构建、模型或支付流程。此结果只覆盖 B00，不标记 B01／B02、A 服务或任何完整 N 案例通过。

## 八、B01 接口独立开发记录（历史快照）

2026-09-15：已注册预算项目新增、修改和取消入口。B 路由校验消费者会话、请求来源、幂等键及路径／正文一致性，并由外层事务调用 `BudgetItemPort`；A 返回项目与 `BudgetBasis` 后，B 在同一事务内读取当日、同分类、active、orderable 且报价有效的最多 10 个候选。候选报价不写回项目，也不构成购买授权。取消结果不返回继续购买的候选。

现有 `idempotency_records` 作为唯一幂等事实：同一用户、路径和键的同参数重试返回首次完整响应，不再次调用 A；改参复用同一键返回 `IDEMPOTENCY_CONFLICT`。正式应用未安装资金模拟写入，A02 尚未注入时统一返回 409／`FINANCE_BASIS_UNKNOWN`，不会把桩结果写成用户事实。

验证：服务端类型检查通过；单个事务内的 B01 定向测试 1 项通过，覆盖未登录、非消费者、错误来源、路径正文不一致、新增／编辑／取消、同一 `PoolClient` 交接、候选报价组装、重复请求和幂等冲突。测试使用已有消费者身份、固定 A 桩和 B00 Demo 报价，数据库变更全部回滚。未运行全量测试、构建、真实 A02 落库、前端、模型或支付流程；B01 真实验收必须等待 A02 接入后补项目状态、版本、跨月和已承诺项目规则。

## 九、B02 接口独立开发记录（历史快照）

2026-09-15：新增并应用 023，只把 `planning_drafts.period_id` 与两个依据版本改为成组可空，补充 owner 直接外键，不改 001—022。已实现 `POST /api/ai/planning-drafts` 和本人读取入口；需求草稿的三个关联值同时为 null，预算草稿三个值同时提供并调用 A03 同事务只读评估端口。保存内容通过严格模式，用户明确值与模型 `suggestion` 分开，缺失日期、估价和必要／可调属性由服务端列出；目录引用必须仍为 active 且适用于相应日期。

新消费者 Agent 工具目录固定为 `read_budget_basis`、`search_offers`、`save_planning_draft`，没有旧流程的建单、付款、暂停购买或提交变更工具。草稿接口没有确认、状态 PATCH、预算项目、储蓄目标、订单、支付或退款写入字段。数据库 `model_source=validated_structured_v1`，只说明结构经过校验，不冒充已经发生真实模型调用。

当时验证：023 成功应用；服务端类型检查通过；单个事务内 B02 定向测试 1 项通过，覆盖需求草稿、预算草稿、三字段成组规则、严格模式拒绝执行字段、目录引用、缺失字段、本人读取、跨用户隐藏、同一事务 A03 桩调用、幂等复用、工具白名单及 A03 未接入时拒绝伪造评估。订单数量保持不变，测试数据全部回滚。当时未运行全量测试、构建、真实模型调用、A03 逐日算法、前端或支付流程；后续 A03 实现与证据见下一节，完整资金联合验收仍待 A02/A04。

## 十、A00/A01/A03 独立开发记录（历史快照）

2026-09-15：本阶段定义的 A00 Demo 口径为消费者 A 的借记账户、observed ¥2,000 快照、本月 ¥500 储蓄目标、¥900 必要项目及 ¥400 可调项目，其中 ¥80 晚餐属于 ¥400，不额外占用；当时记录的初始化方案使用稳定 UUID，重复执行不更新既有账户快照、周期或项目，也不覆盖旧目录/订单。本次复核的当前 `xingzhi_dev` 尚未加载这组 A00 资金样例。A01 已注册本人脱敏账户目录及撤回入口，历史读取与新执行基准分离；现金只取未撤回借记账户最新完整 observed 快照和覆盖截止后的 posted 同来源流水。信用额度、预计收入、pending 与未核退款不参与执行现金；快照超过 24 小时或缺可用余额/覆盖游标时为 unknown。还款账单覆盖分期、已核验结清、包含链循环和还款账户归属由确定性读取处理。

A03 已接入日常 `buildApp`，为 B02 关联周期的草稿提供同事务只读逐日评估。内部月度/滚动 30 日计算聚合同账户必要支出、自定义消费、还款和新订单承诺；银行已核验扣款替代订单承诺，订单替代用户估价，不双扣。未来用户预计收入与退款申请仅作条件说明。相邻月没有 active 周期则 30 日后段显示 unknown；当前周期没有未取消的必要/required 项目也为 unknown，避免把未填写误作零开支。账户撤回返回 `FINANCE_SCOPE_REVOKED`。草稿缺用户日期、估价或优先级为 unknown，不拿模型建议补值；评估不写预算项目、购买评估或订单。A02 后续仍需定义“必要开支已填完整/明确为零”的确认依据，目前没有此字段。

验证：当前数据库事务内 A00/A01 定向检查验证重复初始化、旧订单/目录不变、本人/跨用户权限、晚到旧快照、缺覆盖/余额、posted 与 pending 分离、信用额度不可作现金、账单含分期、已核还款及循环拒绝、撤回只推进版本一次且重复调用不再推进。A03 纯计算固定样例得到 ¥200→¥181→缺 ¥219；另一例月底现金为正但月中透支仍 blocked；同账户两个目标共同占用一份现金。数据库联调覆盖本人/版本、预算草稿只读评估、撤回、相邻月缺失与补齐后不同月目标；测试写入全部回滚。服务端与共享契约类型检查通过；当时尚未执行完整 N 案例、A02 真实项目写入、A04 购买报价预览、模型调用、支付或全量构建。A04/A05 后续证据见下一节。

## 十一、A04/A05 报价预览与事务准入记录（历史快照）

2026-09-15：新增 `POST /api/finance/assessments`，消费者会话、同源和幂等键是写入条件；只按数据库报价的有效期、币种、商品可下单状态、服务日期、现行规则版本和已记录来源计算。用户估价不成为成交价；替换项目后在购买当日预留全部报价，以免把未来估价留在原计划日造成当下资金高估。评估证据最长五分钟、不可改写，`unknown`/`needs_adjustment`/`blocked` 不得据此下单。共享字段目前规定 `shortfallMinor` 非空，所以 `unknown` 行的 0 只作“不可量化”占位，不能当作无缺口；B 和前端必须以 `status` 为准。

A05 内部函数要求 B04 在同一个数据库事务和 `PoolClient` 中调用：账户先锁，再锁同账户 active 周期、目标项目、报价／意图及相关订单，随后重新计算所有同账户承诺；只有新鲜 `allowed`、本人确认金额等于报价、评估／意图／快照及三个版本一致才执行 B 的确认建单回调。回调须写 `proposed→confirmed→一笔 pending 订单→ordered`，A 校验结果后标记预算项目 committed、写预算事件；失败由 B 的整笔事务回滚，外部付款不得在提交前发起。对有 active 新财务周期的消费者，旧 `createConfirmedOrder` 已关闭新建单路径，旧订单读取和善后保留；老计划确认 UI 仍可能显示，需 B/前端在新流程接入时整理，不能把旧入口误认为新财务授权。

验证：数据库事务内定向测试覆盖 ¥80→¥99、五分钟不可改证据、同键重试、改参幂等冲突、报价撤回、跨用户、旧入口门禁、版本与确认金额冲突、意外 ¥300 支出损伤储蓄目标、取消尚未承诺的可调 ¥320 项目后恢复保留线且预览不写库、已承诺项目不得被预览释放、回调未写有效订单时回滚，以及真实表结构下的一笔待付款订单／预算 committed 替换后的同账户余额不双扣。测试全部回滚，服务端类型检查通过。未执行 A02 真实预算写入、B03/B04 生产确认／建单、跨连接并发实测、真实付款、完整 N 案例或全量构建。

## 十二、A06 资金事件与月度复盘记录（历史快照）

2026-09-15：新增并在本机 `xingzhi_dev` 顺序应用 024、025。024 增加已入账流水的唯一引用、每订单一笔已核付款约束、被引用流水不可改写和新订单确认价/环境/提供方不可更改；025 在已应用 024 之后追加直接写资金事件表的订单/账户来源/到账流水防绕过检查，不删除或重写旧数据。`applyVerifiedMoneyEvent` 是受控内部函数，不注册消费者写路由；B 必须先核验渠道原始证据、稳定事件编号并在同一事务传入，A 再校验本人新订单、原周期、借记账户、订单环境/提供方、金额、来源及到账流水。pending、未知、退款申请、渠道核验通过只记事实；到账事件必须关联同订单/金额/方向/来源的 posted 流水。模拟只允许 Demo 来源，沙盒回执不能单独成为 Demo 或银行到账。已核付款把原预算项目置 settled；关闭周期和撤回后的历史结算允许记录，旧无 `budget_period_id` 的订单仍走原恢复流程。

`GET /api/budget-periods/:id/review` 仅本人可读，分列本周期 posted 收支、按原订单归属的已核付款/退款、渠道成功但未到账差额、原/当前目标及未知事项；目标审计链与当前值不一致时为 unknown。退款渠道成功不等于银行到账；关闭周期后晚到结果仍归原订单。只有 observed、来源一致且明确代表上海时区月末瞬间的快照，才计算月末剩余现金与目标缺口；否则返回 null，不把当前余额、信用额度、预计收入或 AI 建议当实际存款。月底余额称为“未用现金”，不声称已转入储蓄账户。

验证：本机 024、025 成功应用；A00/A01/A03/A04/A05/A06 相关 8 项定向测试通过，服务端类型检查通过。A06 测试在回滚事务中覆盖同提供方编号重试、重复引用已入账流水、无流水拒绝 posted、直接写表伪造来源被数据库拒绝、已引用流水及订单金额不可改、支付 pending 与退款申请/渠道确认不增加现金、Demo 实际入账后支付 ¥99/退款 ¥30 只各增加或减少一次、已关闭周期晚到 ¥10 退款、沙盒回执不能冒充 Demo 到账、跨主人/角色读取拒绝、目标审计值不连贯转 unknown，以及缺月末快照返回 unknown、有精确月末快照才返回可解释未用现金。所有测试写入回滚；未执行 B05/B06 实际受信渠道适配、真实银行余额同步、跨连接并发、完整 N 案例、前端连续流程或全量构建。

### A02 自然月预算及项目写入（随后补齐）

2026-09-15：在本机 `xingzhi_dev` 顺序应用 026，给 `budget_periods` 增加可记录本人明确确认必要支出（包括确认为零）的时间字段。A02 提供本人自然月 draft 创建/读取、当前月资金依据完整后的确认激活、储蓄目标变更审计，以及供 B01 在同一事务中调用的项目新增/编辑/取消函数；`buildApp` 已默认注入真实 A02 port，不再使用“未接入”桩。用户自定义估价写 `budget_items`，不写旧 30 天表，不当商户真实报价。账户→周期→项目按固定顺序锁定；归属、预期版本、closed/committed/settled、已有购买意图和跨月日期均受控。取消已 cancelled 且版本匹配返回原状态，不额外推进版本。

验证：026 本机迁移成功，服务端类型检查通过；A02 与 B01、A03、A00/A01、A04/A05、A06 的相关 10 项定向检查通过。A02 回滚事务测试覆盖缺资金依据保留 draft、observed 快照后资金版本更新、自定义晚餐 ¥80、跨月拒绝、过期周期版本、明确确认零必要支出后激活、目标 ¥500→¥600 的前后值/确认人审计、取消与重复取消、跨用户隐藏；B01 独立检查覆盖路由幂等同键重试。测试写入全部回滚。尚未执行完整 N 案例、B03/B04 生产确认与建单、B05/B06 受信结果接线、前端连续流程、真实银行或沙盒端到端。

## 十三、B03—B07 与 M1 原范围 simulation 验证记录

2026-09-16：B04 已在 A05 唯一外层事务中完成本人确认和新订单，B05 基于 020 的 operation/refund 新范围及 027 的人工任务扩展，将支付复核、支付宝通知、退款批次和人工恢复按旧 `plan_id + merchant_id` 与新 `budget_period_id + responsible_provider` 分流；B06 完成意外支出方案、未承诺项目取消及已付款订单的拒绝／延迟／固定批次退款；B07 增加预算事件、调整读取及新消费者 Pi 运行入口。027、028 已顺序应用到本机开发库。

你说明的流程是：`huixiang` 合并后先完成基线测试，之后继续进行 B 系列和 M1 后端开发。后续开发后的首次完整复跑共 69 项，68 项通过；失败项不是产品链路，而是 B02/B07 Agent 测试重置全局模型账本后未恢复，污染了后面的模型预算断言。现已将模型账本清理放入相关测试的 `finally`，并同步隔离旧 Agent 费用测试；截至本次文档修订，隔离修复已落入工作区，但目标套件及串行复跑尚未重新执行，因此 69/69 保留为待复核记录，而不是本次新的通过结论。服务端与共享契约类型检查通过；没有运行前端构建或全量构建。

新交易连续链在隔离临时 schema 中验证：¥80 用户估价由 ¥99 Demo 报价替换；B03 创建待确认意图；B04 本人确认只创建一笔 pending 新订单；payment-handoff 创建原号模拟任务；Worker 生成 Demo posted 支出及 `payment_posted`；¥400 意外支出存在取消未承诺可调项目后可行的解释选项；已付款订单取消只创建退款申请，不把预计退款计入当前现金；固定退款批次完成后依次形成 `refund_verified`、Demo 入账流水及 `refund_posted`，订单和调整方案最终完成。预算事件覆盖上述因果链。

新 Agent 连续链使用注入的确定性 Pi 流验证真实运行目录，只调用 `save_planning_draft` 保存一份 `periodId=null` 的无账户需求草稿，缺失日期、金额和优先级保持 null，运行完成后该用户订单数仍为 0。该检查证明工具接线与执行权限边界，不证明外部模型文案质量。

本节原验收结论限于受控 simulation 领域主链。2026-09-16 只读复核后，M1 整体放行改为“补充收口待完成”：下面第十四节通过前不冻结前端接口。支付宝沙箱仍只保留历史核心子链证据；真实银行接口、统一前端和 N01—N12 的页面／用户验收没有执行。

## 十四、M1 后端收口集中验收（待执行）

本节对应 [A/B 总方案第 11 节](../../行止_AB后端开发总方案_现状接口数据库任务.md#11-m1-后端收口进入-m2-前的补充任务)。原有 69/69 记录保留为待复核证据，但新交易连续测试直接调用领域函数，400 元分支只检查可行选项、实际退款走另一笔 1 元变化；新 Agent 使用确定性模型流，未证明绑定周期上下文和结构化产物能经 HTTP 取得。上述是证据边界，不把原已通过检查改写为失败。

收口实现完毕后使用隔离 simulation 场景，启动真实应用路由及 Worker。可以复用 Fastify 注入测试请求以避免额外测试平台，但须经过实际路由、登录会话、Origin、Idempotency-Key、解析、事务和响应；领域函数直接调用不能代替此项。进程恢复单独通过实际重启服务/Worker 验证。仅补最少可回归案例，不逐小任务跑套件。

| 检查组 | 必须取得的结果 | 当前状态 |
| --- | --- | --- |
| 主 API 故事 | 本人账户/周期→草稿产物读取与逐项确认→报价评估→意图→本人确认→原单模拟付款→400 元意外预览得 219 元缺口→确认可行调整→复盘；付款只记一次，必要支出只落一次 | 未执行 |
| 原单善后 | 同场景已付款原单单独预览/确认退款，申请不加现金、到账才变化；资金不足及账户撤回时仍可查询/受理原单善后，新购买继续被拒绝 | 未执行 |
| 未付款退出与恢复 | 意图放弃/过期后可编辑、取消和新建意图；pending/unknown 原号查询与关单；重启后复用原操作，不重复建单、扣款或退款 | 未执行 |
| 幂等与权限 | 消费者完整 HTTP 写链的同键重复与改参冲突、他人资源拒绝、旧版本拒绝；确认与意图退出互斥，不复活旧意图 | 未执行 |
| Agent 交接 | 无账户和绑定周期均能返回本人结构化草稿引用；缺金额保持 null；最终文案失败时已落库产物可读；运行列表/详情恢复不触发新模型调用；没有交易工具 | 未执行 |
| 财务展示与场景 | 已引用流水可纠正展示分类且原事实/资金版本不变；账单包含分期不双扣；新场景日期/报价匹配；旧场景过期如实拒绝，不被新初始化覆盖 | 未执行 |

400 元主链可通过取消尚未承诺的 320 元可调项目恢复约束；不能要求取消 99 元订单的待退款立即消除 219 元缺口。退款作为同场景另一项本人确认善后验证，明确记录动作先后及现金影响，不把 1 元分支当成 400 元主链已执行证据。撤回、未知支付等会改变前置条件的分支使用独立隔离场景，避免污染主故事。

真实模型仅做少量已配置环境下的代表性检查：无账户缺失信息、绑定周期读取/检索/保存草稿；沿用共享费用预算，明确区分真实模型结果与注入流。外部模型暂不可用时如实记录阻塞：结构化 simulation API 可单独通过，Agent 不能标为可演示；后端整体放行仍等待这项代表性检查，不靠反复调用碰成功。

执行受影响服务端/共享契约类型检查；已有有效检查不无条件重跑，只有改动触及原路径时补对应回归。资金/权限失败先修复再定向复测，不以测试总数替代判断。不运行前端构建；完整候选构建仍留交付阶段。

通过后登记适用版本、场景标识、请求/响应摘要、原单与操作关联、实际金额、失败及复测范围，并同步开发计划与文档中心为“M1 收口通过，进入 M2”。这仍不等于 N01—N12 页面、用户观察或支付宝沙箱新连续链通过。本次文档修改没有执行本节验收。
