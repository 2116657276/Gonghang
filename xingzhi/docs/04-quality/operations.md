# 运行、数据保护与交付门槛

| 字段 | 内容 |
| --- | --- |
| 文档编号 | XZ-OPS |
| 更新日期 | 2026-09-14 |
| 状态 | 后端本轮实现与 55 项定向验收通过，当前保持 simulation；前端开发者按契约接手，录制 Demo 时再切换 sandbox |

## 一、环境和凭据

本地模拟、官方沙箱与未来正式交易分别隔离数据、应用、凭据和网关；本地 S1 已创建被 `.gitignore` 排除的 `xingzhi/.env`，其中包含独立 `xingzhi_dev` 连接、本地测试账号密码，以及用户提供的沙箱应用参数，均只供本机使用。当前先在本机开发，固定域名和云服务器不作为前置条件。沙箱采用明确允许的网关目标，启动和调用前检查环境一致性；不只依赖前端标签。没有 HTTPS 时，本机沙箱付款使用返回页后的主动查单；只有配置 `notify_url` 并做异步通知验收时，才需要渠道可访问的 HTTPS 地址，后续可经用户授权采用临时隧道，或在云服务器部署后配置固定地址。测试商户身份不能冒充真实品牌。

密钥仅由商户支付适配器读取，不进入用户端、模型上下文、工具返回、普通日志或公开仓库。日志默认脱敏订单与账号，付款入口引用受权限与有效期限制。P0 使用预置本地测试账号、服务端会话与固定角色，不开放注册或短信接入；至少准备两名消费者用于越权验证、一名商户管理员及一名只读审核者。密码使用专用密码摘要算法和随机盐，凭据由本地环境提供，不写入仓库。会话存 PostgreSQL，随机会话令牌经 HttpOnly、SameSite Cookie 传递，HTTPS 环境启用 Secure；状态变更接口校验请求来源并防止 CSRF。退出即时失效；会话期限默认 24 小时，和购买／善后授权分别判断，退出登录不撤销已受理业务任务。审核范围由服务端配置。

S4 只允许服务端读取 `DEEPSEEK_API_KEY`；当前模型配置为 `DEEPSEEK_BASE_URL=https://api.deepseek.com` 与 `DEEPSEEK_MODEL=deepseek-flash`，对应 DeepSeek V4.1 Flash（用户简称“ds4.1flash”）。运行准入、Pi 模型对象、烟测入口、请求体夹具和人民币费率已同步；密钥缺失、基地址不是允许值、模型标识不是 `deepseek-flash` 或预算账本不可用时，拒绝发起外部模型请求。Pi 运行层不启用 shell、任意 HTTP、任意文件写入或支付密钥工具。

### 沙箱浏览器自动化结果与当前模式

2026-09-14 已完成首批沙箱浏览器自动化测试。随后按用户安排将日常开发模式恢复为 `PAYMENT_MODE=simulation`，录制 Demo 时再切换 sandbox。以下对象与判据保留为本次证据及后续定向复测基线。

本次复用 `alipay.trade.page.pay` 交接和主动查单路径，没有新增支付接口或生产免密能力。买家登录密码／支付密码只用于官方沙箱收银台；它们不是网页支付 API 参数，不传给行止业务模型或保存在仓库。浏览器自动化可在已授权的测试范围内代替人工输入并操作页面；本次没有遇到验证码，后续若出现无法自动完成的交互再由用户接续，不能由单次通过推定长期无人值守。

首批使用同一计划内两个虚构测试项目：

| 对象 | 金额与用途 | 验收终点 |
| --- | --- | --- |
| A：全退样本 | 沙箱 0.01 元，完成付款后按原单全退 0.01 元 | 官方支付成功、固定退款号查询成功、累计支付 0.01 元／退款 0.01 元／净额 0 元 |
| B：待付关单样本 | 沙箱标价 0.01 元，只形成可查待付交易，不完成付款 | 查到 WAIT_BUYER_PAY 后关闭原单，核实 TRADE_CLOSED 并释放占用 |

首批实际付款 0.01 元、原单退款 0.01 元，均已完成；B 单已形成可查 `WAIT_BUYER_PAY` 并查询到 `TRADE_CLOSED`。后续按实际账号支持范围和具体交易授权补齐费用／分批退款及必要外部中断场景，复用本次有效付款证据。

执行时 API 与 worker 使用一致的沙箱模式，限定领取本次测试计划的任务；不修改全局模式后放任旧任务运行。付款链接按需生成，当前有效期 15 分钟，避免提前生成等待过期。官方订单结果只由后端核对原单号、金额、卖家和渠道状态后更新；浏览器显示成功仅作页面证据。默认本机返回页主动查单，无需新增域名或隧道。

测试通过后曾更新本机 `.env` 为 `PAYMENT_MODE=sandbox`，API 与 worker 分别启动核对并读取同一配置；当前已恢复为 simulation。每次切换只影响后续新订单，既有订单仍按原环境处理，不迁移或改写渠道事实。本次只核对必要的模式生效结果，没有重复完整测试或构建；业务模型仍不能读取买家密码或直接执行商户退款。

## 二、观测与人工接管

| 观测项 | 用途 | 处理原则 |
| --- | --- | --- |
| 暂停受理至生效耗时 | 验证停止新购买的执行路径 | 本地目标与计时边界见下方参数表；记录在途例外 |
| 未知支付／退款数量与年龄 | 防止任务静默丢失 | 超出经确认等待门槛产生人工任务 |
| 重复请求与参数冲突 | 排查幂等问题 | 保留业务编号，不手改成功状态 |
| 通知或查单的签名／金额／归属失败 | 排查无效渠道事实 | 拒绝更新并隔离审查 |
| 后台任务积压和领取过期 | 判断恢复能力 | 接管先核对外部结果 |
| Agent 轮次、工具失败和费用 | 控制推理开销 | 到上限结束决策，保持业务任务 |

人工任务必须有对象、原因、证据、责任角色、下一动作和复核时间。人员可重发查询或按同一业务请求重试，不能直接把未知结果改成已退款。人工接管状态面向用户可见。

## 三、故障处置

渠道不可用时停止新增不安全操作、保留未知占用、展示待核对；不自动切换正式环境。模型不可用或预算达到 ¥100 时保留业务查询与暂停入口，后台继续已授权任务。疑似越权或凭据暴露时暂停相关执行入口，保留证据并由有权限人员处理凭据，不把凭据贴入工单。

部署回退只能回退应用逻辑，不能回滚外部付款。旧程序若不能解释新状态或数据结构，不允许直接回退上线；先停止新购买、排空或接管在途任务，确认兼容性。备份恢复后必须查询渠道补齐备份时间之后的外部事实。

## 四、P0 本地运行参数

下列参数是按已授权的首版完整 Demo 目标选定的本地开发默认值，不是渠道要求、生产服务承诺或实测结果。实现时集中配置，不在各模块各写一份；故障用例可使用独立测试配置加速时间，不修改业务时钟事实。

| 项目 | 本地默认值与处理 |
| --- | --- |
| 报价／待确认方案有效期 | 10 分钟，并取商品规则、订单状态与授权约束中更早的失效条件；规则变更仍触发版本校验 |
| 暂停响应目标 | 普通本地单用户场景下，从服务端收到请求至暂停提交并返回不超过 1 秒；不含自然语言识别和客户端网络，实测未达标则记录原因并修复 |
| 后台领取 | 空闲扫描 1 秒，领取租期 60 秒；长任务每 20 秒续租，失去领取版本不可提交结果 |
| 外部请求超时 | 默认 15 秒；写请求超时转未知，不能按失败自动重发 |
| 渠道任务恢复 | 退款与关单写请求不自动重放；可能已发送时只查原业务号。查询按 15 秒起步退避至 300 秒，未知持续 30 分钟转人工 |
| 渠道状态查询 | 本地模拟默认 10／30／60／120／300 秒逐步等待，之后每 300 秒一次；沙箱实际配置须在 S2 按所用官方接口复核后固定，采用更严格的等待和限频要求 |
| 人工接管 | 明确标识为本机 `PENDING`／`UNKNOWN` 的模拟结果立即生成唯一人工任务，以便核对状态与权限链路；实际渠道未知、商户延迟超过 30 分钟或安全重试耗尽时才生成任务并暂停自动轮询／重试。两种情况均保留占用和未决状态；之后按原操作由有权人员复核，默认下一次人工复核时间为 24 小时内，不承诺渠道在该时限完成 |
| Pi 单轮边界 | 最多 8 次模型调用、20 次工具调用、总运行 120 秒；每次调用超时 30 秒；达到任一上限则结束本轮、保留业务任务并展示原因 |
| 模型费用 | 本地开发／验收环境累计上限为人民币 100 元；DeepSeek V4.1 Flash 高峰费率按缓存命中 0.04 元、未命中 2 元、输出 8 元／百万 token 估算，闲时为高峰一半；剩余预算不足时拒绝新调用，重置账本必须人工记录，不能通过重启绕过 |
| Agent 入口限流 | 已认证消费者的 Agent 运行请求采用令牌桶，默认每分钟 10 次、突发容量 2；同一计划同时最多 1 个活动运行；限流发生返回 429 与等待提示，不自动改写请求 |
| 上游错误退避 | DeepSeek 429 若返回 `Retry-After` 则遵守，否则使用本地退避；最多 3 次带抖动退避，仍失败转为可重试的模型错误；不因 429 自动切换正式环境、模型或支付参数 |

## 五、P0 数据、备份与导出

本阶段仅采集测试账号及必要业务记录，不采集真实身份证明、银行卡或旅客资料。以下为本地 Demo 数据政策；正式公开服务、真实用户数据或真实资金场景在引入前另行确定适用政策。

| 数据或动作 | 本地处理规则 |
| --- | --- |
| 会话和模型交流 | 会话到期失效；对话与普通脱敏运行日志保留 30 天后可清理；确认和资金事实不随聊天删除 |
| 模拟业务数据 | 所有关联动作终态且无未决事项后满 30 天可清理；仅本地管理员明确选择的模拟计划允许整组重置 |
| 沙箱订单、确认和核验依据 | 所有关联事项结束后保留至少 90 天；未决操作、相关授权与证据不按期限自动删除 |
| 清理执行 | P0 不建立自动批量删除服务；由管理员显式选择环境和计划，检查关联未决事项、备份与引用后执行，记录清理范围；审核者不能清理 |
| 备份 | 进行沙箱联调的当日及破坏性迁移前制作本地备份；保留最近 7 份，清理旧备份须明确选择；凭据独立保存，备份不进入 Git |
| 恢复核验 | 在独立测试库恢复一份备份，核对计划、订单、任务关联；恢复后先按渠道补齐外部事实，不能直接重放资金写请求 |
| 行迹导出 | 首版提供可读 HTML 与结构化 JSON；服务端鉴权下载，引用 24 小时过期，默认脱敏，不开放匿名公开分享；用户下载后自行分享的副本无法由系统远程撤回 |

### 5.1 环境切换与恢复操作边界

切换 `PAYMENT_MODE` 不迁移既有订单的环境，不把模拟订单转换成沙箱订单。启用沙箱前先关闭开发计划列出的环境分派缺口，并为该环境使用独立数据库或已验证的等效隔离；当前表中存在环境字段不等于隔离已经验收。切换前确认 API 与 worker 配置一致，列明未决操作；旧环境任务保留在原环境处理，不由新适配器接管。

后台恢复前先核实任务类型、环境、原业务编号、领取版本和最后发送阶段，再恢复到期任务。当前模拟与渠道任务已在同一结果事务内检查领取版本；出现业务已成功但任务未完成、事件缺失或金额不一致时，仍按原操作核验并保留证据，不手改成功状态或清库重演。实际进程中断、独立虚构样本库恢复和 API 重启已有本地证据，见[验证记录第十七节](verification.md)；长时间官方交易中断仍待实测。

恢复一份备份时，先停止恢复目标的 API、worker 和模型跟进，再使用 `pg_restore --exit-on-error --no-owner --no-privileges` 恢复到新建独立数据库；不能把恢复演练指向原开发库。核对计划、确认、订单、原支付／退款号、批次、操作、任务及 `sent_at` 关联后，才连接业务服务。恢复后的模型费用账本可能落后于实际支出，核对最新账本前保持模型调用关闭。

备份可能早于外部发送，因此备份里 `sent_at` 为空不能证明从未发送。对恢复库中尚未终态的 `sandbox_close`／`sandbox_refund` 操作，先在停机状态下将空的 `sent_at` 设置为恢复核对时间，作为“可能已发送”的保守标记；它不是官方发送证据。保留非空原值及原业务号，记录此次恢复范围，然后通过原号查询核对。未查明时保留未知和人工责任，不能清空标记或重置任务来强行发送。确认确实未受理而需继续的业务，再由对应主体按现有确认和商户入口处理。独立演练库只使用受控适配器，不访问官方渠道。

参数表是目标运行基线，不代表所有后台退避、自动核对、模型入口或清理能力均已完成。当前 API-25 在沙箱模式创建并执行受控复核任务，模拟对象仍转人工任务；API-24 只读入口和限流已实现，交易写工具、事件恢复、T10 复核和页面入口已接入并完成本地受控核对，首批官方付款／原单退款／待付关单核心链已有记录；官方特殊场景、完整真实模型、前端浏览器联调和完整 P0 仍待完成。上游 429 退避已完成本地受控验证，每项运行能力以开发计划与实际验证记录为准。

## 六、实施与交付阶段

实施顺序与状态仅见[开发计划](../03-engineering/development-plan.md)：后端收口并完成契约交接后，前端开发者可并行统一重构与联调，最后综合交付。S 编号仅作能力及历史追踪；完整 P0 通过才算首次 MVP／Demo。小阶段不单独设置测试或构建门禁，官方条件缺失时只保留对应阻塞，不把它写成已通过。

已核实的本地条件为 Node 26.5.0、pnpm 11.19.0、全局 TypeScript CLI 7.0.2 及项目锁定的 TypeScript 5.9.3、PostgreSQL 17.10；已创建独立 `xingzhi_dev` 开发库。Redis 本机可用，但 S1 不依赖它。S1 已完成迁移、种子和定向业务验证，009—012 已应用本机开发库；运行命令、账号与范围见[本地运行说明](../../README.md)。阶段验收关联需求、定向验证与脱敏证据；不按用例数量批量创建测试文件。沙箱应用参数、付款设备和首批核心交易已核实，异步通知、特殊交易和完整 P0 仍按选择的联调路径核实并取得相应授权。

[验证策略](verification.md) · [未决事项](../00-governance/review-agenda.md)


## 七、2026-09-08 可执行路径与待配条件

当前默认以 `PAYMENT_MODE=simulation` 运行；2026-09-14 已执行首批官方沙箱付款、原单退款查询与待付关单，证据见验证记录第二十节。009—012 迁移已应用本机 `xingzhi_dev`。模拟目录的付款、关单和退款结果可分别设置为 SUCCESS／PENDING／UNKNOWN，并在建单时固化；未决结果保留占用及原操作，进入人工复核。自动测试会创建并清理独立临时 schema，仅接受本机 `xingzhi_dev`，不复用或清空现有订单；当前 `pnpm test:targeted` 为 55 项通过，根目录 `pnpm typecheck` 覆盖 contracts、server、web 均通过。

渠道执行参数已按本轮接口复核补齐：请求超时 15 秒；退款后至少 15 秒再查询，后续退避逐步增加至 300 秒；同单批次发送至少间隔 3 秒；未知持续 30 分钟转人工。退款未知后自动路径只查原退款号，不通过新的请求号再退款；关单仅在首次执行查询明确待付款时发送一次；已有可能发送标记的恢复只查原单，未确定时保留占用并转人工。API-25 的显式复核执行一次，未确定时显示人工责任；消费者授权在排队和执行时分别检查，商户自身职责不冒用消费者授权。

Pi 依赖已安装，此前切片提供 API-24 查询与方案运行、本地测试和 `pnpm --filter @xingzhi/server agent:smoke`。历史真实烟测已在本机 `.env` 配置下通过，使用旧模型；密钥不写入版本库。烟测共享 `model_budget / model_usage` 的 ¥100 上限，输出保守费用估算及 usage，缺失 usage 时保留预占。API-24 只读运行、每分钟 10 次／突发 2 的持久令牌桶和运行记录已实现；009—012 已应用本机 `xingzhi_dev`，交易工具、观察、事件恢复、取消分支队列、T10 复核、页面状态和独立模拟结果已接入。进程中断或过期最多两次有界子运行尝试，普通模型失败或取消不自动重试；上游 429 显式退避已通过本地验证。实际检查结果及适用版本见验证记录；新 `deepseek-flash` 的模型标识、请求体和费率已由本地测试核对，固定场景及进程／授权连续评测见验证记录第十八至十九节，官方核心交易链见第二十节，后端统一复跑见第二十一至二十二节。局部行为验证、真实模型／官方特殊场景／P0 完整验证边界见[验证策略](verification.md)与[开发计划](../03-engineering/development-plan.md)。

当前安装对非本路线所需的 `@google/genai`、`protobufjs` 依赖安装脚本明确禁用，保留 esbuild 必需安装步骤；Pi ESM 导入及确定性工具循环已通过。不因依赖提示升级包管理器或改变已确认 Pi 版本。

方案运行在创建草稿后进入 WAITING_USER 并结束推理；刷新页面通过最新运行接口恢复，不额外调用模型。消费者查看并独立确认原卡片后，按接口返回的 `agentFollowupQueued` 判断助手是否继续执行；未排队时使用既有结构化入口。测试账户、测试计划和模型费用只保留虚构验收资料，浏览器临时会话在验收完成后撤销。

2026-09-10 退避补充：未提供有效 Retry-After 时等待 1／2／4 秒加不足一秒抖动；等待纳入 120 秒总限额，每次发送独立占用模型调用次数与预算。没有 usage 的 429 预占继续保留待核对，不能自动清零；重试成功只结算成功调用。运行取消、超时或预算不足即停止重试。详见[验证记录第十一节](verification.md)。

2026-09-14 当前状态：009—012 已应用本机 `xingzhi_dev`，事件写入、运行读取、退款批次关联、独立模拟结果与 worker 已按新表运行；交易工具、T10 复核、变更善后连续链、恢复准备和页面入口已作本地受控核对，DeepSeek V4.1 Flash 已使用 `deepseek-flash`，当前定向测试 55 项、根目录类型检查通过。首批官方付款、原单退款查询与待付关单核心链已有记录；本次未复跑构建，最近一次完整构建沿用历史证据。官方特殊场景、多场景模型稳定性、前端跨端验收和完整 P0 放行仍待完成，当前边界以[验证策略第十七至二十二节](verification.md)和[开发计划](../03-engineering/development-plan.md)为准。
