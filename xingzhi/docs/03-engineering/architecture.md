# 行止系统架构与迁移边界

> 本文保留原交易底座与早期规划架构的复用说明。新消费者月度模型、需求草稿及交易路径以 [A/B 后端总方案](../../行止_AB后端开发总方案_现状接口数据库任务.md)为准；新规划不双写旧 cashflow 表，新 Agent 不继承旧执行工具。

更新日期：2026-09-16。状态：原交易模块继续保留；001—029、A00—A06 与 B00—B07 的核心实现及 M1 补充收口（含 HTTP 集中验收）已进入当前基线，统一前端和新流程支付宝沙箱连续演示仍待完成。

## 一、总体结构

复用 Vue 3/Vite 响应式网页、Fastify 业务服务、PostgreSQL、独立 worker 和 Pi 单 Agent。网页展示银行微应用原型，不能宣称真实嵌入银行 App。沿用现有 TypeScript 和锁定依赖，不增微服务、消息队列、第二个 Agent 编排层或通用银行平台。

服务端计算金额与状态，PostgreSQL 保存业务事实，worker 处理已受理的渠道任务。Pi 解释需求、查询受控工具，并可按用户当前消息明确表达的内容保存非执行目标草稿；商品选定、active 目标变化和购买分别由用户通过结构化界面确认，再由业务层执行。模型和前端都不能写入支付成功或银行余额事实。

## 二、模块与现状

| 模块 | 职责 | 现状 |
| --- | --- | --- |
| 身份与权限 | 原测试账号、会话、消费者／商户／审核范围 | 可复用；不引入真实开户 |
| 资金与账目 | 主账户、来源时间、流水、分类纠正和独立还款安排 | A00—A06 已实现 Demo 事实、确定性核算、资金事件和复盘；真实银行适配未接入 |
| 近期规划 | 日期级收支和保留目标、缺口、收入情景、跨计划影响 | 月度预算、逐日计算、同账户承诺和本人读取已实现 |
| 目录与需求 | 登记商品条件、候选、空状态需求、选择替换 | B00—B03 已实现目录、报价、草稿和待确认购买意图 |
| 计划与确认 | 购买／变更草稿、版本、授权、预算和暂停 | B04 已接入 A05 完成本人确认及新消费者订单 |
| 订单与渠道 | 原单支付、关单、取消、退款及查询 | B05/B06 已支持新旧范围和 simulation 闭环；新流程沙箱连续链待 M3 |
| Agent | 现有计划内工具循环和事件恢复 | 旧运行链保留；新消费者运行只装载资金读取、商品检索和草稿保存工具 |
| 前端工作区 | 资金、对话、计划与变化的连续体验 | 待 M1 补充收口及 HTTP 连续验收通过后进入 M2 统一重构 |

## 三、数据流与边界

账户及还款来源→用户确认必要安排→服务端规划快照→候选及报价→用户确认→订单与 worker→来源可追溯的支付／退款结果→新的规划快照。银行事实、测试账目和渠道事实各自保存来源；展示整合不等于混写事实。

现金流按账户可见范围合并相关计划，购买权限仍按具体计划检查。规划只读计算不占用真实银行卡余额，消费确认也不形成银行冻结。执行前重查最新资金安排及确认依据，处理变化时重新预览，不依靠模型自行推断授权。

新银行数据首版为受控测试资料，不调用个人网银抓取或自动登录。将来接银行接口需有具体权限和来源契约，不能因已有支付适配器就假定已支持银行读数。

## 四、工程迁移

按[数据设计](data-and-events.md)增量迁移，保留交易链和历史业务编号，不把 SQL 重构等同于清库。通用 `db:seed` 仍保留旧 A/B/C/D 目录和测试账号；A00 资金样例与 B00 新目录使用独立入口，原固定样本保留为隔离回归资料。消费者界面先等待 M1 补充收口和 HTTP 契约冻结，按[开发计划](development-plan.md)推进。

必要并发、金额和权限约束由既有事务与唯一约束落实；小规模目录直接查询与规则筛选，不引入向量库、智能推荐训练或统一事件总线。恢复先核对既有业务事实，不重新发送可能已执行的资金动作。

## 五、队员建议的最小落点

生活目标使用 `plans`／`plan_items` 扩展少量日期与可调整属性；可行性来自资金计算服务；两套方案比较复用目录和只读计算，选定后复用 `proposals`／`confirmations`。决策摘要、变化差异和复盘使用现有快照、事件与导出，不新增独立目标中心、资金池、偏好学习服务或多 Agent。

微应用入口可先记录目标，再引导补齐账户与资金资料。账户不足时只允许草稿和解释，不输出已验证可行结论或放行交易。新 API、字段及旧工具耦合以接口和数据设计为准，必要改造在 M1 一起收口。
