# 行止开发与迁移计划

更新日期：2026-09-17。状态：001—029、A00—A06、B00—B07 与 M1 补充收口已进入当前代码基线；独立数据库集中验收含类型检查、74/74 目标套件、消费者 HTTP 连续链、Worker 与真实模型代表性检查，后续补充回归为 75/75，均已登记通过，M1 后端已达到进入 M2 的放行条件。构建与提交登记已完成，下一步启动 M2 微信小程序开发；M3 分别验收小程序 simulation 演示、支付宝 sandbox 连续链和用户证据。小程序方案已确定，应用及会话适配尚未实施。

## 一、文档职责与执行边界

产品范围由 [PRD](../01-product/prd.md) 定义，N01—N12 判据由 [Demo 与验收](../01-product/demo-and-acceptance.md)维护，金额、来源及权限原则见[领域规则](../02-domain/rules-and-state.md)。当前后端任务、月度数据模型、A/B 交接和目标接口以 [A/B 后端总方案](../../行止_AB后端开发总方案_现状接口数据库任务.md)为唯一实施基线，共享代码 `packages/contracts/src/consumer-backend.ts` 表示已经落入代码的字段。M2 平台、页面、状态与恢复边界由[微信小程序前端实施方案](miniapp-frontend.md)维护；[接口契约 1.3](api-contract.md#13-m2-小程序会话接入待实现)及 1.4 单列客户端会话与恢复读取的待实现契约，不改变已冻结的资金和交易规则。旧接口及早期设计不覆盖现行消费者 DTO。

M1 表示整个后端大阶段；A00—A06 与 B00—B07 是 M1 内的分工，B 三批交付不是另一套项目阶段。M1 核心领域链和补充收口已通过集中验收，M2 联调契约自迁移 029 起冻结；M3 负责连续沙箱演示、用户观察和交付。

复用 Fastify、PostgreSQL、Pi、现有会话、幂等及支付适配。微信小程序首版演示与日常开发使用 simulation，独立的支付宝渠道连续演示使用获授权的 sandbox；真实银行接入与宿主不作为受控原型的开工前提。模型继续使用现有共享费用上限，不重置历史费用。A00 资金样例不随迁移自动加载，需通过独立种子按需初始化。

## 二、M1 的实际执行顺序

| 批次 | A 交付与依赖 | B 交付 | 出口 |
| --- | --- | --- | --- |
| 阶段 0（历史基线） | 独立核对本地 001—023，随后追加至 028；约定账户、预算样例和公共文件落笔人 | 补齐取消 DTO、撤回错误及最小内部交接签名；核商品、报价和草稿迁移边界 | 已完成 |
| B-Ⅰ | A01—A03 可并行开发；B 可用测试桩 | B00 目录报价 → B01 项目入口 → B02 需求与规划草稿 | 一次 B 独立验收；模拟 A 的结果不能算真实预算写入或资金评估通过 |
| B-Ⅱ | A04 preview、A05 同事务最终准入和账户撤回已可用 | B03 绑定评估的购买意图 → B04 本人确认、订单与支付交接 | 已完成并纳入 M1 验收 |
| B-Ⅲ | A06 核验资金事件和复盘；复用 A 逐日核算 | B05 Worker → B06 意外调整与善后 → B07 证据交接 | 已完成并纳入 M1 验收 |

023 已允许无账户需求草稿的周期和两个依据版本成组为空，并补 owner 直接外键；013—022 未改写。开发早期 B01 的真实项目保存依赖 A02、B02 的预算草稿评估依赖 A03，桩只服务边界开发和定向测试，不注册成日常服务中的伪成功写接口；当前 `buildApp` 已默认注入 A02/A03 真实实现，B-Ⅱ 仍不允许模拟准入进入付款链。

主演示固定为 2,000 元确认现金、500 元储蓄目标、900 元必要支出及当期还款、400 元可调计划（包含晚餐估价 80 元）。初始缓冲 200 元，晚餐报价 99 元替换估价后缓冲 181 元，新增 400 元必要支出后缺口 219 元。正式准入按日期计算，30 日视图从自然月预算派生，相邻月事实缺失返回 unknown。

### 数据与样本边界

保留原订单、支付尝试、确认、授权、退款、任务、幂等和费用事实。已应用的迁移（001—029）不改写、不清库。新消费者预算只写 `budget_periods/budget_items`，旧 `cashflow_plans/items` 不双写。

A 提供资金样例，B 提供独立稳定 code 的商品与报价，公共 seed 由阶段 0 约定的合并人落入。新初始化不调用会覆盖或重新激活旧 A/B/C/D 商品的路径。固定场景日期和报价有效期在初始化时明确，GET 不移动日期或重新生成报价；报价到期需要显式受控刷新并生成新记录，保留原证据。

### 失败、恢复与回退

增量迁移先在隔离库完成。现有 `db/migrate.ts` 按文件名顺序执行，每个文件和对应 `schema_migrations` 登记在同一事务中；当前文件失败回滚，先前文件仍已提交。执行器仅按文件名跳过已应用迁移，因此必须追加新编号，不能改写旧文件期待自动生效。失败后检查已提交部分和迁移编号，不手动标记成功，不以删除旧表重试。将来对已有数据进行破坏性清理前，必须具备已核对的目标范围与可恢复备份；当前迁移以新增和兼容列为优先，不预设必须清库。

应用切换前暂停售出入口并核对在途 worker；新程序仅恢复自己可解释的任务。需要回退应用时，保留新增表和事实，先确认旧程序能否安全读取新列及状态；不默认执行反向删表迁移。凡已发到渠道的动作不能通过数据库回滚撤销，先按原单核对并交接。

M1 的评审出口包括迁移映射、字段契约、样本说明、金额及权限判据和集中结果。没有真实银行接口不构成这一阶段的阻塞；未知银行字段不伪造为已授权、已读取或零余额。

### M1 补充收口：前端开工前必须完成（已通过）

2026-09-16 只读审核发现：购买意图缺少退出/失效路径，既有订单善后受新预算准入耦合，Agent 上下文与结构化草稿交接不完整，分类纠正未实现，Demo 快照无法连续多日使用；现有领域函数测试也不能替代 HTTP 连续验收。具体接口、数据规则与 A/B 责任以 [A/B 总方案第 11 节](../../行止_AB后端开发总方案_现状接口数据库任务.md#11-m1-后端收口进入-m2-前的补充任务) 为唯一补充契约。

| 顺序 | 交付 | 责任与边界 |
| --- | --- | --- |
| 收口 1 | 未确认购买意图撤回、到期失效、重新评估购买 | B；保留原意图，不释放已建单承诺 |
| 收口 2 | 新订单独立关单/取消预览与本人确认 | B，A 核对资金事件；资金不足或账户撤回不阻断原单善后，退款到账前不加现金 |
| 收口 3 | 服务端绑定 Agent 上下文、run→draft 产物、本人运行读取恢复 | B；不扩大交易工具权限、不新建长期记忆系统 |
| 收口 4 | 本人流水分类展示覆盖、账户读取字段与还款样例 | A/B；复用账户接口，原始流水不改写，900 元总额不重复计入 |
| 收口 5 | 显式建立新隔离 Demo 场景 | A/B；旧样例/订单/费用保留，不刷新旧快照伪装新资金 |
| 收口 6 | 消费者 HTTP 连续验收、缺陷复测与契约冻结 | 一次集中执行，按验证策略第十四节记录结果 |

以上六项已于 2026-09-16 完成并集中验收：追加迁移 029，隔离场景 `m1-acceptance-20260916` 使用 2,000/500/900/400 元固定口径；类型检查和 74/74 目标套件通过，真实 HTTP 主链得到 99 元订单、400 元意外支出和 219 元缺口，确认取消 320 元可调项目后恢复 allowed，原单 99 元退款到账。真实模型的绑定周期与无账户两条运行均保存可恢复草稿。M1 后端正式放行进入 M2；真实银行、微信小程序和支付宝新连续链仍按 M2/M3 单独实施和验收。

## 三、M2：微信小程序与接口联调

M2 采用 Taro 4.x + Vue 3 + TypeScript，新建消费者应用，原网页保留商户、审核及历史善后。具体实现见[小程序方案](miniapp-frontend.md)。按以下顺序完成，每一阶段结束后再推进依赖它的主链；无需为每个页面另建阶段或重复全量验收。

| 小阶段 | 工作与交付物 | 完成条件 |
| --- | --- | --- |
| M2.1 平台与会话 | 创建最小 Taro 应用，锁定版本并验证共享契约编译；适配消费者令牌会话及写路由来源门禁；确认手机网络、AppID 和开发权限 | 真机完成登录→本人账户读取→一项受控草稿写入→退出／失效；定向验证网页 Cookie 来源防护、无效令牌和越权仍被拒绝；小程序构建通过 |
| M2.2 规划与选择 | 规划／账目入口、无账户 Agent 草稿与恢复、为既有场景入口补 account-only 模式（待实现）、账户／周期准备、逐项预算确认和激活、可行性卡、分类纠正、有效候选比较 | 真机从空状态走到各候选独立评估；unknown 不当零；呈现 200→181 元依据。独立核对新建 draft→确认→active，不用已激活种子替代 |
| M2.3 交易与恢复 | 先补 API 契约 1.4 的本人意图列表／详情与订单列表、操作引用；购买意图及退出、本人确认、付款交接、订单与操作查询、同键重试、切后台／冷启动恢复；确定 M3 沙箱外部浏览器交接方式 | simulation 原单付款可核对；重复点击、超时及重进不重复建单；报价／版本变化重新确认；明确沙箱交接所需的最小页面及尚未验证条件 |
| M2.4 变化与闭环 | 新增必要支出、219 元缺口、调整比较与确认、原单独立善后、退款分层、决策摘要和复盘；完成移动端视觉与交互细节 | 微信真机同场景连续完成 N01—N11 主线，并补齐对应权限／未知／恢复边界；记录实际结果，N12 不提前通过 |

**验证尺度：** 首个包即编译并真机运行，不能等全部页面结束才检查平台兼容。后续按改动执行受影响类型检查与必要小程序构建；改会话／来源门禁时定向验证相关后端，其他 M1 证据不重复。商户和审核网页只检查受影响路径。H5／开发工具运行不能替代微信真机结果。设备或网络条件未落实时继续独立工作，但不宣称 M2.1 已完成。

## 四、M3：演示、用户观察与交付

| 小阶段 | 工作与交付物 | 完成条件 |
| --- | --- | --- |
| M3.1 连续证据 | 微信真机演示 simulation 主链；另以同一 sandbox 场景完成支付宝付款、变化确认、原单取消退款与查询 | 两条运行分别标识环境和证据，不切换已有订单环境、不拼接历史片段；小程序 simulation 不能替代 N12 |
| M3.2 用户观察 | 用消费者小程序完成情境任务，记录需求、步骤、耗时和金额理解偏差 | 来源及观察条件可追溯，小样本不作普遍效果结论 |
| M3.3 交付 | 按新版 N 案例登记结果与未覆盖能力，整理运行说明、演示和交接材料 | 在实际体验版配置下验证目标手机；最终候选小程序构建一次，未变且已有对应证据的构建复用 |

**集中验收：** N01—N12 的必验范围有相应环境连续证据；沙箱交接未通过则 N12 保持未执行／阻塞，不降低为模拟支付通过。真实微信支付、银行宿主、银行接口和正式商品履约不在首版范围。资金、越权、环境混淆及假成功问题不得放行。复杂退款专项不阻断未启用该功能的基础原型，但不得声称全部历史用例通过。交付称为微信小程序受控原型，不称银行正式上线。

## 五、验证节奏与恢复入口

M1 历史节奏为 B 每批一次集中定向验收、最后联合验收。各批已通过且代码、环境、输入未变化的结果直接复用；只有新变更、失败或依赖替换才补测相关范围。新资金或权限问题可提前做最小复现，不逐文件构建、不要求每批全量测试。M2 已涉及新增前端构建链，按第三节执行，旧 M1 的“不运行前端构建”不再适用。

B-Ⅰ 已接入 A02／A03 的真实实现；固定 A 桩只保留在边界测试中。B03 复用 A04 的不可改写评估证据建立 `proposed` 意图；B04 在唯一外层事务中调用 A05 完成本人确认和新订单；B05/B06 将新预算周期订单接入 Worker、资金事件、调整、取消和退款；B07 提供预算事件、调整读取和新消费者 Agent 运行入口。M1 集中验收记录 74/74；同日后续补充回归记录为 75/75，构建与提交也已登记，见[验证策略](../04-quality/verification.md)第十四节。69/69、74/74 保留各自执行范围，不冒充当前再次运行的结果。支付宝沙箱连续链、真实银行和前端仍按 M2/M3 单独验收。

**M2 启动条件：** M1 实现、集中验收、构建和提交登记均已完成，证据见验证策略第十四节；无需为了本轮文档更新重跑或重新登记 M1。可直接开始 M2.1；其首项开发工作是小程序会话与真机最小链路，并非已经通过的平台验收。惰性过期事件属于系统状态变化，不计作用户主动操作。

**场景与运行：** 联调开始时先核对目标库及当前场景有效性。需要新的资金基准时，在仓库根目录执行 `pnpm --filter @xingzhi/server db:seed:consumer-scenario <新场景标识> <上海服务日期 YYYY-MM-DD>`。例如当日首场可用 `m2-20260917-01`，以后按实际日期与序号另取唯一标识；登录账号取初始化输出（新建账号格式为 `m1-<场景标识>@xingzhi.local`），密码取本机 `SEED_DEMO_PASSWORD`。不得固定重用 `m2-dev` 期待更新快照或报价，也不在一条未结束交易链中换场景。快照超过 24 小时、报价到期或账户撤回按真实状态处理，原订单和证据保留。

API 本地默认端口为 8787，网页 Vite 代理只服务现有网页；小程序使用手机可访问的 API 基址，网络和体验版条件见[小程序方案第六节](miniapp-frontend.md#六开工资料与验证落点)。初始化、设备准备和外部环境配置是后续开发任务，本次文档整理未执行这些操作。
