# 行止本地运行说明

| 项目 | 当前事实 |
| --- | --- |
| 阶段 | 后端本轮实现与 55 项定向验收通过，支付宝沙箱核心链已有记录；当前保持 simulation，前端开发者按契约接手，录制 Demo 时再切换 sandbox |
| 运行组成 | Vue 3/Vite 网页、Fastify API、独立 worker、PostgreSQL |
| 本地地址 | 网页 `http://localhost:5173`；API `http://127.0.0.1:8787` |
| 数据环境 | `xingzhi_dev` PostgreSQL 本地开发库；当前新订单默认走本地模拟，既有沙箱订单保留原环境 |

后端开发与本轮统一验收已经完成；本次不修改 `apps/web`，前端开发者可按[开发计划](docs/03-engineering/development-plan.md)的交接边界开始统一重构与联调。实现状态见开发计划，实际结果见[验证记录](docs/04-quality/verification.md)。

支付宝沙箱浏览器自动化已完成：A 单付款后按原号全退，B 单形成待付交易后按原单关单，后端金额一致。为继续开发和重复演示，本机 `xingzhi/.env` 当前使用 `PAYMENT_MODE=simulation`；录制 Demo 时再切换 sandbox，并在最终付款提交前由用户确认。模拟目录可分别配置付款、关单和退款的 SUCCESS／PENDING／UNKNOWN，结果随订单固化。账号凭据仅保存在被 Git 忽略的 `xingzhi/.local-secrets/alipay-sandbox-accounts.json`，不复制到文档或模型输入。

## 首次运行

在行止项目根目录 `xingzhi/` 使用 `.env.example` 创建本地 `.env`，设置独立的 `SEED_DEMO_PASSWORD`，并确认 `DATABASE_URL` 指向 `xingzhi_dev`，不要连接其他项目的数据库。若该库尚未创建，先执行 `createdb xingzhi_dev`；可从仓库根目录执行：

```sh
cp xingzhi/.env.example xingzhi/.env
pnpm install
pnpm db:migrate
pnpm db:seed
pnpm dev
```

`pnpm dev` 同时启动网页、API 与 worker。`pnpm db:migrate` 只应用未执行的顺序 SQL 迁移；`pnpm db:seed` 可重复执行，用于补齐本地测试账号和 A/B/C/D 目录，不写入真实数据。

历史真实模型只读烟测和 T04／T08 方案联调已通过，API-24 提供查询与方案助手入口；Pi 依赖已安装。服务端只从本机 `.env` 读取 `DEEPSEEK_API_KEY`，固定 `DEEPSEEK_BASE_URL=https://api.deepseek.com`，当前配置为 `DEEPSEEK_MODEL=deepseek-flash`（DeepSeek V4.1 Flash，用户简称“ds4.1flash”）；累计模型费用上限为人民币 100 元，采用每分钟 10 次、突发 2、同计划并发 1 的常规服务端限流。当前模型的固定场景、进程中断与授权连续核对见验证记录第十八至十九节，官方核心交易链和后端复跑见第二十至二十二节；未配置密钥或人民币预算账本时不会发起外部模型调用。

本阶段还提供 `pnpm typecheck` 和 `pnpm test:targeted`。`pnpm build` 留给首次交付构建或构建路径变更时执行，不作为日常文档／业务检查。

## 测试账号

种子数据使用两个消费者、一个测试商户管理员和一个只读审核者。邮箱分别是 `consumer-a@xingzhi.local`、`consumer-b@xingzhi.local`、`merchant@xingzhi.local`、`reviewer@xingzhi.local`；密码只取自本机 `.env` 的 `SEED_DEMO_PASSWORD`，不写入 Git。

消费者工作台可建立 A/B/C/D 计划，生成和确认购买预览、建单、观察模拟结果、暂停购买、生成变更预览、提交模拟善后，并查看授权、退款批次和当前人工责任。商户工作台可管理命名规则预设、查看取消队列、处理延迟申请、安排固定退款批次及记录人工复核；审核者只显示服务端分配的计划范围。所有写操作由服务端会话、请求来源、幂等键和计划版本共同约束。

## 当前已核对范围与仍待放行范围

后端已覆盖目录、计划、购买确认、预算占用、暂停、变更确认、善后／查询授权、事件、可恢复 worker、商户规则、固定退款批次、人工复核、API-25 复核及独立模拟结果；交易执行工具、事件恢复、T10 复核和 A/B/C/D 连续链已有本地受控证据。商户仍独占退款批次执行权，消费者与 Agent 不能调用该入口。模拟结果只用于验证内部状态和恢复路径，不能证明官方渠道成功。

S2 已取得首批官方连续链：A 单由浏览器完成 0.01 元付款，后端主动查单并按固定退款号全退；B 单查到待付后按原单关单。当前开发默认 `PAYMENT_MODE=simulation`，录制 Demo 时再切换 sandbox。未填齐沙箱配置时仍拒绝生成官方付款交接或处理渠道通知；异步通知仍需渠道可访问的 HTTPS 地址。消费者页面明确区分模拟订单与沙箱交接，不把打开链接冒充为付款成功。

官方核心交易链已取得连续证据；异步回调、外部调用中断、费用／分批退款、拒绝／延迟／未知和恢复竞争的完整验收仍按[分阶段开发计划](docs/03-engineering/development-plan.md)推进。当前模型固定场景、实际进程与独立虚构库恢复见验证记录第十七至十九节，官方链见第二十节，独立模拟和后端统一复跑见第二十一至二十二节。审核行迹导出已提供本机 JSON／HTML 脱敏路径，但尚未纳入 36 项完整验收。

[返回行止文档中心](docs/README.md) · [返回仓库](../README.md)


## 历史自动验收与模型烟测记录

2026-09-10 的 429 退避切片运行过 36 项定向检查，其中数据库故障测试仅在本机 `xingzhi_dev` 的独立临时 schema 执行并清理；这 36 项只证明该历史切片，不能代表后续修改后的当前工作区。`pnpm --filter @xingzhi/server agent:smoke` 是真实模型复核入口：只读取固定虚构数据，最多两次模型调用，费用受数据库共享 ¥100 上限约束，无需汇率。历史真实烟测对应旧模型且已通过；当前 `deepseek-flash` 配置已在本地请求体夹具中核对，固定场景及进程／授权连续评测见验证记录第十八至十九节。API-24 的数据库限流、四个只读工具、两个方案工具、交易执行工具、事件恢复、T10 复核及自然语言面板已在当前工作区接入。2026-09-13 历史工作区复核为 54 项通过，2026-09-14 当前工作区复核为 55 项通过；两者均不代替 36 项 P0 的完整跨端验收。

前文 S1／S3 记录是既有局部验证。本轮新增渠道关单、原单退款、退款查询、API-25 持久复核及独立模拟结果的适配器／权限检查；2026-09-14 已完成首批浏览器官方交易链，当前定向测试 55 项、类型检查通过，最近一次完整构建仍沿用历史证据；官方特殊场景与完整跨端验收仍待补齐。

2026-09-10 的 429 切片已补齐显式退避与逐次预算检查；没有 usage 的旧调用预占保留，重试成功不会将它清零，取消和总时限可中断等待。该切片的 36 项检查和服务端类型检查不适用于之后写入的交易工具、事件恢复及页面初稿，也不代表 36 项 P0 放行验收通过。最新证据边界见[验证记录第十七至二十二节](docs/04-quality/verification.md)，交易工具、事件恢复、T10 复核、变更善后连续链和独立模拟结果已有本地受控核对，官方核心交易链已实测；官方特殊场景、真实模型多场景稳定性、完整跨端验收和首次交付仍待完成。
