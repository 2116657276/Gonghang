# 行止：当前进度与后续工作

> 本文是唯一维护开发状态和下一步顺序的文档。状态分为“代码存在”“已有记录”“外部验收”和“未确认”，不要把其中一种替换成另一种。
>
> 核对基线：2026-09-25，`huixiang` 分支，提交 `d277d4d`。本页状态依据当前仓库代码、已有执行记录及本次明确标出的检查整理；既有历史结果不是本次重跑。提交后仍须以 Git 实际分支和工作区状态为准。
>
> 2026-09-23 已进行 F3 服务端领域与真实模型集中验收；微信设备、Sandbox 和正式录制仍未验收。

## 1. 当前结论

D1/D2 跨角色 Demo 工作流已交付；F1/F2 已完成代码并有本地 H5 财务场景记录，F3.1—F3.3 代码及定向服务端回归已有记录。F3 的服务端领域与真实模型集中验收已执行，首次因模型对金额的解释出错而未通过，现已修复并完成定向复验；微信开发者工具、真机、Sandbox、商户/审核现场证据和正式录制仍无本轮新证据。

产品表达已经回到“看懂收支—安排资金—应对变化”。商品和交易能力继续保留；F1、F2 的开发与本地代码级检查已有记录，F3.1—F3.3 已完成代码。AI 金额解释已改为服务端事实呈现并通过定向复验。2026-09-25 代码审查未发现“敬请期待”、空按钮或纯占位页面；当前缺口主要是启动可靠性收尾、微信/真机连续验收、外部渠道证据和前端自动化测试，而不是继续新增主页面。

## 2. 阶段状态

| 阶段 | 代码/材料状态 | 已有验证或记录 | 尚未完成/不可替代项 |
| --- | --- | --- | --- |
| D1 跨角色后端 | 已完成迁移 033、共享契约、商户订单处理、人工退款批次、审核范围和证据导出 | README 历史记录有跨角色服务端回归与集中验收；代码入口已在 `app.ts` 注册 | 最终演示仍需沿同一订单现场核对商户处理和审核证据，静态路由存在不等于现场通过 |
| D2 页面联调与 Simulation | 已完成商户/审核工作台、消费者连续操作、自动与人工售后分支、刷新/晚到响应处理 | README 历史记录有 H5 + 管理端集中联调、窄屏/短屏/断网观察和相关回归 | 不再新增 D2 开发计划；后续复用其结果并保存跨角色证据 |
| D3.1 微信代码准备 | 已完成 Bearer 会话、API 地址注入、幂等保留、演示环境页、启动脚本和微信产物准备 | 已有记录的 macOS 脚本语法检查、消费者类型检查和微信生产构建 | 微信开发者工具完整主线、至少一台真机最小主线 |
| D3.2 包体评估 | 2026-09-25 真机调试出现 80051（主包 3,140 KiB，超过 2 MiB）后，已将 28 个非核心页面移入 18 个分包，并让启动脚本以压缩模式监听微信构建 | 当前 `dist/weapp` 主包为 51 个非 source-map 文件、约 605.8 KiB；6 个主包页面、28 个分包页面均有编译产物，API 已编入热点地址 | 仍需在微信开发者工具重新编译并执行真机调试，记录工具显示的实际上传大小与启动结果 |
| D3.3 Sandbox 代码收口 | 已连接付款交接、主动查单、取消退款、退款查单和审核证据路径；默认环境仍是 `simulation` | 只核对了配置入口和代码路径，没有发起支付宝外部请求 | 全新 Sandbox 订单的付款→主动查单→取消退款→退款查单连续实测 |
| D3.4 演示材料 | 已准备演示叙事、录制顺序、证据保存要求和未覆盖项 | 历史录制材料已并入当前验收文档，Simulation 与 Sandbox 边界已写明 | 包含交易的扩展录制须先完成微信、Sandbox、商户/审核证据；财务主线按第 4 节独立验收 |
| F1 资金事实与展示口径 | 已实施 | 契约、服务端和小程序类型检查记录通过；三组相关定向回归 45/45；差异格式检查通过 | 仅有代码级集中检查；不代表 H5 后续修复、微信工具或真机当前状态已复验 |
| F2 五页财务体验 | 已实施 | 2026-09-23 本地 H5 场景记录覆盖五个主标签、月度切换、计划操作、账目汇总、窄屏和一次断网恢复 | 后续版本/时区修复未重跑浏览器主线；微信工具与真机仍待验收 |
| F3 AI 与计划实际复盘 | F3.1—F3.3 代码已完成；仓库含迁移 034/035 | 2026-09-23 金额解释修复的定向检查 6/6 与两类真实提问复验通过；2026-09-24 后续评审修复的 12 个不同定向用例、服务端/小程序类型检查、两次真实模型请求及指定 H5 操作通过 | 上述范围以外的页面连续操作、完整微信工具与真机主线仍待验收；其他目标库迁移须独立核对 |

## 3. 验证记录的来源和范围

### 3.1 D1/D2 阶段交付记录

Git 历史显示 `a7afecc` 的提交标题为“feat(xingzhi): complete cross-role demo workflow”。此外，本次会话提供了该阶段的以下交付记录：

- 该阶段修复审核证据业务号问题，并完成跨角色 Demo 工作流交付。
- 上一轮服务端定向回归记录为 79/79，全工作区类型检查记录为通过。

上述检查结果来自会话提供的交付记录，不是本次静态核对从测试日志中重新取得的结果；本轮没有重新运行它们。提交标题只能说明版本意图，不能单独证明测试结果或外部验收结论。

### 3.2 仓库已有历史记录

以下记录来自当前 README 和已提交的开发材料，保留原执行范围，不改写成当前新执行：

- D1 记录：服务端/共享契约检查，以及受影响跨角色回归 41/41；覆盖消费者 Simulation 付款、本人商户人工批准、固定退款批次、重复处理不重发、越权隐藏和审核 JSON 导出。
- D2 记录：两个隔离场景完成 H5 + 管理端自动退款和商户人工处理分支；记录有 360×740、360×640、断网恢复和订单事实一致性观察；相关交易回归记录为 40/40。
- D3.1/D3.2 历史记录：微信独立 Bearer 会话、API 地址注入、开发者工具/真机边界和包体评估；早期记录有 163 个文件、772,670 字节，后续 D3.3 记录为 163 个文件、783,323 字节。两者仅保留为历史数据；2026-09-25 曾测得未分包开发产物约 3.12 MiB，随后真机调试以 80051 明确确认主包超限，当前分包后的测量见阶段表，最终上传包仍以微信开发者工具分析为准。
- D3.3/D3.4 记录：Sandbox 配置入口和代码路径收口，默认未发起支付宝外部请求；H5 收银台新标签页/复制地址行为和 Demo 录制材料已准备。

历史记录证明的是相应时间、环境和范围内的检查，不自动证明当前目标数据库、当前设备、当前渠道账号或正式演示已经通过。

### 3.3 本轮代码级检查（2026-09-23）

- `pnpm typecheck`：通过，覆盖 contracts、miniapp、server、web 四个工作区项目。
- `pnpm test:targeted`：通过，服务端 `src/domain/*.test.ts` 与 `src/payment/*.test.ts` 共 82/82。
- 未运行构建、数据库迁移、真实模型、微信开发者工具、真机、Sandbox 或支付流程；上述两项不能替代这些验收。

### 3.4 评审修复复验（2026-09-24）

退款到账识别、未绑定草稿选周期、草稿最新版本预览及 AI 针对性解释均已修复。12 个不同定向用例最终通过，服务端与小程序类型检查通过；两次真实模型请求与两条 H5 草稿操作通过。随后推送前审查重新运行全工作区 `pnpm typecheck` 和 `pnpm test:targeted`，类型检查通过、定向回归 86/86 通过。范围、测试修正及未覆盖项见 [验收记录](05-acceptance-and-demo.md)。本次未运行构建、迁移、微信工具、真机或支付流程；微信完整主线仍待接手者执行。

### 3.5 合并后代码与构建审查（2026-09-25）

- Git 基线为 `huixiang` / `d277d4d`；本轮文档更新前工作区干净并与 `origin/huixiang` 同步。
- `pnpm typecheck` 通过，覆盖 contracts、miniapp、server、web 四个工作区。
- 本机目标库先应用到迁移 `035`，随后 `pnpm test:targeted` 通过 86/86；迁移只证明该本机目标库，其他环境仍须分别核对 `schema_migrations`。
- 消费者 H5、微信小程序、商户/审核网页生产构建通过。H5 构建存在大资源警告，微信磁盘产物测量见 D3.2；两者都需要设备/开发者工具结果决定是否优化。
- 小程序登记 34 个页面和 10 个公共组件；源码搜索未发现 TODO/FIXME、敬请期待、空按钮或纯占位页面。部分资料、隐私和说明页面按产品边界为静态内容，不属于未接接口。
- 服务端现有 19 个定向测试文件；消费者 H5/小程序与商户网页目前没有自动化 UI 测试，页面回归仍依赖 H5、微信开发者工具和真机验收。
- `start-xingzhi.ps1` 已补入 `localhost` IPv4 端口探测、重启时清理残留 Vite 进程，并新增 `-Mobile`：自动识别当前热点 IPv4、让后端临时监听局域网并将该地址编入微信产物。普通启动仍只监听本机；真机正式预览和发布仍须 HTTPS 合法域名。
- 真机调试曾返回 80051（主包 3,140 KiB，超过 2 MiB）。当前已保留登录和五个主标签为主包，将其余 28 页拆到 18 个分包；Windows/macOS 启动脚本的小程序监听构建使用压缩模式。当前主包磁盘测量约 605.8 KiB，34 个页面编译产物齐全，小程序类型检查和监听编译通过；尚未把该结果追认为开发者工具真机验收通过。
- 根目录 `pnpm build` 不会构建小程序；完整构建仍须分别运行 `pnpm build:miniapp:h5`、`pnpm build:miniapp:weapp` 和 `pnpm build`。
- 本轮未启动完整应用、未执行微信开发者工具、真机、Sandbox、商户/审核现场流程或正式录制；构建通过不能替代这些结果。

### 3.6 小程序登录后导航修复（2026-09-25）

开发者工具登录页曾在凭据校验成功后显示“操作没有完成”。本机健康检查和 H5/小程序登录接口均返回 200，问题位于登录成功后的首页标签切换：Taro 导航失败对象被当成登录错误展示。登录页现将身份验证与导航分开处理，`switchTab` 失败时以 `reLaunch` 重建首页页面栈；如果两种导航都失败，则明确提示“登录已经成功但首页无法打开”，不再误报账号或后端故障。消费者端类型检查和微信生产构建通过；仍需在微信开发者工具重新编译并实际登录确认。

## 4. 当前唯一后续顺序

按以下顺序推进；前一项的阻断问题先闭环，再进入下一项：

1. **启动与交付入口收尾（已完成代码，待真机确认）。** Windows 脚本已补入 IPv4 端口探测、残留 Vite 清理和 `-Mobile` 热点联调模式；完整构建命令边界已写入 README。下一步只需确认手机浏览器和小程序能访问脚本打印的局域网 API。
2. **微信开发者工具包体与启动检查（超限代码修复完成，待工具确认）。** 使用当前分包产物重新编译，执行开发者工具包体分析，确认主包低于 2 MiB并记录主包/分包实际上传大小；确认登录页、API 地址和控制台无阻断错误。
3. **微信开发者工具完整财务主线。** 连续核对首页、计划、账目、AI/草稿、我的，以及 F3 计划与实际关联、返回、刷新、重登、断网恢复和底部面板。发现问题时保存第一条错误并做最小修复，然后只复测受影响步骤。
4. **至少一台真机最小主线。** 配置手机可访问的 HTTPS API 与微信合法域名，重复登录、五页主线和一个底部操作面板；记录机型、系统、微信版本、基础库、提交和 API 地址。
5. **财务 Demo 冻结与录制。** 只有步骤 3、4 通过后才冻结演示数据、版本和脚本并录制；不得剪辑未通过的模型、设备或到账能力为成功。
6. **按交付范围决定是否进入交易专项。** 若 Demo 包含交易，使用全新支付宝 Sandbox 同一订单连续完成付款、主动查单、取消退款和退款查单，再核对同一订单的商户处理及审核证据。纯财务主线不以 Sandbox 为前置条件，也不能代表交易专项通过。
7. **回归保护。** 在上述人工主线稳定后，为登录、五个主标签打开、关键详情跳转和一条计划/流水关联路径补最小自动化冒烟；不追求覆盖率，不引入与当前交付无关的测试平台。
8. **最后做其余证据驱动的性能优化。** 主包超限已有设备证据并已完成分包；除此之外，只有开发者工具或设备数据证明存在首屏、解析或图片问题时，才继续实施资源拆分或大资源优化。

F3 金额解释缺陷已修复并定向复验通过；F3 页面连续操作/恢复仍需在步骤 3、4 中核对。执行步骤见根目录[开发接力指南](../../开发接力指南.md)，通过条件见[验收文档](05-acceptance-and-demo.md)。设计图功能与核对边界见[产品文档第七节](01-product.md#7-ui-设计图功能清单与实现对照)。不重写既有交易底座，不重新做前端视觉体系。

### F1：资金事实与展示口径（已实施）

| 小阶段 | 必做内容、实现边界 | 完成标准 |
| --- | --- | --- |
| F1.1 账户和月份上下文（已完成） | useOverview 当前分别选默认账户和第一个 active 周期；按所选账户及上海自然月匹配周期，禁止拿别的账户/月份结果作首页概览。无当前周期显示待建立；历史月份显式选择。AI 账户来自所选周期，账目月份独立于是否建立预算。修正未选账户误显示已撤回、无周期月份出现 undefined 的空态 | 两个账户、不同月份、无当前周期下资金和标题均一致；只调整读取/选择，不迁移业务模型 |
| F1.2 日期与资金影响契约（已完成） | 复用 budget-cashflow/cashflow-engine.daily，补共享类型和必要的每日事件引用，让趋势、最低余额、保留目标、缓冲及原因来自同一计算。缓冲使用有效预测的最低余额减保留目标，负数显示缺口，未知不显示零。单项卡明确“周期整体结论”；进入单项影响时用现有引擎比较含/不含该未承诺项目，不把整体风险直接当项目自身风险。新增必要支出和收入日期变化复用评估，不前端计算准入 | 可追溯到日期、项目/义务；跨月缺数据保持 unknown；预计收入只作独立条件说明，不进入已确认现金；不再复制图中固定曲线/日期 |
| F1.3 流水、退款与提醒语义（已完成） | finance-facts 当前只有分类/方向/金额等，补可核验业务摘要（订单名或来源摘要），缺失时用分类兜底。退款依据原始来源/关联资金事实识别，不依据 displayCategory；退款是流入子集，“收入”若包含退款必须标清，退款不再次累加。仅 posted 参与已入账统计。未知义务不冒充未发生。通知设置改为“应用内提醒偏好”，仅过滤提示摘要，不隐藏必须处理的业务入口；保留微信推送未接入说明 | 分类纠正不改变退款金额或现金；本月/历史月、收入/退款、未来义务各自清楚；必要新增列只追加迁移，不改原流水金额 |

F1 实施前的静态发现依据：`composables/useOverview.ts` 的 primaryAccount/currentPeriod 独立选择；`pages/plan/index.vue` 向所有卡传 period.forecast；`pages/ledger/index.vue` 按 displayCategory 识别退款；`domain/finance-facts.ts` 的 LedgerRow 无摘要；首页未消费 notifications。这些是已修复的问题来源，不是当前仍存在的故障，也没有被表述为经运行复现的线上故障。

### F2：五个主页面补齐（已实施，复用现有组件）

| 小阶段 | 内容 | 完成标准 |
| --- | --- | --- |
| F2.1 首页（代码已完成） | 补资金事实时间、缓冲、真实逐日趋势/日期列表、未来房租/还款/预计工资安排、最低日原因；默认提示“最低日与保留目标差额”，有不足才写需调整。图中“接近”无阈值契约，不擅自发明风险阈值 | 余额、最低余额、缓冲、预计收入不混淆；趋势 unknown 区间断开；日期列表有同内容文字摘要 |
| F2.2 计划（代码已完成） | 四项月度摘要；状态多选（全部与其他互斥、其他按并集）、分组数量/折叠、结束/取消项目入口。已有周期时“＋”新增项目，无周期时创建周期。保留手工必要支出/预计收入/可调消费编辑与取消；保存前比较金额/日期/最低余额/缓冲及缺口。需调整卡直达该项影响与编辑，已承诺订单仍走原售后 | 不经过商品目录也能新建和调整计划；周期状态与项目影响明确；不新增多套预算表 |
| F2.3 账目（代码已完成） | 独立月份选择、标题/分类搜索、日期组折叠、未来义务筛选、业务摘要/分类字形/订单关联详情。复用月度统计弹层，增加分类汇总和口径说明，无需新报表平台。无预算月份仍能看流水；选未来义务时不混入已发生统计 | 至少当前月、历史月和空月可用；收支/退款汇总与明细一致；未来义务状态可解释 |
| F2.4 我的与素材（代码已完成） | 主账户金额隐藏（与首页同一会话内一致）、空账户状态、默认账户和目标设置一致；通知设置准确说明实际能力。已核对品牌图标与现有压缩 IP 素材，静态表情按实际录制需要后置 | 所有设置有实际反馈；不为图稿增加上传、动画系统、真实银行授权或微信推送 |

### F3：财务 AI 与轻量复盘（代码已实施，服务端与真实模型定向复验通过）

| 小阶段 | 内容 | 完成标准 |
| --- | --- | --- |
| F3.1 带事实提问（代码已完成） | 首页最低日、计划卡和账目月份带上下文进入 AI，先显示待发送问题，由用户点发送；服务端校验本人周期，并将账目读取锁定到本次选中月份。复用 read_budget_basis 和 30 天预测；模型只收到 posted 月度汇总，不收到全部逐笔流水。快捷问题覆盖安排、周末草稿、最低日原因、可调整项目 | 结构化事实与原页面可核对；已修正模型金额转述问题并通过定向复验，最终资金结论由服务端工具事实生成；模型不可用不阻断原资金页面 |
| F3.2 草稿与影响卡（代码已完成） | AI 产物卡及草稿详情读取已持久化字段，分别标注用户提供、模型建议和待补充；用户字段完整且绑定周期时调用现有只读预览，缺失时进入编辑补充。已采纳草稿仍允许逐项进入正式计划确认 | 建议值不会直接变成用户确认值；查看影响不激活预算、不建单，多个项目逐项进入确认 |
| F3.3 计划与实际复盘（代码已完成） | 复用订单、流水与退款事实；新增本人同账户、同月 posted 非订单支出与未承诺预算项目的人工关联。每笔流水至多一个有效关联，用户确认覆盖金额，解除保留历史；部分覆盖和超出部分单独说明。预测只保留尚未发生的计划余额，复盘显示已覆盖、未发生和未覆盖普通支出 | 关联/解除后版本变化并重新评估，已入账事实只扣一次；退款仍按原渠道与账户事实核对。定向代码回归已过，页面和设备连续操作留待集中验收；不做自动匹配算法 |

### 后续验收顺序与范围

- F1/F2 的代码级检查与 H5 场景记录已完成，见下方记录；后续只复测明确受改动影响的步骤，不重跑已通过且环境未变的交易链。
- F3 服务端领域与真实模型集中验收已执行，问题与证据见 [验收文档第九节](05-acceptance-and-demo.md#9-f1f3-财务体验集中验收与主线录制)。金额与影响说明已改为服务端确定性呈现并复验通过；模型不替用户确认。
- 最后在微信开发者工具与至少一台真机完成财务主线；无法自动化的步骤由接手者按验收文档执行。Sandbox 继续独立新场景验证，同单证据要求不删，交易未改时不反复构建与测试。
- F1、F2 的已有验收范围见下方记录；F3.1—F3.3 已完成代码，真实模型集中验收发现的资金解释缺陷已修复并定向复验通过，F3 连续页面与设备端尚未验收。下一步由接手者完成微信主线。

## 5. 后置与待决事项

- 图中没有手工新增流水入口：任意手工收支录入继续后置，若纳入需另定用户账本来源及去重，不开放银行流水伪造接口。
- 微信订阅通知、真实银行同步、银行 App 宿主、生产支付、长期目标/理财、全网目录、语音/附件、多 Agent 不纳入 F1—F3。IP 中旅行/学习/储蓄等是生活场景，不是新增产品承诺。
- 图中“依赖预计收入”需要可核验的条件情景支持；若现有引擎只能确认现金准入，则显示“预计收入未计入，到账后重新判断”，不制造新的可购买状态。
- 演示 HTTPS、合法域名、体验成员及渠道配置由实际外部验收核实，不能从本机源码判断已经可用。
- 纯视觉的多表情、品牌素材适配仍是后置事项，不阻断财务主线；不做无证据的包体重构。

## 6. 交付判断

现有 D1/D2 能力及历史交付记录继续有效，但不代表全部 UI 差距已补齐。F1/F2 已有代码和本地场景记录，F3.1—F3.3 已完成代码；F3 服务端领域与真实模型集中验收曾发现的金额解释错误已修复，本轮定向复验通过。财务主线、交易专项、微信设备和 Sandbox 分别登记结果。纯财务 Demo 不以 Sandbox 为前置条件，原完整微信＋Sandbox Demo 仍须补齐其全部渠道与跨角色证据。本次文档更新没有执行设备或外部渠道验收。

### F1.1 实施记录（2026-09-22）

概览按已连接借记账户及上海当前月份选择周期，不回退历史周期或其他账户；计划列表限定当前周期，历史周期继续从既有历史入口查看。账目默认月份独立于预算，日期分组及创建周期默认月采用上海日期。AI 上下文标签和选择列表显示周期对应账户；已失效的选择在刷新后清空。修正无周期标题与无账户状态。未新增接口、迁移或依赖。已复读调用链并检查差异格式，类型检查、运行回归和目标构建留到 F1 阶段末统一执行；月份选择 UI 仍属 F2.3。

### F1 集中验收记录（2026-09-22）

F1.2 将逐日资金、最低余额、最低日缓冲、原因日期和事件引用纳入共享响应，并新增本人未承诺项目的只读影响预览；页面明确区分周期整体结论与单项增量。F1.3 复用流水已有商户名、备注和订单名生成摘要，以原始类别或已核验退款事件确定退款身份；修改展示分类不再改变退款统计。首页提醒偏好只过滤提示摘要，逾期、订单等完整待办仍可进入。

集中检查结果：contracts、server、miniapp 类型检查通过；cashflow-engine、budget-cashflow、worker-runtime 三组定向测试共 45/45 通过；差异格式检查通过。未新增迁移或依赖，未重复运行全量构建、Sandbox、微信开发者工具或真机流程。F1 的设备端呈现随最终微信验收核对。

### F2.1 实施记录（2026-09-23）

首页改为读取本人当前周期的只读 30 天预测，跨月缺少有效相邻周期时，逐日列表在未知日期显示中断，完整 30 天最低余额不作确定结论。主账户卡显示资金依据或最近快照时间；未来安排按日期列出已登记必要支出、还款和预计收入，预计收入明确不计预测现金。最低余额日的保留目标差额与全区间最小缓冲分开显示，避免跨月目标变化时混淆。复用现有预算引擎和账户事实，没有迁移或新依赖。F2.1 仅做相关类型与差异检查，完整页面、窄屏和断网恢复随 F2 阶段末统一验收。

### F2.2—F2.3 实施记录（2026-09-23）

计划页补四项摘要、并集多选与分组折叠、已结束/取消项目和历史入口；新增/编辑项目先调用只读预览比较原值与变更后的日期、金额、最低余额及缓冲或缺口，用户再确认保存。取消未承诺项目复用单项影响预览。前端写入附带所预览的资金版本，服务端在版本变化时拒绝旧确认；已承诺项目不显示普通编辑入口。账目页的月份独立于预算周期，所选账户的 posted 流水参与支出、总流入及退款子集统计；新增未来义务筛选、日期折叠、分类汇总和订单详情入口。普通非订单流水与计划的人工关联已于 F3.3 实施，具体金额、归属、版本和保留历史规则见 F3.3 实施记录。未新增迁移或依赖；F2 集中 H5 核对记录见下文。

### F2.4 与 F2 本地验收记录（2026-09-23）

首页和“我的”在同一应用会话共享主账户金额显隐；账户页区分可用、已撤回和无借记账户，默认账户、目标设置与通知摘要偏好显示对应真实状态。复用已有压缩 IP 素材，不为纯装饰增加主包资源。本地 H5 财务场景核对过五个主标签、当月/历史/空月份、计划新增与取消、账目汇总、360×640 窄屏及一次断网恢复；微信开发者工具和真机未执行。后续静态审核发现单项影响前后可能混用资金版本，以及日期格式随设备时区漂移，本轮已分别统一版本检查和上海时区格式，不能把修复后的代码检查追认为浏览器重测。

### F3.1—F3.2 实施记录（2026-09-23）

首页最低日、计划项目和账目月份可将选中的周期、项目/日期或月份带入 AI 页面。AI 先展示结构化资金依据与待发送问题，用户点击发送后才启动模型；账目工具由服务端限定本人周期账户及本次选中上海自然月，只返回 posted 收支、退款子集与分类汇总，不向模型暴露全部逐笔流水。已有预算工具补充 30 天预测，用于解释最低日；资金依据不足时保留未知状态。

对话产物卡和草稿详情读取持久化草稿，逐字段区分用户输入、行止建议和待补充。Agent 保存单项草稿前会核对本次用户消息中明确出现的日期、金额及优先级；无法核对或同一消息含多项时，这些值降为建议，登记商品也只作为建议，不冒充用户已选商品。只有用户字段完整时才能直接查看该项只读资金影响；缺失时进入编辑，由用户逐项补全并确认正式计划。采纳整份草稿只改变草稿状态，已采纳草稿的多个项目仍可分别进入确认；运行历史按服务端 `COMPLETED` 状态标为已完成。查看影响本身不建单、不支付。登录失败限制采用迁移 `034` 的持久计数，H5 与小程序入口共用；公开部署仍需结合实际网关评估分布式请求限制。

已有实施记录称迁移 `034` 已应用到当时的本机开发库；服务端受影响的预算计算、草稿、运行时定向回归通过，其中包含登录限制和月度汇总模型工具用例；全工作区类型检查和微信产物构建通过。真实模型回答、F3 页面连续操作、真机及 Sandbox 均未在该轮执行。此后 F3.3 已完成代码及定向用例；本轮集中验收结论见上文。迁移是否已在其他目标库登记须另行核对。

### F3.3 实施记录（2026-09-23）

迁移 `035` 新增普通流水与计划的关联及解除历史，当前本机开发库已应用。消费者在账目详情选择本月未承诺的支出计划并确认覆盖金额；同一已入账非订单流水只能有一个有效关联，不跨账户、跨自然月或覆盖超过流水/计划余额，已形成购买意图、订单或还款结算的事实不能复用。关联和解除都会推进周期版本，原流水金额、状态与分类不改；已有订单和退款处理保持原有证据链。接口为 `GET/POST /api/budget-periods/:id/ledger-links` 和 `POST /api/budget-periods/:id/ledger-links/:linkId/unlinks`，写入使用消费者会话、结构化确认与幂等键。

资金预测与预算摘要按已覆盖金额扣减“尚未发生计划”，已入账支出仍只通过账户事实扣一次。月度复盘列出已覆盖金额、剩余计划金额、未覆盖普通支出及单笔部分覆盖，计划详情和账目明细均可追到关联对象；解除后恢复未发生计划，并保留事件和历史行。定向服务端用例覆盖部分发生、重复关联、解除及复盘口径；该次实施未验收 F3 真实模型、页面连续流程、微信设备或 Sandbox；后续模型修复复验结果见上文及验收文档。
