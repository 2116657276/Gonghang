# 行止 UI 升级详细实施计划 v2

> 编制日期：2026-09-26。用途：交给 AI 或开发人员逐项实施的界面规格书。
>
> 本文是**待实施的设计方案**，不是 UI 已完成或真机已验收的记录。本轮只新增本文，不修改应用代码。
>
> 设计方向：**薄荷生活计划册**。面向青年用户，清新、明亮、轻松、有陪伴感；资金、授权、支付和退款表达仍须准确、克制。

## 阅读顺序

1. 先读第 1 节的改动边界，不能把 UI 升级变成业务重写。
2. 第 2—5 节定义颜色、单位、字体、布局和公共组件；所有页面必须继承。
3. 第 6 节覆盖全部 34 个消费者页面；第 7 节覆盖管理网页及角色入口。
4. 第 8—10 节规定状态、数据约束、实施顺序和验收。
5. 第 11 节提供可直接复制给 AI 的执行任务模板。

相关文件：

- [现行产品定位与能力边界](xingzhi/doc/01-product.md)
- [业务与资金规则](xingzhi/doc/02-business-rules.md)
- [唯一的项目开发状态记录](xingzhi/doc/04-development.md)
- [完整业务验收文档](xingzhi/doc/05-acceptance-and-demo.md)
- [项目启动说明](README.md)
- [交接说明](开发接力指南.md)
- [原设计规范](xingzhi/行止_小程序前端设计与开发规范_v1.md)

本文只细化本轮 UI 升级。历史进度继续维护在 `04-development.md`；不要另建一份互相矛盾的项目状态表。新旧视觉规范冲突时，本轮采用本文的视觉参数；安全规则、财务规则和用户的新指令不因本文而失效。

## 1. 目标、范围与禁止事项

### 1.1 本次要达到的效果

- 用户第一眼能分清“我现在的钱”“未来安排”“已经发生的账目”。
- 字更清楚、按钮更好点，重要金额和状态不埋在小字里。
- 颜色有活力，但不是每张卡都使用渐变、插图和阴影。
- 主页面、详情、编辑表单、弹层、空状态使用同一套视觉语言。
- 微信小程序和消费者 H5 来自同一套页面源码，功能、文案与核心布局保持一致。
- 保留现有绿色卡通 IP；不更名、不更换角色、不重新生成一套形象。
- 首页“我的主账户”和“未来 30 天”两张卡**始终并排**。小屏增加卡片高度和文字换行，不改为上下堆叠。

### 1.2 代码范围

| 范围 | 现有位置 | 本轮处理 |
| --- | --- | --- |
| 消费者端 | `xingzhi/apps/miniapp/src` | 34 个页面、公共组件、样式和现有素材的展示方式 |
| 共享样式 | `src/styles/_tokens.scss`、`src/app.scss` | 统一颜色、字号、留白、按钮和状态 |
| 页面外壳 | `src/components/PageShell.vue` | 页头、内容容器、安全区域与详情返回布局 |
| 公共组件 | `src/components` | 卡片、列表、标签、弹层、空状态、IP 等 |
| 管理网页 | `xingzhi/apps/web/src` | 登录、商户、审核、消费者迁移入口的视觉一致性 |
| 业务数据 | `src/lib/api.ts`、`src/lib/types.ts`、`src/composables` | 读取现有数据；不为视觉效果改变业务接口、字段含义或计算 |
| 构建配置 | `app.config.ts`、各页 `.config.ts`、`config/index.ts` | 原则上不动路由、分包、构建插件；仅在必要时调整导航配色 |

表中的 `src` 若未另行说明，指 `xingzhi/apps/miniapp/src`。

### 1.3 本轮不做

1. 不修改后端、数据库、资金算法、权限规则和支付流程；发现确实需要后端支持的内容，单列并先请示用户。
2. 不增加签到、积分、成就、消费评分、长期理财目标、社区、语音、附件或银行自动同步。
3. 不新增没有接口支持的按钮、占位入口或虚假的点击成功提示。
4. 不生成随机余额、随机趋势、假账目或伪造 AI 回复来填满界面。
5. 不更换 Taro/Vue 框架，不引入大型 UI 库、图表库、图标字体包或状态管理框架。
6. 不修改启动脚本、API 地址选择、鉴权、幂等键、轮询协议或登录跳转来“配合换皮”。
7. 不恢复旧消费者网页的写流程；`apps/web` 的消费者入口仍只引导到新版消费者端。
8. 不以“年轻化”为由模糊预计缺口、授权撤回、付款结果未知、退款未到账等重要事实。

### 1.4 必须保留的近期修复

- 首页返回后的后台刷新不应销毁整块已经加载的内容；刷新失败应明确标识旧数据。
- 时间显示复用 `lib/format.ts`，不要重新引入未做兼容处理的 `Intl.DateTimeFormat`。
- AI 发请求前的消息准备也必须位于异常处理内，失败后恢复输入与发送状态。
- 手机键盘确认携带最终输入值；按钮点击事件不能覆盖已经输入的文字。
- 保留当前原生组件保留插件、分包、H5/微信独立输出和稳定发布目录。
- 不回退到单凭 `env(safe-area-inset-top)` 判断顶部安全区；保留 `system-insets.ts` 的设备信息处理。
- 保留 Bearer 会话与当前移动联调地址机制，不在界面中写死某次热点 IP。

## 2. 全局视觉规格

### 2.1 风格定义

关键词：奶白底、薄荷绿、圆角线性图标、少量暖色、明确的大数字、轻量生活感。

视觉比例建议：约 80% 中性底色和白色，15% 薄荷或浅蓝信息区域，5% 暖色装饰。比例是设计约束，不是程序计算要求。

- 活泼主要来自 IP、图标、浅色分区和有温度的引导文案。
- 页面正文不使用手写字体；金额不使用花体、窄体或斜体。
- 不使用全屏毛玻璃、持续漂浮、自动轮播、炫光和大面积高饱和渐变。
- 单页最多一个重点摘要区域使用轻渐变；其余卡片使用纯色。
- 大部分装饰无交互，不能盖住按钮或进入无障碍朗读顺序。

### 2.2 色彩表

优先修改现有 SCSS token；只为实际使用的辅助色增加少量变量，不创建主题切换系统。

| 建议 token／用途 | 色值 | 用法 |
| --- | --- | --- |
| `$brand-primary` 品牌主色 | `#267D62` | 主按钮、选中态、关键链接；搭配白字 |
| `$brand-deep` 深品牌色 | `#1B5744` | 强调标题、重点图标；不能代替全部正文 |
| `$background` 页面底 | `#F7FAF7` | 所有消费者页面的主背景 |
| `$card` 标准卡片 | `#FFFFFF` | 详情、列表组、表单 |
| `$surface-tint` 薄荷浅底 | `#E1F5EA` | 主账户、用户气泡、选中账户 |
| `$soft-surface` 轻底 | `#F2F6F3` | 输入框、弱信息区、分区背景 |
| `$accent-cream` 奶油浅黄 | `#FFF1CC` | 计划的少量生活感点缀，不代表风险 |
| `$accent-blue` 晴空浅蓝 | `#E8F1FF` | 预测或只读分析区域，不代表成功 |
| `$accent-peach` 蜜桃浅底 | `#FFE8DE` | 小面积生活分类点缀，不用于严重错误 |
| `$text-primary` 正文 | `#243B32` | 标题、主要金额、内容 |
| `$text-secondary` 次级文字 | `#53685D` | 说明、日期、来源 |
| `$text-tertiary` 弱文字 | `#607166` | 次要元信息；每种底色组合仍须检查对比度 |
| `$border` 边界 | `#DDE8E0` | 输入边框、列表分隔线、弱卡片边框 |
| `$success` / `$success-surface` | `#246C4E` / `#E7F5EC` | 已确认成功、已入账；必须配文字 |
| `$info` / `$info-surface` | `#365F89` / `#EDF4FC` | 中性提示、处理中、只读信息 |
| `$warning` / `$warning-surface` | `#85501A` / `#FFF3DA` | 依据不足、需要调整、待核验 |
| `$danger` / `$danger-surface` | `#AF3941` / `#FFF0EF` | 操作失败、撤回授权、危险操作 |

限制：

- 浅黄、浅蓝、蜜桃色只作为底色，文字仍用深色。
- 不用颜色作为唯一状态区分手段，必须同时显示文字，必要时加图标。
- 金额流入可用成功色；支出默认深正文色，不把所有消费都渲染成红色警告。
- 已撤回、归档等状态用中性色；不能因为灰色就隐藏相关历史入口。
- 现有弱文字 `#97A19C` 与白底对比度约 2.66:1，不再用于需要阅读的正文、时间和状态。

### 2.3 单位约定：必须先读

本文所有界面尺寸默认使用 **L：375 宽手机基准下的逻辑像素**。它不是物理像素，也不是可直接粘贴进当前 SCSS 的设计稿数字。

当前 Taro 配置为 `designWidth: 750`。对随设计稿缩放的常规尺寸，采用 **D = 2 × L** 写入现有设计稿样式体系，再由项目现有转换链处理。例如：

| 设计意图 L | 750 设计稿 D | 说明 |
| --- | --- | --- |
| 正文 16 | 32 | 不要直接把当前 SCSS 字号改成 16 后再缩放一次 |
| 页面边距 16 | 32 | 375 宽基准下对应 16 的留白 |
| 卡片圆角 20 | 40 | 普通卡片不再混用十几个相近圆角 |
| 主按钮高 48 | 96 | 实际触控区还需通过真机核对 |

特别说明：

- 320、360、390、430 宽上的换算会变化，必须核对最终实际字号和触控区，不能仅做乘二后宣布完成。
- 设备 API 返回的状态栏、胶囊、键盘尺寸是运行时逻辑尺寸，**不乘二**。
- `env()` 安全区、运行时内联尺寸和构建转换后的 rpx/rem 不再二次转换。
- 不盲目把运行时 px 改成 rpx，不盲目用大写 `PX` 绕过转换；先检查对应平台产物。
- `apps/web` 的管理网页使用普通 CSS px，**不乘二**。
- 响应式断点按实际视口宽度定义；必须检查转换后的媒体查询，避免 360 宽断点被误转为 180。
- 本文列出的高度多数是最小高度。文字增大、换行和系统字体设置优先，禁止为了固定截图而裁切内容。

### 2.4 字体与字号

字体族沿用系统字体：`-apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', sans-serif`。不下载网络字体，不额外增加字体包。管理网页可保留已有系统无衬线回退；去掉页面大标题与正文不一致的装饰性宋体。

| 编号 | 用途 | L / D 字号 | 字重 | 行高 |
| --- | --- | --- | --- | --- |
| T01 | 一级页面标题 | 24 / 48 | 700 | 1.3 |
| T02 | 详情页、弹层标题 | 20 / 40 | 600—700 | 1.35 |
| T03 | 区块标题、重点卡标题 | 18 / 36 | 600 | 1.4 |
| T04 | 正文、表单值、列表名称 | 16 / 32 | 400；名称 500—600 | 1.5 |
| T05 | 日期、来源、说明、状态 | 14 / 28 | 400—500 | 1.5 |
| T06 | 少量次要元信息 | 13 / 26 | 400 | 1.45 |
| T07 | 极少量非关键脚注 | 12 / 24 | 400 | 1.45 |
| T08 | 主余额、全宽金额 | 32 / 64 | 700 | 1.15 |
| T09 | 双卡金额、重点统计 | 24 / 48 | 700 | 1.2 |
| T10 | 列表金额、次级统计 | 18 / 36 | 600—700 | 1.3 |
| T11 | 按钮文字 | 16 / 32 | 600 | 1.25 |
| T12 | AI 长回复 | 16 / 32 | 400 | 1.65 |

使用规则：

- 页面中一般只同时出现 3—4 个字号层级，不能把上表所有层级都塞进每张卡。
- 关键状态、授权影响、报价有效期、错误说明不得使用 T07。
- 同一行的名称、图标、金额使用一致对齐规则；金额不能随不同数字位数左右摇动。
- 金额使用 `font-variant-numeric: tabular-nums`，继续复用 `yuan()`，不改变分／元换算。
- 长金额优先增高、合理换行；禁止省略为 `¥12…`、裁切小数或把未知值显示为 0。
- 长姓名、账户名可在列表中两行展示；详情中完整显示。标识号和 API 地址允许断行。

### 2.5 间距、圆角、阴影与动效

| 项目 | L | D | 使用规则 |
| --- | --- | --- | --- |
| 间距阶梯 | 4 / 8 / 12 / 16 / 24 / 32 | 8 / 16 / 24 / 32 / 48 / 64 | 优先从此表取值 |
| 页面左右留白 | 16；320 宽可为 12 | 32；窄屏 24 | 不挤压首页双卡到互相覆盖 |
| 卡片间距 | 12 | 24 | 连续卡片 |
| 大区块间距 | 24 | 48 | 标题之前 |
| 区块标题到内容 | 12 | 24 | 全站统一 |
| 标准卡内边距 | 16 | 32 | 摘要卡可为 20 / 40 |
| 紧凑双卡内边距 | 14 | 28 | 不与全宽大卡相同 |
| 卡片圆角 | 20；重点卡 24 | 40；重点卡 48 | 列表组统一 20 |
| 输入圆角 | 12 | 24 | 不使用巨大的药丸输入框 |
| 标签圆角 | 8 或胶囊 | 16 或胶囊 | 同种标签一致 |
| 弹层顶部圆角 | 24 | 48 | 底部不额外留悬浮空隙 |

- 普通卡片：白底＋弱边界，通常不加阴影。
- 重点摘要：允许一层轻阴影，视觉基准为 `0 4px 14px rgba(36,59,50,.05)`，落地时按所属单位体系处理。
- 弹层阴影强于卡片，但不能靠阴影替代遮罩。
- 按压反馈 120—160ms；弹层过渡 180—220ms；不添加首页入场整屏动画。
- H5 尊重减少动态效果偏好；小程序优先简单的颜色、透明度反馈。
- 切换标签不因动画重新挂载页面或重置业务状态。

## 3. 公共布局与卡片体系

### 3.1 页面外壳

继续复用 `PageShell.vue`，最多增加少量明确的布局变体，不引入新的路由或布局框架。

| 布局 | 使用页面 | 布局定义 |
| --- | --- | --- |
| L01 主标签页 | 首页、计划、账目、我的 | 安全区 → 标题行 → 可选一行说明 → 内容区 → 底部安全余量 |
| L02 详情页 | 账户、计划项目、资金影响等 | 安全区 → 左返回＋标题 → 首张卡，内容自然滚动 |
| L03 编辑／确认页 | 新建预算、编辑计划、购买确认 | L02＋分组表单／事实＋页底操作区 |
| L04 对话页 | AI | 安全区 → 紧凑标题和上下文 → 对话 → 键盘感知输入区 |
| L05 登录页 | 消费者登录 | 安全区 → 品牌 IP → 登录表单 → 环境说明 |

具体要求：

- 主页面标题 T01，副标题 T05，间距 6；不再每页使用大面积山丘背景占据首屏。
- 页头可有小块薄荷装饰，最大高度约 96，不计系统安全区；不能硬编码总页面顶部为某个固定值。
- 详情页返回按钮位于左侧、触控区至少 44；标题 T02；返回不是右上角装饰按钮。
- 自定义顶部内容放在设备胶囊区域下方，或明确避让其占用范围；不能与微信关闭、更多按钮重叠。
- 详情页标题很长时允许两行，页头随内容增高。
- 移除依靠大负 margin 叠压正文的布局方式；装饰和内容的关系用正常文档流定义。
- H5 手机端全宽；桌面消费者 H5 将主内容居中到约 480—520 宽，不能无限放大成工作台。具体最大宽度必须核对转换后的真实 CSS 尺寸。
- 列表优先单一页面滚动。只有弹层和对话区等确有必要的区域使用独立滚动，避免多层滚动相互争抢。

### 3.2 卡片类型

| 编号 | 类型 | 外观、布局与适用场景 |
| --- | --- | --- |
| C01 | 标准信息卡 | 白底，圆角 20，内边距 16；标题 T03，正文 T04、说明 T05；账户依据、设置说明 |
| C02 | 重点摘要卡 | 薄荷／浅蓝／奶油浅底，圆角 24，内边距 20；最多一个主金额；首页、月度概览 |
| C03 | 列表分组卡 | 白底，圆角 20，组外间隔 12；内部行间用细线或留白，不每行再加阴影 |
| C04 | 状态提示卡 | 状态浅底＋对应深色图标／标题；图标 20，标题 T04，说明 T05；错误必须给恢复动作 |
| C05 | 表单卡 | 白底，圆角 20，字段纵排；标签 T05，输入 T04，字段间距 20 |
| C06 | 确认对比卡 | 白底＋轻色表头，横向或纵向“修改前／修改后”；金额 T10，明确变化项；不把所有内容画成平铺 FactRow |
| C07 | 分析卡 | 白底，标题 T03，主结论 T04；图表与数据口径同卡呈现，说明 T05 |

统一卡片排列：卡标题 → 核心内容 → 辅助说明 → 动作。单卡最多一个明显主动作，其他动作使用次按钮或文字按钮。

### 3.3 列表与对齐

- 普通列表行最小高度 64；账目、计划等两到三行内容为 76—96，实际可增高。
- 标准三段式：左图标 40 → 间距 12 → 自适应文字区 → 金额／状态区。
- 中间文字容器必须 `min-width: 0`；右侧金额不得被挤到卡外。
- 标题 T04，副标题 T05，金额 T10；金额与名称第一行的基线或顶部对齐，全站一致。
- 图标容器不随文本高度被压扁；统一 `flex-shrink: 0` 或等效约束。
- 在窄屏下，右侧次要操作可移到下一行；不得让“查看详情／问行止／状态”三者挤在同一行。
- 可点击整行必须有明确箭头或动作文字；纯展示行不要加箭头。

### 3.4 底部导航与操作区

- 保留原生五标签：首页、计划、行止 AI、账目、我的；顺序不变。
- 图标采用同一视觉体系，视觉尺寸约 24；标签保留原生可用显示规则。
- 选中为品牌绿，未选中为次级灰绿；不增加悬浮突出的大 AI 球，不改自定义 TabBar。
- 非对话页优先使用文档流中的底部按钮组，避免新固定栏压住内容。
- 若确认页确需固定操作栏，必须为其实际高度预留内容底部空间，并处理安全区；不能硬编码一个高度覆盖所有设备。
- 不同时重复扣除原生 TabBar、系统底部安全区与自建占位，避免底部出现大空洞。

## 4. 公共组件详细规格

### 4.1 `SectionHeader` 与 `SectionCard`

- `SectionHeader`：左标题 T03，右动作 T05，整体最小高 44；长标题允许换行，右动作不与标题重叠。
- `SectionCard`：默认 C01；重点色应通过少量语义变体或页面限定类表达。
- 全站不要再次定义会互相污染的 `.metrics`、`.chip`、`.summary-card` 裸通用类。复用公共组件，或使用页面前缀限定。
- 不为本次改版建设任意参数驱动的“万能卡片”；已有组件能承载的内容继续复用。

### 4.2 `StatusBadge`

- 字号 T05，字重 500，内边距上下 4、左右 8，最小高约 28。
- 图标为可选 14—16；短状态用单行，长状态允许换行，不能撑坏卡片。
- 成功、待确认、未知、失败、已撤回必须有准确文字，不只依赖绿色／黄色。
- “资金结论”“项目状态”“支付状态”不是同一字段，不能用一个徽章代替三者。

### 4.3 `FactRow`

- 左标签 T05，右值 T04；关键金额可用 T10。
- 默认标签占 38%、值占剩余空间，列间距 12；两列顶部对齐。
- 值右对齐；长解释改为下一行左对齐，不能把一整段话塞进窄窄的右栏。
- 行内边距上下 12；最后一行不画分隔线。
- 320 宽或放大字体后标签与值互相挤压时，切换为上下两行，不缩小文字。
- 未知值使用现有明确文案；不得用空白或破折号掩盖必须说明的未知原因。

### 4.4 `PlanItemCard`

- C01／C03 的计划变体：左侧生活类别图标，名称 T04，日期和安排属性 T05，金额 T10。
- 不再用“必／愿”汉字作为主要图标；必须保留／可以调整继续作为文字标签。
- 第一行是对象与金额；第二行是日期和属性；第三行才放状态与行动。
- “查看详情”或“看看影响”为一个主入口，“问行止”为次入口；必要时动作单独占一行。
- 已结束项目降低背景强调，不降低必要文字的可读性。
- 周期整体资金结论必须标注“本月整体”，不得让用户误以为它是该单项的独立准入结论。

### 4.5 `SettingGroup`

- C03，标题 T03，图标 20—24，设置行 T04／T05，行最小高 56。
- 一级设置全部直接可见，删除“已展开”文字，不重新加入自动折叠。
- 分组之间间距 16；组内边线从文字区开始，不穿过图标。
- 右侧说明最长两行；尾部箭头位置一致。

### 4.6 按钮、筛选与表单

- 主按钮：品牌绿＋白字 T11，最小高 48，圆角 14；不默认大渐变。
- 次按钮：白底或轻底＋品牌深色字＋边框；尺寸与主按钮一致。
- 文字按钮：视觉可小，触控区仍以至少 44×44 为目标；不能只有一条很细的可点文字。
- 危险按钮：明确写出动作，例如“确认撤回授权”，不用“好的”“继续”代替。
- 筛选项：高度约 36，独立触控区域目标 44；文字 T05，左右内边距 14，项间距 8。
- 筛选选中态为薄荷底＋深绿字＋适当边界；同类组件不能在不同页一会儿白字深底、一会儿绿字浅底。
- 保留计划页已有多选并集逻辑；账目页保留原来的单选筛选，不为统一外观改变选择语义。
- 表单标签常驻，不能只靠 placeholder；输入最小高 48，字号 T04，内边距左右 14。
- 多行备注最小高 96，金额字段可使用更大数字但保留单位。
- 错误就近展示 T05，并保留用户输入；按钮禁用应说明原因，不让整页失去响应。

### 4.7 `BottomSheet`

- 遮罩 `rgba(20,36,28,.32)`；面板实白底，顶部圆角 24。
- 顶部握柄宽 36、高 4，距离顶部 10；可只作视觉提示，不伪装成已经支持拖拽关闭。
- 标题 T02，说明 T05；右侧关闭图标视觉 20，触控区 44。
- 水平内边距 20，窄屏可 16；标题到正文 16。
- 面板使用“标题固定／正文滚动／操作固定”的纵向布局，正文 `min-height: 0`。
- 普通摘要最高约可用屏幕的 72%；长详情最高约 88%；以扣除安全区和键盘后的可用高度为准。
- 正文 T04，事实说明 T05，金额 T08 或 T09；不得为了放下更多行而缩小到难读。
- 两个按钮足够宽时并排；窄屏、长文案或大字号时竖排，保持主次顺序明确。
- 同一时间只保留一个可交互弹层；分类选择、关联计划等二级操作应关闭／切换前一层，并能返回原详情。
- 背景不应跟随弹层正文滚动；关闭后恢复原页面位置。
- 小程序原生 TabBar 与面板的关系必须实测：优先沿用现有已验证方式；若原生栏位于遮罩上方，面板需避让且不得露出可误触的背景入口。任何临时隐藏导航的实现都必须在关闭、退出、异常时恢复。
- 打开弹层不得发起购买、退款、授权撤回等写操作，只有明确确认按钮才能触发原有流程。

### 4.8 `StatePanel` 与骨架屏

- 首次加载可用与最终卡片结构相近的静态骨架，不放假金额、假曲线。
- 已有数据刷新时保留内容，展示小型刷新提示；失败后标注“当前显示上次读取的数据”。
- 空状态：一个小 IP 或线性图标＋T03 标题＋T05 说明＋一个真正可用的下一步。
- 错误状态：明确发生了什么、如何恢复；不使用庆祝表情，不自动跳空白页面。
- 无权限、无数据、尚未创建、读取失败必须分开表达。

### 4.9 `IpAvatar` 与图标

- 继续使用 `src/assets/xingzhi-mascot-mobile.png`；约 98KB 的现有移动素材可复用，不直接把原始大设计板装入主包。
- 头像尺寸：聊天 28、个人区 48、欢迎区 80；登录或空态可用约 96—112 的完整形象。
- 完整形象使用保持比例的展示方式；头像版可裁切，但不能将完整身体强行挤进扁圆形。
- 按钮图标视觉尺寸 20—24，列表图标底容器 40—44，底色浅、文字或线条深。
- 需要新增的统一图标仅覆盖已用语义：账户、趋势、日历、餐饮、交通、学习、购物、娱乐、住房、设置、隐私、历史、搜索、查看、返回、关闭、显隐金额等。
- 可以增加一个轻量 `AppIcon` 组件或素材映射，前提是确实被多处复用；不引入整套图标库。
- 小程序使用经过验证的本地兼容素材；H5 与小程序视觉保持一致，不默认认为网页中的 SVG／字体图标可直接用于微信。

## 5. 全局状态与文案规范

| 状态 | 样式 | 推荐表达与限制 |
| --- | --- | --- |
| 初次加载 | 骨架／小型等待区 | 正在读取账户／正在整理计划；不显示 0 元占位 |
| 后台刷新 | 保留内容＋轻提示 | 正在更新，当前显示上次读取的数据 |
| 网络失败 | C04 错误＋重试 | 连接暂时中断，请重试；保留输入 |
| 未知资金 | 警示浅底＋明确文字 | 数据不足，暂时无法预测；不能显示“缓冲充足” |
| 无内容 | 中性空态 | 还没有计划，创建第一项；不得替用户自动创建 |
| 已入账 | 成功标签或普通状态 | 已入账；不要每笔重复“钱已经记好啦” |
| 待确认／处理中 | 中性信息色 | 待确认、处理中；不是成功色的完成勾 |
| 失败／需复核 | 危险／警示 | 失败、结果待核对；提供原有恢复入口 |
| 已归档／撤回 | 中性色 | 保留历史说明，隐藏不允许的写动作但保留合法查询 |

文案保持自然，不卖萌过度。可写“把想做的事安排好”“先看看这笔钱”；不能写“稳赚”“放心花”“已到账”来替代真实资金结论。预计缺口即使很大也不能统一叫“还差一点”。

所有可见按钮均要有真实响应；不支持的能力不放入口。与本地环境、Demo 数据、Simulation／Sandbox 相关的说明可降低重复度，但在账户来源、交易确认、环境详情等必要位置必须保留。

## 6. 消费者端逐页实施规格（P01—P34）

以下文件路径相对于 `xingzhi/apps/miniapp/src`。未单独覆盖的参数继承第 2—5 节。每节的“顺序”就是页面从上到下的排列顺序；不允许只改颜色而跳过布局和状态要求。

### P01 登录页

**文件：** `pages/login/index.vue`；布局 L05。

- **风格配色：** 奶白绿底，顶部一小块薄荷渐变；登录卡纯白，不使用满屏山丘和浓阴影。
- **字体：** 品牌名 28、欢迎标题 T02、表单值 T04、标签和环境说明 T05；全部系统无衬线。
- **布局顺序：** 安全区 → 完整 IP → 行止名称／一句定位 → 登录卡 → 环境说明。
- **位置尺寸：** 页面边距 20；IP 96；品牌与表单间距 24；卡内边距 20；字段间距 20；按钮距最后字段 24。
- **卡片内容：** 邮箱、密码、就近错误提示、进入按钮；不把标题、副标题、口号各自做成独立卡片。
- **交互状态：** 密码仍为原生安全输入；输入过程中不重建输入节点；提交中按钮防重复、键盘不遮挡错误。
- **短屏：** IP 可缩至 72，品牌区留白减少；表单字号不缩小，页面允许滚动。
- **不改：** 登录协议、角色判断和首页导航恢复逻辑；不增加手机号、验证码或一键微信登录占位。

### P02 首页

**文件：** `pages/home/index.vue`；布局 L01。

- **风格配色：** 奶白底；主账户浅薄荷；未来 30 天白底或浅蓝；趋势白底；提醒按真实状态着色。
- **字体：** 问候 T01；双卡标题 T04、金额 T09、状态 T05；区块 T03；列表名称 T04、金额 T10。
- **卡片顺序：** 问候／待办 → 双卡 → 资金趋势 → 问行止 → 需要留意／未来安排 → 近期计划 → 最近账目。
- 重要逾期／待确认事项在双卡之后增加一行简短提醒，并能定位完整待办；不能因版式把必须处理的事项埋到底部。

375 宽基准下的布局示意，以下不含系统安全区：

```text
左右各 16
问候语                                  待办 44×44
一行简短说明
间距 16
┌──────── 主账户 ────────┐ 12 ┌────── 未来 30 天 ──────┐
│ 钱包图标＋我的主账户   │    │ 趋势图标＋未来 30 天   │
│ 金额／隐藏金额         │    │ 预计最低金额           │
│ 账户短名称、数据时间   │    │ 最低日期／未知说明     │
└───────────────────────┘    └───────────────────────┘
间距 16
资金趋势：结论 → 折线／未知态 → 日期与口径
间距 12
小 IP  问问行止：看看本月安排                      ›
需要留意／未来安排
近期计划
最近账目
原生五标签导航
```

**双卡细节：**

- 两列等宽，列间距 12，卡片内边距 14，圆角 20，最小高约 168；两卡自然等高。
- 375 宽、左右 16 留白时，每卡约 165.5 宽；320 宽、左右 12 留白时每卡约 142 宽。标题和金额必须按此空间设计。
- 图标 24；标题最多两行；显隐按钮放在不挤压标题的位置，仍有足够触控区。
- 去掉重复的“账户概览／我的主账户”双标题，只保留明确名称。
- 主账户显示确认资金，未来卡明确标注“预计最低”，不能把它们都叫“可用余额”。
- 长金额允许分行或提高卡片高度；不隐藏卡片、不横向溢出、不省略金额、不改上下排列。
- 账户信息缺失时显示“选择规划账户”且可进入原有选择页；不要显示假余额。

**趋势和下方内容：**

- C07 全宽，图形区高约 144，图上／图下保留一条主要结论。
- 线宽约 2，主线品牌绿；弱网格线；最低点使用实心点＋日期／金额提示，不靠红点传达全部风险。
- 使用真实 `rollingCashflow` 数据；未知区间断开，不能删除未知天后把两侧已知点直接连成连续预测。
- 若已有折线实现不能正确保持坐标比例，先局部修正并做两端验证；不为换色直接引入新图表库。
- 日期安排用轻量时间线；预计工资明确写“预计，未计入现金”。
- AI 条为浅薄荷横向入口，高度随文字变化，IP 40；点击只带入问题，由用户发送。
- 最近账目按组展示三行左右，整体一个 C03；不每笔再加重阴影。
- 首次加载用骨架；切回刷新保留内容；预测失效与账户失效分开提示。

### P03 计划页

**文件：** `pages/plan/index.vue`；布局 L01；复用 `PlanItemCard`。

- **风格配色：** 白底列表＋奶油／薄荷本月概览；减少现有四个彩色子卡各自的边框和阴影。
- **字体：** 页头 T01；月份 T02；四指标标签 T05、金额 T10；计划名称 T04。
- **顺序：** 标题／新增 → 本月概览 → 状态筛选 → 分组计划 → AI 引导 → 历史／复盘工具组。
- **概览：** C02，圆角 24，内边距 20；“保留目标”重点显示；必要安排、可调计划、最低缓冲组成清晰的 2×2 信息格，间距 12。
- **负数：** 直接显示“预计缺口”；不统一写“还差一点”。无周期不伪造四个 0，展示可创建预算的空态。
- **新增：** 页头或概览上方提供“＋ 新增计划”明确入口，触控高 44；保留已有周期／无周期／已归档的分支。
- **筛选：** 全部、进行中、需留意、草稿、已结束；保留原多选逻辑；可横滑，不能缩字。
- **列表：** 单列，卡间距 12；已结束、取消不与进行中混为同一视觉状态；现有分组折叠可保留。
- **底部：** AI 引导单独一张轻卡；历史与复盘为一个 C03 内两行，不再三块卡互相挤压。无本月周期时复盘行显示原因。
- **弹层：** 月度影响用 C06／FactRow；项目摘要显示金额、日期、属性、真实状态和“查看详情”。不在展开摘要时写入数据。

### P04 行止 AI

**文件：** `pages/ai/index.vue`；布局 L04。

- **风格配色：** 奶白底；用户气泡浅薄荷；AI 气泡白；草稿浅蓝边界；只有异常消息使用警示色。
- **字体：** 页标题 T02；正文 T12；上下文、时间、运行状态 T05／T06；草稿标题 T04。
- **顺序：** 标题／历史 → 一条上下文摘要 → 欢迎或对话 → 待发送问题（有则展示）→ 输入区。
- **上下文：** 默认只显示“账户短名 · 月份 · 状态”；高约 44—56。完整资金依据改为用户可展开区域／现有上下文弹层中的只读区域，当前所选对象始终可见。
- **初次进入：** IP 80、一句欢迎语、最多四个短问题，采用两列或可换行布局。大字号时变单列。
- **开始对话后：** 大 IP 和欢迎区缩减；快捷问题降为一行次级入口，不占据每屏上半部。
- **气泡：** 最大宽度约可用消息区的 86%；内边距 14—16；圆角 18；消息间距 16；头像 28。
- **回复：** 按实际返回文本的段落展示，保留换行；不在前端重写财务结论或编造“原因／建议”小标题。未经安全处理不能用原始 HTML 渲染模型输出。
- **草稿：** 单独结果卡，显示标题、日期、预算及字段来源；“待确认”不能装饰成成功完成。
- **输入：** 保留已验证的原生输入和事件通路作为首版；输入区内边距 12，输入最小高 48，字号 T04；上下文按钮与发送按钮不覆盖原生输入区域。
- **键盘：** 根据真实可用高度调整对话区与输入区。首版可沿用独立发送按钮行；若改为同行，必须先通过真机点击和键盘确认测试，不能为了截图强行合并。
- **不自动：** 快捷问题只填入，不自动提交；已有上下文不被界面切换偷偷改掉；切回时不重复创建 AI 运行。
- **失败：** 保留问题，恢复可操作状态，显示服务未配置、网络失败或运行失败的真实情况；不一直“正在发送”。
- **历史语义：** 当前历史是运行记录和结果恢复，不宣称已经支持跨会话完整上下文记忆。

### P05 账目页

**文件：** `pages/ledger/index.vue`；布局 L01。

- **风格配色：** 白色账目组，薄荷月度摘要；分类图标可少量使用浅蓝／奶油底；未来义务单独警示浅底。
- **字体：** 页头 T01；月份 T03；主要月支出 T08；流入／退款 T10；流水名称 T04、金额 T10、日期 T05。
- **顺序：** 标题／搜索 → 月份与摘要 → 本月 AI 提问 → 类型筛选 → 未来义务区（按筛选显示）→ 按日账目列表。
- **月份：** 将原先独立月份条与“某月账本”重复标题合并；月份可点击切换，仍使用真实月份选择能力。
- **摘要：** C02，左侧或上方突出支出；下方两项写“总流入（含退款）”“其中退款”。不把三者相加为总额。
- **分组列表：** 每天一个 C03，行内上下 14；图标容器 40；名称、时间、必要的状态／关联信息左排；金额右上对齐。
- **状态：** 普通已入账项不反复展示长句；待确认、冲正、关联计划使用明确标签。收入、退款不能仅凭正号互相混称。
- **未来义务：** 与已发生账目视觉分区，金额未知时写“待核验”，不能计入已发生支出。
- **账目详情弹层：** 名称 T02 → 图标 44＋大金额 T08 → 真实状态 → 时间／账户／分类 → 关联计划 → 操作。长说明纵排，不再全塞成很小的双列格。
- **详情文案：** 按 `isRefund`、方向和状态选择文案；不得对待确认流入或退款统一写“收入到账，安心一点”。
- **分类选择：** 三列、图标＋名称，单项最小触控 44；320 宽／大字号降两列。保留分类修改不改原流水的说明。
- **关联计划：** 先展示本笔支出，再选择可关联项目，最后确认覆盖金额。原有未承诺、同账户／月份等限制不改。
- **统计弹层：** 金额汇总＋分类列表／简单横条；只使用现有类别汇总，没有真实数据就显示空态，不画装饰性饼图。

### P06 我的

**文件：** `pages/profile/index.vue`；布局 L01。

- **风格配色：** 奶白底，个人区轻薄荷，设置白色分组；危险操作只在底部突出。
- **字体：** 昵称 T02，邮箱 T05；主余额 T08；设置标题 T03、行文字 T04、说明 T05。
- **顺序：** 个人身份 → 主账户 → 规划设置 → 数据与授权 → 应用设置 → 历史与复盘 → 账户管理 → 退出。
- **身份卡：** 头像 48，文字自适应，右箭头；“查看资料”是动作而非绿色成功状态。
- **账户卡：** C02，来源／尾号、确认余额、显隐按钮、数据时间；无账户与撤回状态分别表达。
- **设置：** 全部行直接可见，删除“已展开”；换掉“规／权／设／史／户”方块汉字，使用统一图标。
- **对齐：** 左名称、右当前值、最右箭头固定轨道；当前值允许两行，不能挤压名称或画出横向滚动。
- **危险区：** 撤回与退出独立，保留二次确认及其影响说明；不因换皮变成一键直接撤回。

### P07 个人资料

**文件：** `subpackage/common/profile-info.vue`；布局 L02。

- **风格配色：** 顶部薄荷 C02，其余白色 C01；不做会员中心或等级勋章。
- **字体：** 姓名 T02，身份 T05，资料标签 T05、值 T04。
- **顺序：** 返回标题 → 居中 IP／姓名／消费者身份 → 资料事实 → 只读说明。
- **卡片位置：** IP 72、距姓名 12；资料卡与身份卡间距 16；邮箱完整断行，账户和预算数量使用 FactRow。
- **组件：** `IpAvatar`、`FactRow`、`StatusBadge`；资料数量不做娱乐化进度条。
- **状态与限制：** 加载失败有重试；本版本无资料编辑／头像上传接口，因此不增加铅笔按钮、相机角标或假保存。

### P08 历史计划

**文件：** `subpackage/common/plan-history.vue`；布局 L02。

- **风格配色：** 白色月份卡，已归档用中性标签，当前／草稿状态清楚区分；年份可作轻分组标题。
- **字体：** 月份 T03、项目数量与保留目标 T10、资金判断 T05。
- **顺序：** 返回标题 → 年月列表 → 无记录空态。
- **卡片布局：** 每个月一张 C01，顶部月份＋状态；下方项目数量／保留目标两列；资金判断占独立一行，避免三列长文案挤压。
- **动作：** 整卡进入原周期详情；有同月不同账户时显示账户标识（仅使用已有可关联账户数据），不把它们合并成一个月。
- **边界：** 不增加当前接口没有支持的按年汇总总资产、历史收益曲线；空态的创建入口复用现有周期创建页。

### P09 AI 历史与结果弹层

**文件：** `subpackage/common/ai-history.vue`；布局 L02。

- **风格配色：** 白色列表，浅蓝运行状态，失败为警示；不把历史记录画成消息联系人列表。
- **字体：** 记录标题 T04，日期和周期说明 T05；结果文本 T12。
- **顺序：** 返回标题 → 记录列表 → 加载更多 → 错误提示；点击后结果弹层。
- **卡片布局：** C03 或间距 12 的 C01；首行关联周期／未绑定说明＋状态；次行时间＋查看结果。
- **数据约束：** 若列表接口没有原问题摘要，不编造摘要，不为显示标题逐条额外请求详情。继续使用周期和时间定位记录。
- **结果弹层：** 使用长详情规格；正文与草稿入口分组；运行中保留“停止本次整理”，该动作仍走原有接口。
- **状态：** 首屏加载与加载更多分开；后者失败保留旧列表；“关闭结果”不等于取消后台运行。

### P10 账户详情

**文件：** `pages/account/detail.vue`；布局 L02。

- **风格配色：** 薄荷余额摘要 C02＋白色依据 C01；预计项使用中性轻底。
- **字体：** 账户名 T03、确认现金 T08、依据标签 T05、值 T04。
- **顺序：** 账户／来源／状态／现金 → 资金依据与时间 → 资金构成 → 查看影响 → 授权管理。
- **摘要：** 尾号而非完整账户号；明确“当前确认现金”，数据不足不显示 0；来源为 Demo 时可见标识。
- **构成：** 已入账统计与预计收入、待核退款、未来义务分组，不能视觉相加成当前现金。统计范围沿用实际数据范围，不随意标成本月。
- **技术信息：** 资金版本、原因码可放“数据依据”区；优先使用现有中文映射，无映射时保留可核对说明，不编造原因。
- **动作：** 授权撤回为次级危险入口；仅 Demo 已撤回账户出现原有重新启用动作，不能泛化为真实银行重新授权。

### P11 规划账户选择

**文件：** `pages/account/select.vue`；布局 L02。

- **风格配色：** 白色账户卡；当前默认用薄荷边界与勾选文字；不可用账户使用中性底，不降低文字对比度。
- **字体：** 账户名 T04、金额 T09、来源／说明 T05。
- **顺序：** 简短规则提示 → 账户列表 → 使用可用账户创建预算按钮。
- **卡片：** 顶部钱包图标 32＋名称／尾号，右状态；中部确认现金；底部“查看依据”和“设为默认”。两操作不互相覆盖。
- **交互：** 默认切换保存中仅禁用有关写动作，保留查看；成功后回显服务端事实。
- **边界：** 只展示可见账户，不能将已撤回／非借记账户伪装成可选主账户；默认变更不改变既有预算归属。

### P12 撤回账户授权

**文件：** `pages/account/revoke-confirm.vue`；布局 L03。

- **风格配色：** 白色事实卡＋危险浅底影响卡；不用卡通庆祝和绿色“轻松撤回”。
- **字体：** 标题 T02，影响分组 T03，正文 T04，辅助 T05。
- **顺序：** 目标账户 → 撤回后不能做什么 → 仍然保留什么 → 知悉确认 → 危险主按钮／保留授权。
- **布局：** 两组影响上下排列，各 C01／C04，间距 16；确认行触控高至少 48，文字可多行。
- **动作：** 未确认影响前不能提交；提交中不得重复操作；错误就近保留，不删除已展示影响。
- **完成态：** 中性完成图标＋授权已撤回＋后续能力边界，按钮返回我的／查看既有订单。不要显示“账户已删除”。

### P13 授权与隐私

**文件：** `pages/settings/privacy.vue`；布局 L02。

- **风格配色：** 白色信息卡、浅蓝隐私图标；账户状态使用语义色。
- **字体：** 小节 T03，内容 T04、行高 1.6，范围标签 T05。
- **顺序：** 当前读取范围 → 账户授权状态 → 安全边界。
- **范围：** 图标＋简短名称的两列区域，大字号变单列；详细说明放其后，不用一大段小字作为首屏。
- **账户行：** 名称／尾号、来源、状态、查看撤回影响；撤回入口与状态标签分开。
- **边界：** 不出现未落地的“银行级加密认证”等宣传；不直接执行撤回，不添加无效隐私开关。

### P14 数据说明

**文件：** `pages/settings/data-usage.vue`；布局 L02。

- **风格配色：** 事实为薄荷标识，预计为浅蓝，AI 建议为奶油标识；正文全部深色，三者不是风险等级。
- **字体：** 类型标题 T03、说明 T04、标签 T05。
- **顺序：** 一句话原则 → 各数据类型说明卡 → 当前能力边界。
- **卡片：** C01，左上类型图标，右上来源标签，正文完整换行；卡间距 12、内边距 16。
- **内容：** 使用页面现有说明数据；可以拆段、调整标题，不能把 `demo`、`bank_api` 预留能力写成当前已接入真实银行。
- **状态：** 纯静态说明不伪造加载动画；如展示动态来源，失败时明确未知。

### P15 应用内提醒

**文件：** `pages/settings/notifications.vue`；布局 L02。

- **风格配色：** 白色设置组，开关开启为品牌绿，关闭为中性灰；边界说明浅蓝或警示浅底。
- **字体：** 选项标题 T04，说明 T05，保存提示 T05。
- **顺序：** 设置列表 → 保存状态／错误 → 提醒边界说明。
- **布局：** 左标题和说明，右固定宽开关区；行最小高 72，说明多行时增高，不压缩开关。
- **交互：** 保存中有明确状态；失败按现有业务逻辑恢复／提示，不只改本地颜色假装成功。
- **边界：** 文案写“应用内提醒”，不能写“微信推送已开启”；重要待办不能被偏好开关隐藏。

### P16 关于行止／运行环境

**文件：** `pages/settings/about.vue`，含 `section=environment` 变体；布局 L02。

- **风格配色：** 关于页薄荷品牌卡；环境页白色事实卡，连接／模型／后台状态使用语义标签。
- **字体：** 品牌名 28，分组标题 T03，信息值 T04，说明 T05。
- **关于顺序：** IP 80＋名称＋定位 → 产品边界 → 环境入口／当前环境事实。
- **环境顺序：** API 与前端形态 → 服务状态 → 数据来源 → 支付模式 → 明确的开发演示说明；保留原查询参数直接定位环境的能力。
- **长字段：** API 地址换行显示、不可截掉主机；版本和日期用 FactRow，不把技术字段挤成小标签墙。
- **状态：** 服务不可达、AI 未配置、Worker 异常分别展示；不能用一个总绿色勾掩盖未就绪项。
- **约束：** 不展示密钥、密码、token；Worker 的说明不应错误暗示所有 AI 请求都由 Worker 执行，文案应以当前后端职责为准。

### P17 新建预算／调整保留目标

**文件：** `pages/period/edit.vue`；布局 L03。

- **风格配色：** 白色 C05，目标输入区允许薄荷轻底；草稿说明用信息浅底。
- **字体：** 标题 T02、字段标签 T05、输入 T04、目标金额可 T09。
- **顺序：** 新建时的账户与月份 → 保留目标 → 编辑时的调整原因 → 影响说明 → 提交。
- **布局：** 字段纵排、间距 20，输入／选择行最小高 48；金额单位固定在旁边，不当作可编辑内容。
- **按钮：** 新建为“创建草稿预算”；编辑为“确认调整”。不能统一变成“立即开启”从而隐去实际动作。
- **状态：** 无可用账户时说明原因并保留查看账户入口；校验失败不清空输入。
- **业务：** 本页没有现成的完整变更预览接口时，不为新布局伪造前后对比。创建仍是草稿，不顺带激活。

### P18 预算周期详情

**文件：** `pages/period/detail.vue`；布局 L02。

- **风格配色：** 月度摘要薄荷，项目白底；已归档提示中性，必要确认警示。
- **字体：** 月份 T02、摘要金额 T09、项目名称 T04、说明 T05。
- **顺序：** 月份／账户／周期状态 → 资金摘要 → 完整依据 → 本月项目 → 原有周期操作。
- **摘要：** 将多条 FactRow 的最关键两项提升为金额块，其他事实保留在同卡／下一张 C01；不丢失必要安排、可调计划和关键日期。
- **项目：** 复用统一 `PlanItemCard`，候选与报价是适用项目的次级动作，不让所有项目都像商品。
- **草稿态：** 保留“确认必要支出并激活”和确认弹层；不自动勾选必要支出已核对。
- **执行态：** 新增项目、意外支出、复盘、订单、归档入口按实际权限显示；归档属于有后果操作，视觉与普通查询区分。
- **归档态：** 历史只读标识显眼，仍可访问订单善后与复盘。

### P19 计划项目详情

**文件：** `pages/item/detail.vue`；布局 L02。

- **风格配色：** 顶部生活类别浅色 C02，影响与记录白底；不能所有项目都用“购物袋”图标。
- **字体：** 计划名称 T02、估算金额 T08、日期属性 T05、分组 T03。
- **顺序：** 项目摘要 → 计划与实际（有则显示）→ 当前资金影响 → 计划操作 → 购买与订单（适用时）→ 变更行迹。
- **摘要：** 名称完整展示、状态在独立位置；日期／类型／必须保留或可调整为短标签，不塞成一行长句。
- **影响：** 明确标注本月整体结论；单项影响仍进入既有影响页，不把整体最低余额写成单项余额。
- **动作：** 修改、查看影响、问行止按主次排列；已承诺、已覆盖、取消等限制沿用现有逻辑。
- **购买区：** 只在已有订单／意图或满足现有候选条件时重点展示；没有适用交易能力的项目不出现占半屏的购买空卡。
- **边界：** 不新增项目封面上传、完成打卡、评分或自动结算。

### P20 新增／编辑计划项目

**文件：** `pages/item/edit.vue`；布局 L03。

- **风格配色：** 白色 C05；类型选项薄荷选中态；预览 C06；取消动作危险文字。
- **字体：** 表单 T04／T05，预览金额 T10，操作 T11。
- **顺序：** 名称 → 金额 → 日期 → 类型／属性 → 修改说明 → 查看变更影响 → 原有取消入口。
- **布局：** 金额与日期可各自成一个视觉小组，但手机默认纵排；类型选项允许换行，不缩小文字。
- **预览弹层：** 每个指标用“修改前／修改后”成对展示，包括日期、金额、最低余额、缓冲、结论；小屏两列改上下。
- **取消弹层：** 明确项目和取消前后影响，确认按钮写“确认取消计划”，不与关闭弹层混淆。
- **约束：** 保留当前月份限制、项目类型／属性约束、已承诺不可普通编辑等条件；AI 建议值仍需用户确认。

### P21 月度复盘列表

**文件：** `pages/review/list.vue`；布局 L02。

- **风格配色：** 奶白底＋白色月度列表，周期执行中／已归档使用明确标签；复盘完整性不由装饰色推断。
- **字体：** 月份 T03，辅助信息 T05，现有可展示金额 T10。
- **顺序：** 标题 → 可回看周期 → 空态／错误。
- **布局：** 每项 C01，月份和账户标识在左、状态在右、底部完整查看入口；列表间距 12。
- **边界：** 仅使用现有返回字段；不能为了卡面增加“本月省下多少钱”或评分。列表当前依据周期状态展示文案，但“已归档”不保证全部证据齐全，建议列表改写为“已归档／查看复盘”或“执行中／阶段回看”；真正的完整性以详情返回的 `reviewStatus` 为准，不额外逐条请求详情来制造一个列表评分。
- **空态：** 无周期引导到现有计划入口；读取失败不是“还没有记录”。

### P22 月度复盘详情

**文件：** `pages/review/index.vue`；布局 L02。

- **风格配色：** 白色分析卡，薄荷用于已确认事实，浅蓝用于计划对照；仍需核对使用警示色。
- **字体：** 月份／复盘标题 T02、关键金额 T09、对照值 T10、说明 T05。
- **顺序：** 复盘状态 → 已确认收支／保留目标 → 计划与实际 → 待核对事项 → 周期事件。
- **概览：** 关键收支可 2×2 排列；退款已到账与待到账分别显示，不作为互斥同一状态覆盖。
- **计划与实际：** 显示已覆盖、仍待发生、未覆盖普通支出及关联明细；不把“支出越多”画成“完成得越好”。
- **图形：** 允许基于同一项目真实已覆盖和剩余数据的对比条；不合并不同口径资金为饼图，不发明完成率。
- **时间线：** 左日期／节点，右事件内容；有原关联入口才可点击；未知事项不能默认收起而不可见。

### P23 资金影响详情

**文件：** `pages/impact/detail.vue`；布局 L02。

- **风格配色：** 根据 allowed／needs_adjustment／blocked／unknown 显示语义浅底结论卡；拆分事实统一白底。
- **字体：** 结论 T02、余量／缺口 T08、关键事实 T04／T10、原因 T05。
- **顺序：** 一句话结论＋核心数值 → 关键日期／影响 → 预算拆分 → 为什么 → 原有下一步。
- **布局：** 结论 C02／C04，其他 C01；预算拆分 2×2，小屏与大字号可单列；不改变金额计算。
- **单项模式：** 在标题下标明正在查看哪个项目，单项影响与周期整体状态分区。不能把移除项目对比当成保证可执行。
- **未知：** 主数值位置直接写“依据不足”，下面说明缺失事实；不能画绿色勾或把 `null` 转成 0。
- **操作：** 返回计划／问行止复用现有入口；解释页面本身不修改预算。

### P24 AI 规划草稿详情

**文件：** `pages/draft/detail.vue`；布局 L02／L03。

- **风格配色：** 草稿总览浅蓝，项目白色，用户确认字段薄荷标签，待补充警示标签。
- **字体：** 草稿状态 T03、项目名称 T04、金额 T10、来源与建议 T05。
- **顺序：** 草稿状态／保存时判断 → 当前周期选择或依据变化提醒 → 项目列表 → 采纳／放弃及边界。
- **项目卡：** 名称 → 日期、预算、属性 → 来源标签 → 建议说明 → 查看影响／继续完善；来源不能只靠颜色区分。
- **布局：** 字段宜纵排，来源放值的下一行或短标签；避免右侧一小列塞“金额＋建议＋待确认”。
- **周期选择：** 无绑定时显式选择本人可用周期；不能默认悄悄绑定别的月份。
- **影响弹层：** C06，同一真实依据前后对比；字段不完整时明确进入补充，而不是显示假预览。
- **确认边界：** “采纳草稿”和“加入正式计划”必须视觉区分，采纳不等于预算已写入；失效／放弃状态禁用相应写动作。

### P25 候选列表与报价核对

**文件：** `pages/offers/index.vue`；布局 L02。

- **风格配色：** 白色候选卡，原计划摘要轻底，报价核对区薄荷／警示语义底。
- **字体：** 候选名 T04、展示价格 T10、规则 T05、核对报价 T09。
- **顺序：** 所属计划／日期／估价 → 候选列表 → 当前选中报价与资金评估 → 原有后续动作。
- **卡片：** 标题与金额为主，描述最多短段，规则清晰；无真实封面时用分类图标，不搜图伪装成真实商户。
- **动作：** 详情是次按钮、核对报价是主要入口；创建意图位于真实评估结果之后。
- **报价：** 展示价、正式报价、与计划估价差额分开；有效期含必要时间信息，不仅展示月份日期而让用户误判有效。
- **边界：** 不增加购物车、销量、评分、折扣、秒杀；状态不允许时不能通过换色重新启用按钮。

### P26 候选详情

**文件：** `pages/catalog/detail.vue`；布局 L02。

- **风格配色：** 白色产品事实卡，类别图标轻彩底，购买方式与规则中性说明。
- **字体：** 名称 T02、展示价 T08、描述 T04、地点／规则 T05。
- **顺序：** 名称与类别 → 展示价 → 描述 → 地点／购买方式／取消规则 → 真实标签 → 返回核对报价。
- **布局：** 一张主卡＋一张规则卡即可，内边距 20／16；没有图片数据不增加大封面空白。
- **状态：** 仅供展示的候选明确说明不可在线建单，不能出现看似可用的“立即购买”。
- **边界：** 当前页面只浏览，报价仍由原核对流程取得；标签只来自服务端。

### P27 购买确认

**文件：** `pages/purchase/detail.vue`；布局 L03。

- **风格配色：** 白色 C06；确认报价为品牌重点，过期／版本问题为警示底。
- **字体：** 商品名 T03、确认报价 T08、原估价 T10、规则和有效期 T05。
- **顺序：** 对象／状态 → 计划估价与确认报价 → 日期／有效期 → 建单边界 → 明确确认按钮。
- **布局：** 价格对照并排，窄屏竖排；差额注明含义，不用划线价假装优惠。
- **动作：** 主按钮必须写“确认建立订单”；放弃为次按钮；已有订单显示恢复入口。
- **过期态：** 禁用旧确认、保留真实金额和过期说明，提供原重新报价入口；不能用新的倒计时伪造延长有效期。

### P28 订单与确认列表

**文件：** `pages/orders/index.vue`；布局 L02（若当前页无返回需补真实返回／来源兼容）。

- **风格配色：** 待用户确认区域浅蓝提示，订单列表白底；Simulation／Sandbox 使用可见环境标签。
- **字体：** 区块 T03、名称 T04、金额 T10、时间环境 T05。
- **顺序：** 等待确认的意图 → 本人订单；空分组用紧凑说明，不让两个巨大空卡占整屏。
- **列表：** C03 或轻 C01，名称和状态同排，金额下一排、日期／环境再下一排；状态长时换行。
- **交互：** 整卡进入相应意图／订单；刷新恢复原状态，不能重新创建对象。
- **边界：** 不增加接口未支持的订单搜索／筛选，不把未确认意图统计为订单数。

### P29 订单详情

**文件：** `pages/order/detail.vue`；布局 L02。

- **风格配色：** 白色事实卡＋状态语义摘要，环境标签明确；退款信息不默认整卡成功绿。
- **字体：** 商品名 T03、订单金额 T08、状态 T04、事实 T05／T04。
- **顺序：** 环境／订单对象／金额 → 付款状态与订单状态 → 退款事实 → 当前可执行动作 → 操作历史。
- **布局：** 付款与订单状态分两条，不用单一“已完成”徽章混合；金额与已到账退款保持独立。
- **动作：** 待付显示付款交接／关闭；已支付可申请取消时保留原入口；未知显示真实恢复／查看路径。
- **历史：** C03 时间线，名称、状态、时间和原操作链接；技术编号可换行，不能隐藏核对关键标识。
- **边界：** 进入详情不自动查单写入、不重复提交付款／退款；沿用现有后端约束。

### P30 退款与善后

**文件：** `pages/refund/detail.vue`；布局 L02。

- **风格配色：** 白色主卡，处理中浅蓝、待核验警示、实际到账成功绿。
- **字体：** 已核验到账 T08，预计退款 T10，阶段标题 T03，说明 T05。
- **顺序：** 退款概览 → 申请与规则费用 → 渠道批次 → 到账核验 → 可用复核动作。
- **阶段布局：** 明确“申请／渠道／到账”三个事实区域，不用随时间自动推进的动画进度条。
- **批次：** C03，每笔显示金额、环境、状态和时间；有 operationId 才提供可点击进度。
- **金额：** 预计、渠道已退、账户到账不得合并；无申请的状态明确“尚未申请”，不能让默认 0 看起来像已经完成零元退款。
- **动作：** 复核按钮保留可用条件和处理中状态；未到账时不能显示庆祝 IP。

### P31 付款交接

**文件：** `pages/payment/handoff.vue`；布局 L03。

- **风格配色：** 白底＋清晰环境标签；等待操作浅蓝、查单未知警示；不做外部支付品牌的仿真收银台。
- **字体：** 标题 T02、阶段标题 T03、说明 T04／T05、按钮 T11。
- **顺序：** 当前环境／交接状态 → 有效期与边界 → 外部操作入口或模拟进度 → 主动查单 → 真实查单结果。
- **布局：** 每阶段一张轻卡或同卡分区；按钮全宽、间距 12；小屏不放两个长按钮并排。
- **Sandbox：** 原打开收银台／平台适用交接方式保留；完成外部操作后主动查单，不能因为返回页面就标记成功。
- **Simulation：** 明确本地模拟，显示原处理进度入口，不画不存在的真实付款二维码。
- **过期／未知：** 原错误恢复入口可用，不通过样式隐藏重试间隔或服务端结果。

### P32 处理进度

**文件：** `pages/operation/detail.vue`；布局 L02。

- **风格配色：** 中性白卡＋语义状态图标；处理中浅蓝、需复核警示、失败危险。
- **字体：** 状态 T02、操作名称 T04、说明 T05。
- **顺序：** 当前状态 → 操作类型／说明 → 离开与恢复说明 → 返回订单。
- **布局：** 首张状态卡内图标 40、状态和短说明；不大面积空白，也不伪造 10%—100% 进度。
- **交互：** 保留真实轮询和离开后恢复机制；不因组件动画重复创建操作。
- **终态：** 停止纯装饰性的无限等待动画，显示结果和真实下一步；无关联订单时不放失效返回按钮。

### P33 意外支出与调整方案

**文件：** `pages/emergency/index.vue`；布局 L03。

- **风格配色：** 输入阶段白色表单；评估阶段警示／中性结论，方案选中薄荷边界，不默认绿色安全。
- **字体：** 字段 T04／T05、意外金额和缺口 T09、方案名称 T04。
- **顺序：** 金额／日期／原因 → 评估 → 基准判断 → 可选方案 → 确认所选方案／返回修改。
- **输入布局：** 字段纵排，金额大一些但单位清楚；标题保留现有业务含义，同时说明这是规划评估，不是新增银行已入账流水。
- **方案卡：** 单列 C01，每项显示名称、结论、缺口、具体变更；选中用勾＋边框，不能只靠色块。
- **确认：** 用户主动选择后才允许原确认动作；修改金额／日期后旧方案不能继续冒充最新结果。
- **边界：** 不添加自动推荐的“最佳方案”标签，除非服务端已有明确依据且本轮获准使用。

### P34 调整结果

**文件：** `pages/adjustment/detail.vue`；布局 L02。

- **风格配色：** 白色结果卡＋真实状态浅底；原订单善后与退款各自按状态着色。
- **字体：** 结果标题 T02、支出金额 T09、分组 T03、事实 T04／T05。
- **顺序：** 调整原因／当前状态 → 已确认安排与时间 → 原订单善后 → 查看订单／退款。
- **布局：** 第一张摘要 C02；每个关联取消请求为 C01／C03 行组，分别显示受理、费用、预计退款、渠道结果。
- **零关联态：** 紧凑说明“本次不涉及订单善后”，不显示巨大空失败框。
- **边界：** 调整 complete 不代表退款全部到账；不增加“资金已回到余额”等无事实支持的总结。

## 7. 商户与审核网页规格

管理端沿用 `xingzhi/apps/web`，不与消费者移动页面合并。下面的尺寸是普通 CSS px，不使用 D 换算。

### 7.1 管理端公共规格

- 背景 `#F7FAF7`、卡片白、主色 `#267D62`；文字和状态色沿用全局语义。
- 统一系统无衬线字体；页面标题 28/700，模块标题 20/600，正文和表格 14—16，元信息 13；不再普遍使用 11px 技术小字。
- 顶部 `AppShell` 高约 64，左品牌、右环境／身份／退出；窄屏换行，不能隐藏当前身份。
- 内容最大宽约 1280，左右留白 24—32；区块间距 24；卡片圆角 16、内边距 24。
- 表格头 14，行高至少 56；数字右对齐，状态与金额不挤在同一狭窄单元。
- ≤900 宽主内容改为单列；≤640 宽的表格改行卡或提供明确横向滚动。不能像现有部分样式那样把付款／订单状态直接隐藏掉。
- 主按钮高 44、圆角 12；键盘焦点可见；保留可访问标签、错误朗读与原生表单能力。
- 不在退款批准、审核缺失证据等区域放卡通装饰；IP 仅可用于登录／明确无任务空态。

### M01 管理端登录

**文件：** `views/LoginView.vue`、`styles.css`。

- **配色：** 左侧薄荷品牌区，右侧白色登录区；取消当前大面积深色与装饰宋体组合。
- **布局：** ≥900 宽左右约 45%／55%；登录表单最大宽 400；小屏品牌区缩为紧凑顶部，表单自然滚动。
- **字体：** 品牌标题 32、登录标题 24、表单 16、说明 14；品牌区不使用多行巨字挤压表单。
- **卡片：** 表单内部不重复套多层卡；字段间距 20、按钮距字段 24。
- **内容：** 明确“商户／审核工作台”，角色由服务端确定；测试账号说明保持可展开，不展示密码或密钥。
- **边界：** 不增加前端角色选择器来改变权限；不重写认证。

### M02 商户工作台

**文件：** `views/MerchantDashboard.vue` 及相关商户组件。

- **配色：** 白色订单区；待处理数量／状态使用浅蓝或警示；成功处理才用成功色。
- **顺序：** 身份与数据范围 → 原有反馈 → 订单列表 → 选中订单详情 → 取消退款队列／人工任务 → 历史入口。
- **布局：** 订单表全宽；选中详情放紧接表格的位置；队列和人工任务在宽屏各半，窄屏单列。
- **字体：** 列表名称 16，金额 18，状态 14，时间和编号 13；详情标题 20。
- **详情卡：** 确认金额、付款、已到账退款、操作数量四项统计，窄屏 2×2；业务号和版本完整可核对。
- **取消队列：** 申请、规则费用、预计退款、决定理由分组；批准／拒绝为明确独立动作，拒绝与取消不得共用含糊按钮。
- **人工任务：** 领取、记录结果、复核依照当前权限展示；不可凭本地乐观动画提前标为已解决。
- **历史：** 继续留在原 `details` 归档区域，不与现行消费者订单混排。
- **涉及组件：** `MerchantCancellationQueue`、`ManualTaskList`、`MerchantRulePanel`、`StatusPill`、`EmptyState`。

### M03 审核工作台

**文件：** `views/ReviewerDashboard.vue`。

- **配色：** 白色证据区、浅蓝只读摘要；证据缺失为警示，不能全页成功绿。
- **布局：** ≥1000 宽左周期索引 260、右证据自适应，间距 24；较窄时索引在上、证据在下，不新增页面路由。
- **字体：** 页面 28、模块 20、金额 20、正文 15—16、版本／时间 13—14。
- **证据顺序：** 周期摘要／授权范围 → 受控导出 → 缺失证据 → 订单与原确认 → 资金及后台操作 → 时间线。
- **导出：** 生成与下载保持分离；生成后才出现真实下载链接，不能画永远可点的假下载按钮。
- **表格：** 订单编号、金额、付款、订单状态完整保留；手机改行卡，而不是删列。
- **权限：** 页面明确只读，不出现审核者支付、退款或修改预算入口。
- **历史：** 原历史计划审核入口继续保留在独立归档区，不扩成新消费者编辑功能。

### M04 旧消费者访问管理站点时的迁移入口

**文件：** `App.vue` 的消费者分支；不是新增应用。

- 白色 C01 风格提示，标题 24、正文 16，一个品牌色按钮进入当前配置下的新版消费者端。
- 显示“消费者入口已统一”，不能变成空页面或重新展示旧写流程。
- `views/ConsumerDashboard.vue` 及仅服务旧流程的组件不是本轮要重新启用的页面；只在确实影响现有已展示历史区域时调整相应公共样式。
- 不扩大本轮范围去改部署地址系统；若现有硬编码跳转在目标环境不可用，列为独立问题并请示。

## 8. 数据绑定、后端配合与禁止伪造

### 8.1 首轮直接复用的能力

| UI 内容 | 现有入口／数据 | 实施边界 |
| --- | --- | --- |
| 用户、账户、默认账户、周期 | `useOverview`；`api.session/accounts/preferences/periods` | 不聚合用户不可见账户；不改变默认账户与周期归属规则 |
| 未来 30 天 | `api.rollingCashflow` | 保留资金／周期版本一致性与 unknown |
| 计划列表与单项影响 | `api.period`、`api.budgetItemImpact` | 区分单项与周期整体结论 |
| 计划编辑预览 | `api.budgetItemChangePreview` | 预览不写入，确认仍复核 |
| 账目统计、分类 | 现有账户 ledger 与分类接口 | posted、退款子集、未来义务保持既有口径 |
| 计划与流水关联 | `api.budgetLedgerLinks` 及 link/unlink 方法 | 用户确认、同账户同月、版本检查不改 |
| AI 提问、运行结果 | `api.startAgent/agentRun/agentRuns` | 无自动发送，无假流式，无重复提交 |
| AI 草稿 | `api.planningDraft` 及原采纳／放弃方法 | 字段来源、用户确认与正式计划分离 |
| 订单、退款、操作 | `api.order/orderAftercare/operation` 等 | 保留环境、阶段、幂等和恢复 |
| 环境说明 | `api.runtimeStatus` | 配置就绪不是所有真实业务已验收 |

这里只列出数据入口，不授权修改其协议。真正实施时再次读当前契约，代码发生变化时不能照搬本文假设。

### 8.2 本轮不需要、也不自行申请实现的后端能力

以下若用户以后要求，应另立需求并先请示后端变更：AI 历史问题摘要接口、跨会话连续记忆、长期储蓄目标、成就评分、真实银行同步、手工新增已入账流水、商品评分／图片服务、微信推送、头像上传。

不得为满足视觉稿偷偷增加字段、数据库迁移或模拟接口；缺少数据时使用真实空态，或移除未获授权的视觉元素。

### 8.3 文案与数据口径检查

- `null`／unknown ≠ 0；预计收入 ≠ 已入账；渠道退款成功 ≠ 账户到账。
- 正向流入 ≠ 工资收入，退款必须保留身份；总流入包含退款时必须标明。
- 计划草稿 ≠ 已激活预算；采纳草稿 ≠ 已逐项加入正式计划。
- 用户确认建单 ≠ 已付款；调整执行完成 ≠ 退款到账。
- 颜色、图标、动画不能比文字更早宣布成功。
- 只简化语言，不删除必要金额、日期、来源、风险和确认条件。

## 9. 实施阶段与文件落点

本轮按 S0—S6 执行，避免与项目原有 F1—F5 编号混淆。以下全部为待实施，不是完成记录。

| 阶段 | 内容 | 主要文件／页面 | 完成门槛 |
| --- | --- | --- | --- |
| S0 基线确认 | 记录现有页面、接口、工作区改动和受影响设备场景 | 本文、当前源码、开发状态 | 不覆盖用户改动；明确本批范围与不改内容 |
| S1 视觉基础 | 颜色、字号、布局、安全区、图标、按钮、卡片、弹层 | `_tokens.scss`、`app.scss`、`components` | 统一尺寸体系，组件无重叠、无未处理事件 |
| S2 三个视觉样板 | 首页、AI、账目详情及必要列表 | P02、P04、P05 | H5＋微信工具核对；展示样板供用户确认方向；真机键盘／返回优先验证 |
| S3 主页面闭环 | 登录、计划、我的；补齐 P05 统计和筛选 | P01、P03、P05、P06 | 五标签与登录一致，双卡保留，设置全部可见 |
| S4 所有二级页 | 按账户设置、计划草稿、交易恢复三组逐批推进 | P07—P34 | 34 个消费者页面全部按清单核对，无旧式小字弹层遗漏 |
| S5 管理端一致性 | 登录、商户、审核、旧消费者迁移提示 | M01—M04 | 品牌一致、权限／业务事实不变、窄屏不丢关键列 |
| S6 最终验收 | 视觉、可达性、错误态、连续操作、包体与设备 | 全部实际改动范围 | 明确 H5／微信工具／真机各自结果，不能互相替代 |

### 9.1 改动组织要求

- 先读取当前文件，再局部修改；不要整页复制另一个页面覆盖原有业务逻辑。
- 优先公共样式与局部组件复用，避免每个页面再写一套新颜色、字号和 `.chip`。
- 能通过 props／slot 的少量明确变体实现，就不新建平行版本组件。
- UI 样板阶段可以增加轻量统一图标组件；不要建设“设计系统后台”或通用渲染引擎。
- 公共组件和全局样式会影响样板之外的页面。每次修改后至少抽查主页面、长列表、表单、详情、弹层五类已有界面，不能只看样板截图就忽略其他页面回归。
- 不删除原页面的分支、确认弹层、错误恢复和历史入口，除非确认它只是重复装饰且不承载功能。
- 消费者 H5 和微信必须修改同一源文件，不通过复制目录维护两套界面。
- 不编辑 `dist` 充当源代码修改；产物必须由现有构建生成。
- 管理端与消费者端可以各有轻量样式 token，但色彩语义一致；本轮不建立新的跨应用组件包。

### 9.2 建议的定向验证命令

在仓库根目录执行；先确认脚本仍与当前 `package.json` 一致。

```powershell
# 消费者代码检查：修改消费者页面／组件后执行
pnpm --filter @xingzhi/miniapp typecheck

# 保留首页／AI 兼容性回归：改动涉及相应逻辑时执行
pnpm --filter @xingzhi/miniapp test:device

# 修改管理端后执行
pnpm --filter @xingzhi/web typecheck
```

影响模板、原生组件、素材和样式适配的批次，还需核对 H5／微信实际编译输出，可使用正在运行的监听编译；不必每改一行就重跑所有服务端测试。

如果需要重新建立真机联调环境，再使用：

```powershell
.\start-xingzhi.ps1 -Restart -Mobile -NoBrowser
```

这条命令会重启项目服务，应先告知用户；不要仅为编辑本文或查看视觉方案执行。热点地址在启动时识别，重新编译后再重新发起真机调试。不能将 `127.0.0.1` 的模拟器可用视为手机可用。

## 10. 验收清单与记录要求

### 10.1 全局视觉验收

- [ ] 字体族统一，金额不使用装饰字体，关键文字没有过浅灰或低对比度。
- [ ] 常规正文遵循至少 4.5:1 的对比度目标；正文不因浅色背景变得发虚。
- [ ] 主要按钮、显隐、返回、关闭、筛选等触控区符合本计划的尺寸目标。
- [ ] 320、360、390、430 宽，短屏与系统较大字号均无横向溢出。
- [ ] 首页双卡在上述宽度始终并排、金额完整、图标不挤压、两卡都可操作。
- [ ] 图标不再混用难以对齐的汉字和特殊字体符号；同类图标尺寸、基线一致。
- [ ] 卡片圆角、内边距、区块间距有统一规则，页面没有不必要的大空白。
- [ ] 页头避开状态栏与微信胶囊；底部不被原生导航或手势区遮挡。
- [ ] 长标题、长邮箱、长地址、大金额、负数、0、unknown、无记录、50 条以上已有列表均有可读布局。
- [ ] 页面不因加载、切换或动画丢失卡片、输入或已返回的数据。

### 10.2 逐页覆盖清单

| 编号 | 页面 | 设计实施 | H5 | 微信工具 | 真机 |
| --- | --- | --- | --- | --- | --- |
| P01 | 登录 | 待实施 | 待测 | 待测 | 待测 |
| P02 | 首页 | 待实施 | 待测 | 待测 | 待测 |
| P03 | 计划 | 待实施 | 待测 | 待测 | 待测 |
| P04 | 行止 AI | 待实施 | 待测 | 待测 | 待测 |
| P05 | 账目及详情／分类／关联／统计弹层 | 待实施 | 待测 | 待测 | 待测 |
| P06 | 我的及退出／撤回引导弹层 | 待实施 | 待测 | 待测 | 待测 |
| P07 | 个人资料 | 待实施 | 待测 | 待测 | 待测 |
| P08 | 历史计划 | 待实施 | 待测 | 待测 | 待测 |
| P09 | AI 历史与结果 | 待实施 | 待测 | 待测 | 待测 |
| P10 | 账户详情 | 待实施 | 待测 | 待测 | 待测 |
| P11 | 规划账户选择 | 待实施 | 待测 | 待测 | 待测 |
| P12 | 撤回账户授权 | 待实施 | 待测 | 待测 | 待测 |
| P13 | 授权与隐私 | 待实施 | 待测 | 待测 | 待测 |
| P14 | 数据说明 | 待实施 | 待测 | 待测 | 待测 |
| P15 | 应用内提醒 | 待实施 | 待测 | 待测 | 待测 |
| P16 | 关于／运行环境 | 待实施 | 待测 | 待测 | 待测 |
| P17 | 新建预算／调整目标 | 待实施 | 待测 | 待测 | 待测 |
| P18 | 周期详情／激活确认 | 待实施 | 待测 | 待测 | 待测 |
| P19 | 项目详情 | 待实施 | 待测 | 待测 | 待测 |
| P20 | 项目编辑／变更预览／取消 | 待实施 | 待测 | 待测 | 待测 |
| P21 | 复盘列表 | 待实施 | 待测 | 待测 | 待测 |
| P22 | 复盘详情 | 待实施 | 待测 | 待测 | 待测 |
| P23 | 资金影响 | 待实施 | 待测 | 待测 | 待测 |
| P24 | 草稿详情／周期选择／影响 | 待实施 | 待测 | 待测 | 待测 |
| P25 | 候选与报价核对 | 待实施 | 待测 | 待测 | 待测 |
| P26 | 候选详情 | 待实施 | 待测 | 待测 | 待测 |
| P27 | 购买确认 | 待实施 | 待测 | 待测 | 待测 |
| P28 | 订单与确认列表 | 待实施 | 待测 | 待测 | 待测 |
| P29 | 订单详情 | 待实施 | 待测 | 待测 | 待测 |
| P30 | 退款与善后 | 待实施 | 待测 | 待测 | 待测 |
| P31 | 付款交接 | 待实施 | 待测 | 待测 | 待测 |
| P32 | 处理进度 | 待实施 | 待测 | 待测 | 待测 |
| P33 | 意外支出与调整方案 | 待实施 | 待测 | 待测 | 待测 |
| P34 | 调整结果 | 待实施 | 待测 | 待测 | 待测 |
| M01 | 管理端登录 | 待实施 | 待测 | 不适用 | 手机浏览器待测 |
| M02 | 商户工作台 | 待实施 | 待测 | 不适用 | 手机浏览器待测 |
| M03 | 审核工作台 | 待实施 | 待测 | 不适用 | 手机浏览器待测 |
| M04 | 消费者迁移提示 | 待实施 | 待测 | 不适用 | 手机浏览器待测 |

本表只记录本次 UI 升级覆盖情况，不把历史功能已实现误写成新版 UI 已完成。正式阶段完成情况仍回写 `04-development.md`。

### 10.3 必须实测的交互

1. 登录：输入稳定、密码遮蔽、键盘确认、错误提示、成功跳转和退出重登。
2. 首页：连续执行“首页 → 计划 → 首页”至少 5 次；再从账户详情返回；不能白屏或丢失双卡。
3. AI：页面按钮发送、键盘发送、运行中切页返回、失败后重试、查看历史；不得重复发起同一运行，不隐藏输入区。
4. 账目：月份切换、搜索、五种筛选、长名称、详情、分类、关联计划；关联写操作只在隔离测试数据和用户授权范围内验证。
5. 计划：新增／编辑／影响预览／取消／归档分支；禁止为视觉验收对用户现有计划进行不可恢复操作。
6. 我的：所有设置可达、所有分组可见、账户显隐与首页一致；撤回只检查流程时不得真正点击最终确认。
7. 弹层：短屏、大字号、内容很长、键盘弹起、关闭再打开、原生 TabBar 遮挡；背景不能误触。
8. 网络：首次读取失败与刷新失败区分；原有内容可识别为旧数据；AI 提交失败不清空问题。
9. 状态：依据不足、无账户、已撤回、已归档、报价过期、支付未知、退款未到账，都不能变成成功视觉。
10. 管理端：键盘操作、窄屏表格、只读审核范围、真实导出入口；UI 测试不得擅自批准退款或更改商户规则。

### 10.4 包体与性能

- 复用现有分包；装饰图、图标不得把主包重新推到平台限制以上。
- 不将整张设计草图、IP 设计板或大字体放进运行包。
- 只增加实际使用的图标；优先复用现有素材，新增图片记录尺寸与体积。
- 图表、气泡、列表不引入无必要的定时动画；轮询仍由原业务控制。
- 核对当前构建与微信开发者工具实际包体；本文不承诺某个旧测量值仍是当前上传大小。

### 10.5 每批交付记录模板

```text
实施阶段／页面编号：
实际修改文件：
视觉变化：
保留的业务行为与确认边界：
运行的类型检查／定向测试／编译及结果：
H5 检查尺寸与结果：
微信开发者工具版本、基础库与结果：
真机机型、系统、微信版本、网络与结果：
仍未验证的项目：
发现的独立问题／需要用户决定的事项：
下一批范围：
```

不能把“编译通过”写成“真机通过”，不能把“接口返回成功”写成“所有页面已验收”，不能把待测试格提前填为通过。

## 11. 可直接交给 AI 的执行任务模板

以下内容是**未来获得代码修改授权后**使用的任务模板，不代表编写本文时已经授权或开始实施全部改版。

```text
请先完整阅读根目录《行止_UI升级详细实施计划_v2.md》、AGENTS.md，以及本批涉及的现有源码。

本次授权实施的范围是：【填写 S1 / S2 或明确的 Pxx、Mxx 页面编号】。
只执行这个范围，不自动扩展到其他阶段。若只让你评审，不得改应用代码。

设计方向：薄荷生活计划册。请严格使用文档的颜色、字体、L/D 尺寸约定、卡片体系、
布局顺序和状态规范，不要只换背景色。首页主账户与未来 30 天必须并排；保留现有 IP。
五个主页面、详情和弹层都以当前业务能力为边界，不能新增空按钮、假数据或未接接口。

消费者 H5 与微信小程序使用同一套源码；不创建两套页面。保留近期首页返回、Intl 兼容、
AI 发送恢复、安全区、原生组件保留、分包与热点联调相关修复。
优先复用现有组件，避免同名全局样式互相覆盖，不引入大型 UI／图表依赖。

本次默认不允许修改后端、数据库、资金算法、鉴权、幂等与支付协议。
若无法只靠现有接口完成某个设计点，请说明缺少什么并先询问，不用模拟数据掩盖。
未知值不是零，预计收入和退款不是已到账，采纳草稿不是写入预算，建单不是支付成功。

先列出本批改动文件和需要保留的行为，再按最小改动实施。
完成后按文档执行必要的类型检查、定向测试与对应端验证，不要求无关全量测试。
按第 10.5 节交付结果，明确实际已验和未验事项；需要真机时不能假称已经实测。

若本批是 S2，请先交付首页、AI、账目详情三个视觉样板及已验证结果，
等待用户确认风格后，再继续授权范围之外的页面推广。
```

## 12. 参考与文档核对说明

- 字体、配色与布局来自对当前仓库页面、组件、素材和原设计规范的检查，是本项目的设计选择，不是已经验证的所有青年用户偏好结论。
- 普通文字对比度参考 [W3C Contrast Minimum](https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum)。
- 本计划把 44×44 作为主要触控目标，参考 [W3C Target Size Enhanced](https://www.w3.org/WAI/WCAG22/Understanding/target-size-enhanced)；这是增强级设计目标，不应误写成所有场景的最低合规阈值。
- 本文中的图示仅说明位置与层级，不包含可直接上线的金融数据。
- 本轮文档检查范围：源码路径、消费者页面覆盖、文件链接、编号、单位约定和设计／业务边界。应用改版、构建、浏览器操作和真机验收须在后续获准实施后分别执行。
